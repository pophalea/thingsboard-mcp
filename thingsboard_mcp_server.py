import os
import json
import logging
from typing import Optional, Dict, Any, List
from mcp.server.fastmcp import FastMCP

# --- IMPORTS ---
try:
    if "PE" == "PE":
        from tb_rest_client.rest_client_pe import RestClientPE as RestClient
        import tb_rest_client.models.models_pe as models_pe
        import tb_rest_client.models.models_ce as models_ce
        
        def get_model_class(name):
            if hasattr(models_pe, name): return getattr(models_pe, name)
            if hasattr(models_ce, name): return getattr(models_ce, name)
            return None
    else:
        from tb_rest_client.rest_client_ce import RestClientCE as RestClient
        import tb_rest_client.models.models_ce as models_ce
        
        def get_model_class(name):
            if hasattr(models_ce, name): return getattr(models_ce, name)
            return None

    from tb_rest_client.rest import ApiException
except ImportError:
    print("Error: tb-rest-client not found")
    exit(1)

# --- CONFIG ---
mcp = FastMCP("ThingsBoard MCP (PE)")
TB_URL = os.getenv("TB_URL", "http://localhost:8080")
TB_USERNAME = os.getenv("TB_USERNAME", "tenant@thingsboard.org")
TB_PASSWORD = os.getenv("TB_PASSWORD", "tenant")

_client = None

def get_client():
    global _client
    if not _client:
        _client = RestClient(base_url=TB_URL)
        _client.login(username=TB_USERNAME, password=TB_PASSWORD)
    return _client

def deserialize_param(value: str, model_name: str):
    """Helper to convert LLM JSON strings back into SDK Objects."""
    if not value: return None
    try:
        data = json.loads(value)
        clazz = get_model_class(model_name)
        if clazz:
            return clazz(**data)
        return data
    except Exception:
        return value

def format_response(resp: Any) -> str:
    """Helper to convert SDK objects back to JSON strings."""
    if hasattr(resp, 'to_dict'):
        return json.dumps(resp.to_dict(), indent=2, default=str)
    if isinstance(resp, list):
        return json.dumps([x.to_dict() if hasattr(x, 'to_dict') else x for x in resp], indent=2, default=str)
    return str(resp)

# --- GENERATED TOOLS ---

@mcp.tool()
def accept_terms_of_use() -> str:
    """
    Accept Terms of Use (acceptTermsOfUse)  # noqa: E501

Accept Terms of Use by the current user.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.accept_terms_of_use()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'accept_terms_of_use'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def ack_alarm(alarm_id_json: str) -> str:
    """
    Acknowledge Alarm (ackAlarm)  # noqa: E501

Acknowledge the Alarm. Once acknowledged, the 'ack_ts' field will be set to current timestamp and special rule chain event 'ALARM_ACK' will be generated. Referencing non-existing Alarm Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AlarmInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.ack_alarm(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'ack_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def activate_email(email_code: str, pkg_name: Optional[str] = None) -> str:
    """
    Activate User using code from Email (activateEmail)  # noqa: E501

Activate the user using code(link) from the activation email. Validates the code an redirects according to the signup flow. Checks that user was not activated yet.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str email_code: Activation token. (required)
:param str pkg_name: Optional package name of the mobile application.
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.activate_email(email_code=email_code, pkg_name=pkg_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'activate_email'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def activate_instance(license_secret: str, release_date: str) -> str:
    """
    Activate edge instance (activateInstance)  # noqa: E501

Activates edge license on license portal.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str license_secret: licenseSecret (required)
:param str release_date: releaseDate (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.activate_instance(license_secret=license_secret, release_date=release_date)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'activate_instance'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def activate_user(body: str, send_activation_mail: bool) -> str:
    """
    Activate User  # noqa: E501

Checks the activation token and updates corresponding user password in the database. Now the user may start using his password to login. The response already contains the [JWT](https://jwt.io) activation and refresh tokens, to simplify the user activation flow and avoid asking user to input password again after activation. If token is valid, returns the object that contains [JWT](https://jwt.io/) access and refresh tokens. If token is not valid, returns '404 Bad Request'.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ActivateUserRequest body:
:param bool send_activation_mail: sendActivationMail
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.activate_user(body=body, send_activation_mail=send_activation_mail)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'activate_user'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def activate_user_by_email_code(email_code: str, pkg_name: Optional[str] = None) -> str:
    """
    Activate and login using code from Email (activateUserByEmailCode)  # noqa: E501

Activate the user using code(link) from the activation email and return the JWT Token. Sends the notification and email about user activation. Checks that user was not activated yet.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str email_code: Activation token. (required)
:param str pkg_name: Optional package name of the mobile application.
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.activate_user_by_email_code(email_code=email_code, pkg_name=pkg_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'activate_user_by_email_code'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def add_entities_to_entity_group(entity_group_id_json: str, body_json: str = None) -> str:
    """
    Add entities to the group (addEntitiesToEntityGroup)  # noqa: E501

Add entities to the specified entity group. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'ADD_TO_GROUP' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.add_entities_to_entity_group(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'add_entities_to_entity_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_alarm(alarm_id_json: str, assignee_id: str) -> str:
    """
    Assign/Reassign Alarm (assignAlarm)  # noqa: E501

Assign the Alarm. Once assigned, the 'assign_ts' field will be set to current timestamp and special rule chain event 'ALARM_ASSIGNED' (or ALARM_REASSIGNED in case of assigning already assigned alarm) will be generated. Referencing non-existing Alarm Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str assignee_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Alarm
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_alarm(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'), assignee_id=assignee_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_device_to_tenant(tenant_id_json: str, device_id_json: str) -> str:
    """
    Assign device to tenant (assignDeviceToTenant)  # noqa: E501

Creates assignment of the device to tenant. Thereafter tenant will be able to reassign the device to a customer.  Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'ASSIGN_TO_TENANT' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Device
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_device_to_tenant(tenant_id=deserialize_param(tenant_id_json, 'TenantId'), device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_device_to_tenant'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_entity_group_to_edge(edge_id_json: str, group_type: str, entity_group_id_json: str) -> str:
    """
    Assign entity group to edge (assignEntityGroupToEdge)  # noqa: E501

Creates assignment of an existing entity group to an instance of The Edge. Assignment works in async way - first, notification event pushed to edge service queue on platform. Second, remote edge service will receive a copy of assignment entity group (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once entity group will be delivered to edge service, edge will request entities of this group to be send to edge. Once entities will be delivered to edge service, they are going to be available for usage on remote edge instance.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str group_type: EntityGroup type (required)
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityGroup
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_entity_group_to_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), group_type=group_type, entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_entity_group_to_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_integration_to_edge(edge_id_json: str, integration_id_json: str) -> str:
    """
    Assign integration to edge (assignIntegrationToEdge)  # noqa: E501

Creates assignment of an existing integration edge template to an instance of The Edge. Assignment works in async way - first, notification event pushed to edge service queue on platform. Second, remote edge service will receive a copy of assignment integration (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once integration will be delivered to edge service, it's going to start locally.   Only integration edge template can be assigned to edge.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: edgeId (required)
:param str integration_id: integrationId (required)
:return: Integration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_integration_to_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), integration_id=deserialize_param(integration_id_json, 'IntegrationId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_integration_to_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_rule_chain_to_edge(edge_id_json: str, rule_chain_id_json: str) -> str:
    """
    Assign rule chain to edge (assignRuleChainToEdge)  # noqa: E501

Creates assignment of an existing rule chain to an instance of The Edge. Assignment works in async way - first, notification event pushed to edge service queue on platform. Second, remote edge service will receive a copy of assignment rule chain (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once rule chain will be delivered to edge service, it's going to start processing messages locally.   Only rule chain with type 'EDGE' can be assigned to edge.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: edgeId (required)
:param str rule_chain_id: ruleChainId (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_rule_chain_to_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_rule_chain_to_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def assign_scheduler_event_to_edge(edge_id_json: str, scheduler_event_id_json: str) -> str:
    """
    Assign scheduler event to edge (assignSchedulerEventToEdge)  # noqa: E501

Creates assignment of an existing scheduler event to an instance of The Edge. Assignment works in async way - first, notification event pushed to edge service queue on platform. Second, remote edge service will receive a copy of assignment scheduler event (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once scheduler event will be delivered to edge service, it is going to be available for usage on remote edge instance.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: SchedulerEventInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.assign_scheduler_event_to_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'assign_scheduler_event_to_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def auto_commit_settings_exists() -> str:
    """
    Check auto commit settings exists (autoCommitSettingsExists)  # noqa: E501

Check whether the auto commit settings exists.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.auto_commit_settings_exists()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'auto_commit_settings_exists'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def cancel_job(id_json: str) -> str:
    """
    Cancel job (cancelJob)  # noqa: E501

Cancels the job. The status of the job must be QUEUED, PENDING or RUNNING.  For a running job, all the tasks not yet processed will be discarded.  See the example of a cancelled job result in getJobById method description.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.cancel_job(id=deserialize_param(id_json, 'JobId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'cancel_job'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def change_owner_to_customer(owner_id_json: str, entity_id_json: str, body_json: str = None) -> str:
    """
    Change owner to customer (changeOwnerToCustomer)  # noqa: E501

Tenant/Customer changes Owner to Customer or sub-Customer. Sub-Customer can`t perform this operation!   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.change_owner_to_customer(owner_id=deserialize_param(owner_id_json, 'UserId'), entity_id=deserialize_param(entity_id_json, 'EntityId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'change_owner_to_customer'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def change_owner_to_tenant(owner_id_json: str, entity_id_json: str, body_json: str = None) -> str:
    """
    Change owner to tenant (changeOwnerToTenant)  # noqa: E501

Tenant changes Owner from Customer or sub-Customer to Tenant.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.change_owner_to_tenant(owner_id=deserialize_param(owner_id_json, 'UserId'), entity_id=deserialize_param(entity_id_json, 'EntityId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'change_owner_to_tenant'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def change_password(body: Optional[str] = None) -> str:
    """
    Change password for current User (changePassword)  # noqa: E501

Change the password for the User which credentials are used to perform this REST API call. Be aware that previously generated [JWT](https://jwt.io/) tokens will be still valid until they expire.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ChangePasswordRequest body:
:return: ObjectNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.change_password(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'change_password'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_activate_token(activate_token: str) -> str:
    """
    Check Activate User Token (checkActivateToken)  # noqa: E501

Checks the activation token and forwards user to 'Create Password' page. If token is valid, returns '303 See Other' (redirect) response code with the correct address of 'Create Password' page and same 'activateToken' specified in the URL parameters. If token is not valid, returns '409 Conflict'.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str activate_token: The activate token string. (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_activate_token(activate_token=activate_token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_activate_token'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_instance(body_json: str = None) -> str:
    """
    Check edge license (checkInstance)  # noqa: E501

Checks license request from edge service by forwarding request to license portal.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_instance(body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_instance'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_integration_connection(body: Optional[str] = None) -> str:
    """
    Check integration connectivity (checkIntegrationConnection)  # noqa: E501

Checks if the connection to the integration is established. Throws an error if the connection is not established. Example: Failed to connect to MQTT broker at host:port.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Integration body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_integration_connection(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_integration_connection'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_repository_access(body: Optional[str] = None) -> str:
    """
    Check repository access (checkRepositoryAccess)  # noqa: E501

Attempts to check repository access.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param RepositorySettings body:
:return: DeferredResultVoid
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_repository_access(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_repository_access'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_reset_token(reset_token: str) -> str:
    """
    Check password reset token (checkResetToken)  # noqa: E501

Checks the password reset token and forwards user to 'Reset Password' page. If token is valid, returns '303 See Other' (redirect) response code with the correct address of 'Reset Password' page and same 'resetToken' specified in the URL parameters. If token is not valid, returns '409 Conflict'.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str reset_token: The reset token string. (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_reset_token(reset_token=reset_token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_reset_token'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_tenant_can_update_plan(body: str) -> str:
    """
    checkTenantCanUpdatePlan  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: canUpdatePlanRequest (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_tenant_can_update_plan(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_tenant_can_update_plan'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_two_fa_verification_code(provider_type: str, verification_code: str) -> str:
    """
    Check 2FA verification code (checkTwoFaVerificationCode)  # noqa: E501

Checks 2FA verification code, and if it is correct the method returns a regular access and refresh token pair.  The API method is rate limited (using rate limit config from TwoFactorAuthSettings), and also will block a user after X unsuccessful verification attempts if such behavior is configured (in TwoFactorAuthSettings).  Will return a Bad Request error if provider is not configured for usage, and Too Many Requests error if rate limits are exceeded.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str provider_type: providerType (required)
:param str verification_code: verificationCode (required)
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_two_fa_verification_code(provider_type=provider_type, verification_code=verification_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_two_fa_verification_code'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def check_updates() -> str:
    """
    Check for new Platform Releases (checkUpdates)  # noqa: E501

Check notifications about new platform releases.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: UpdateMessage
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.check_updates()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'check_updates'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_delete(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_delete(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_delete'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_get(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_get(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_get'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_head(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_head(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_head'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_options(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_options(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_options'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_patch(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_patch(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_patch'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_post(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_post(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_post'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def chirp_stack_process_request_put(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.chirp_stack_process_request_put(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'chirp_stack_process_request_put'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def claim_device(device_token: str, body: Optional[str] = None) -> str:
    """
    Save claiming information (claimDevice)  # noqa: E501

Saves the information required for user to claim the device. See more info about claiming in the corresponding 'Claiming devices' platform documentation.  Example of the request payload:   ```json {"secretKey":"value", "durationMs":60000} ```  Note: both 'secretKey' and 'durationMs' is optional parameters. In case the secretKey is not specified, the empty string as a default value is used. In case the durationMs is not specified, the system parameter device.claim.duration is used.  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.claim_device(device_token=device_token, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'claim_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def clear_alarm(alarm_id_json: str) -> str:
    """
    Clear Alarm (clearAlarm)  # noqa: E501

Clear the Alarm. Once cleared, the 'clear_ts' field will be set to current timestamp and special rule chain event 'ALARM_CLEAR' will be generated. Referencing non-existing Alarm Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AlarmInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.clear_alarm(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'clear_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def clear_events_post(entity_id_json: str, body: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.clear_events_post(entity_id=deserialize_param(entity_id_json, 'EntityId'), body=body, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'clear_events_post'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def code_processing_url(code: str, state: str) -> str:
    """
    codeProcessingUrl  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str code: code (required)
:param str state: state (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.code_processing_url(code=code, state=state)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'code_processing_url'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def compare_entity_data_to_version(entity_id_json: str, version_id: str) -> str:
    """
    Compare entity data to version (compareEntityDataToVersion)  # noqa: E501

Returns an object with current entity data and the one at a specific version. Entity data structure is the same as stored in a repository.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str internal_entity_uuid: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str version_id: Version id, for example fd82625bdd7d6131cf8027b44ee967012ecaf990. Represents commit hash. (required)
:return: DeferredResultEntityDataDiff
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.compare_entity_data_to_version(entity_id=deserialize_param(entity_id_json, 'EntityId'), version_id=version_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'compare_entity_data_to_version'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def count_alarms_by_query(body_json: str) -> str:
    """
    Count Alarms by Query (countAlarmsByQuery)  # noqa: E501

Returns the number of alarms that match the query definition.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AlarmCountQuery body:
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.count_alarms_by_query(body=deserialize_param(body_json, 'AlarmCountQuery'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'count_alarms_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def count_by_device_group_and_empty_ota_package(ota_package_type: str, ota_package_id_json: str, entity_group_id_json: str) -> str:
    """
    Count devices by device profile  (countByDeviceProfileAndEmptyOtaPackage)  # noqa: E501

The platform gives an ability to load OTA (over-the-air) packages to devices. It can be done in two different ways: device scope or device profile scope.In the response you will find the number of devices with specified device profile, but without previously defined device scope OTA package. It can be useful when you want to define number of devices that will be affected with future OTA package  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_type: OTA package type (required)
:param str ota_package_id: otaPackageId (required)
:param str entity_group_id: entityGroupId (required)
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.count_by_device_group_and_empty_ota_package(ota_package_type=ota_package_type, ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'), entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'count_by_device_group_and_empty_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def count_by_device_profile_and_empty_ota_package(ota_package_type: str, device_profile_id_json: str) -> str:
    """
    Count devices by device profile  (countByDeviceProfileAndEmptyOtaPackage)  # noqa: E501

The platform gives an ability to load OTA (over-the-air) packages to devices. It can be done in two different ways: device scope or device profile scope.In the response you will find the number of devices with specified device profile, but without previously defined device scope OTA package. It can be useful when you want to define number of devices that will be affected with future OTA package  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_type: OTA package type (required)
:param str device_profile_id: Device Profile Id. I.g. '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.count_by_device_profile_and_empty_ota_package(ota_package_type=ota_package_type, device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'count_by_device_profile_and_empty_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def count_entities_by_query(body: Optional[str] = None) -> str:
    """
    Count Entities by Query  # noqa: E501

Allows to run complex queries to search the count of platform entities (devices, assets, customers, etc) based on the combination of main entity filter and multiple key filters. Returns the number of entities that match the query definition.  # Query Definition    Main **entity filter** is mandatory and defines generic search criteria. For example, "find all devices with profile 'Moisture Sensor'" or "Find all devices related to asset 'Building A'"  Optional **key filters** allow to filter results of the entity filter by complex criteria against main entity fields (name, label, type, etc), attributes and telemetry. For example, "temperature > 20 or temperature< 10" or "name starts with 'T', and attribute 'model' is 'T1000', and timeseries field 'batteryLevel' > 40".  Let's review the example:  ```json {   "entityFilter": {     "type": "entityType",     "entityType": "DEVICE"   },   "keyFilters": [     {       "key": {         "type": "ATTRIBUTE",         "key": "active"       },       "valueType": "BOOLEAN",       "predicate": {         "operation": "EQUAL",         "value": {           "defaultValue": true,           "dynamicValue": null         },         "type": "BOOLEAN"       }     }   ] } ```   Example mentioned above search all devices which have attribute 'active' set to 'true'. Now let's review available entity filters and key filters syntax:   # Entity Filters Entity Filter body depends on the 'type' parameter. Let's review available entity filter types. In fact, they do correspond to available dashboard aliases.  ## Single Entity  Allows to filter only one entity based on the id. For example, this entity filter selects certain device:  ```json {   "type": "singleEntity",   "singleEntity": {     "id": "d521edb0-2a7a-11ec-94eb-213c95f54092",     "entityType": "DEVICE"   } } ```  ## Entity List Filter  Allows to filter entities of the same type using their ids. For example, this entity filter selects two devices:  ```json {   "type": "entityList",   "entityType": "DEVICE",   "entityList": [     "e6501f30-2a7a-11ec-94eb-213c95f54092",     "e6657bf0-2a7a-11ec-94eb-213c95f54092"   ] } ```  ## Entity Name Filter  Allows to filter entities of the same type using the **'starts with'** expression over entity name. For example, this entity filter selects all devices which name starts with 'Air Quality':  ```json {   "type": "entityName",   "entityType": "DEVICE",   "entityNameFilter": "Air Quality" } ```  ## Entity Type Filter  Allows to filter entities based on their type (CUSTOMER, USER, DASHBOARD, ASSET, DEVICE, etc)For example, this entity filter selects all tenant customers:  ```json {   "type": "entityType",   "entityType": "CUSTOMER" } ```  ## Asset Type Filter  Allows to filter assets based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'charging station' assets which name starts with 'Tesla':  ```json {   "type": "assetType",   "assetType": "charging station",   "assetNameFilter": "Tesla" } ```  ## Device Type Filter  Allows to filter devices based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Temperature Sensor' devices which name starts with 'ABC':  ```json {   "type": "deviceType",   "deviceType": "Temperature Sensor",   "deviceNameFilter": "ABC" } ```  ## Edge Type Filter  Allows to filter edge instances based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Factory' edge instances which name starts with 'Nevada':  ```json {   "type": "edgeType",   "edgeType": "Factory",   "edgeNameFilter": "Nevada" } ```  ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```  ## Api Usage Filter  Allows to query for Api Usage based on optional customer id. If the customer id is not set, returns current tenant API usage.For example, this entity filter selects the 'Api Usage' entity for customer with id 'e6501f30-2a7a-11ec-94eb-213c95f54092':  ```json {   "type": "apiUsageState",   "customerId": {     "id": "d521edb0-2a7a-11ec-94eb-213c95f54092",     "entityType": "CUSTOMER"   } } ```  ## Relations Query Filter  Allows to filter entities that are related to the provided root entity. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'filter' object allows you to define the relation type and set of acceptable entity types to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only those who match the 'filters'.  For example, this entity filter selects all devices and assets which are related to the asset with id 'e51de0c0-2a7a-11ec-94eb-213c95f54092':  ```json {   "type": "relationsQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e51de0c0-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "filters": [     {       "relationType": "Contains",       "entityTypes": [         "DEVICE",         "ASSET"       ]     }   ] } ```  ## Asset Search Query  Allows to filter assets that are related to the provided root entity. Filters related assets based on the relation type and set of asset types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'assetTypes' defines the type of the asset to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only assets that match 'relationType' and 'assetTypes' conditions.  For example, this entity filter selects 'charging station' assets which are related to the asset with id 'e51de0c0-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "assetSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e51de0c0-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "relationType": "Contains",   "assetTypes": [     "charging station"   ] } ```  ## Device Search Query  Allows to filter devices that are related to the provided root entity. Filters related devices based on the relation type and set of device types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'deviceTypes' defines the type of the device to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Charging port' and 'Air Quality Sensor' devices which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "deviceSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 2,   "fetchLastLevelOnly": true,   "relationType": "Contains",   "deviceTypes": [     "Air Quality Sensor",     "Charging port"   ] } ```  ## Entity View Query  Allows to filter entity views that are related to the provided root entity. Filters related entity views based on the relation type and set of entity view types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'entityViewTypes' defines the type of the entity view to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Concrete mixer' entity views which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "entityViewSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "relationType": "Contains",   "entityViewTypes": [     "Concrete mixer"   ] } ```  ## Edge Search Query  Allows to filter edge instances that are related to the provided root entity. Filters related edge instances based on the relation type and set of edge types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'deviceTypes' defines the type of the device to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Factory' edge instances which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "deviceSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 2,   "fetchLastLevelOnly": true,   "relationType": "Contains",   "edgeTypes": [     "Factory"   ] } ```   # Key Filters Key Filter allows you to define complex logical expressions over entity field, attribute or latest time-series value. The filter is defined using 'key', 'valueType' and 'predicate' objects. Single Entity Query may have zero, one or multiple predicates. If multiple filters are defined, they are evaluated using logical 'AND'. The example below checks that temperature of the entity is above 20 degrees:  ```json {   "key": {     "type": "TIME_SERIES",     "key": "temperature"   },   "valueType": "NUMERIC",   "predicate": {     "operation": "GREATER",     "value": {       "defaultValue": 20,       "dynamicValue": null     },     "type": "NUMERIC"   } } ```   Now let's review 'key', 'valueType' and 'predicate' objects in detail.  ## Filter Key  Filter Key defines either entity field, attribute or telemetry. It is a JSON object that consists the key name and type. The following filter key types are supported:    * 'CLIENT_ATTRIBUTE' - used for client attributes;   * 'SHARED_ATTRIBUTE' - used for shared attributes;   * 'SERVER_ATTRIBUTE' - used for server attributes;   * 'ATTRIBUTE' - used for any of the above;   * 'TIME_SERIES' - used for time-series values;   * 'ENTITY_FIELD' - used for accessing entity fields like 'name', 'label', etc. The list of available fields depends on the entity type;   * 'ALARM_FIELD' - similar to entity field, but is used in alarm queries only;     Let's review the example:  ```json {   "type": "TIME_SERIES",   "key": "temperature" } ```  ## Value Type and Operations  Provides a hint about the data type of the entity field that is defined in the filter key. The value type impacts the list of possible operations that you may use in the corresponding predicate. For example, you may use 'STARTS_WITH' or 'END_WITH', but you can't use 'GREATER_OR_EQUAL' for string values.The following filter value types and corresponding predicate operations are supported:    * 'STRING' - used to filter any 'String' or 'JSON' values. Operations: EQUAL, NOT_EQUAL, STARTS_WITH, ENDS_WITH, CONTAINS, NOT_CONTAINS;   * 'NUMERIC' - used for 'Long' and 'Double' values. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;   * 'BOOLEAN' - used for boolean values. Operations: EQUAL, NOT_EQUAL;  * 'DATE_TIME' - similar to numeric, transforms value to milliseconds since epoch. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;    ## Filter Predicate  Filter Predicate defines the logical expression to evaluate. The list of available operations depends on the filter value type, see above. Platform supports 4 predicate types: 'STRING', 'NUMERIC', 'BOOLEAN' and 'COMPLEX'. The last one allows to combine multiple operations over one filter key.  Simple predicate example to check 'value < 100':   ```json {   "operation": "LESS",   "value": {     "defaultValue": 100,     "dynamicValue": null   },   "type": "NUMERIC" } ```  Complex predicate example, to check 'value < 10 or value > 20':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "operation": "GREATER",       "value": {         "defaultValue": 20,         "dynamicValue": null       },       "type": "NUMERIC"     }   ] } ```  More complex predicate example, to check 'value < 10 or (value > 50 && value < 60)':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "type": "COMPLEX",       "operation": "AND",       "predicates": [         {           "operation": "GREATER",           "value": {             "defaultValue": 50,             "dynamicValue": null           },           "type": "NUMERIC"         },         {           "operation": "LESS",           "value": {             "defaultValue": 60,             "dynamicValue": null           },           "type": "NUMERIC"         }       ]     }   ] } ```   You may also want to replace hardcoded values (for example, temperature > 20) with the more dynamic expression (for example, temperature > 'value of the tenant attribute with key 'temperatureThreshold'). It is possible to use 'dynamicValue' to define attribute of the tenant, customer or user that is performing the API call. See example below:   ```json {   "operation": "GREATER",   "value": {     "defaultValue": 0,     "dynamicValue": {       "sourceType": "CURRENT_USER",       "sourceAttribute": "temperatureThreshold"     }   },   "type": "NUMERIC" } ```   Note that you may use 'CURRENT_USER', 'CURRENT_CUSTOMER' and 'CURRENT_TENANT' as a 'sourceType'. The 'defaultValue' is used when the attribute with such a name is not defined for the chosen source.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityCountQuery body:
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.count_entities_by_query(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'count_entities_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def create_custom_menu(body: Optional[str] = None, assign_to_list_json: str = None, force: Optional[str] = None) -> str:
    """
    Create Custom Menu (createCustomMenu)  # noqa: E501

The api is designed to create Custom Menu without configuration. Is not applicable for update.  Security check is performed to verify that the user has 'WRITE' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param CustomMenuInfo body: (required)
:param list[object] assign_to_list: A list of entity ids, separated by comma ','
:param bool force: Use force if you want to create default menu that conflicts with the existing one (old one will be update NO_ASSIGN assignee type)
:return: CustomMenu
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.create_custom_menu(body=body, assign_to_list=json.loads(assign_to_list_json) if assign_to_list_json else None, force=force)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'create_custom_menu'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def create_notification_request(body_json: str) -> str:
    """
    Create notification request (createNotificationRequest)  # noqa: E501

Processes notification request. Mandatory request properties are `targets` (list of targets ids to send notification to), and either `templateId` (existing notification template id) or `template` (to send notification without saving the template). Optionally, you can set `sendingDelayInSec` inside the `additionalConfig` field to schedule the notification.  For each enabled delivery method in the notification template, there must be a target in the `targets` list that supports this delivery method: if you chose `WEB`, `EMAIL` or `SMS` - there must be at least one target in `targets` of `PLATFORM_USERS` type. For `SLACK` delivery method - you need to chose at least one `SLACK` notification target.  Notification request object with `PROCESSING` status will be returned immediately, and the notification sending itself is done asynchronously. After all notifications are sent, the `status` of the request becomes `SENT`. Use `getNotificationRequestById` to see the notification request processing status and some sending stats.   Here is an example of notification request to one target using saved template: ```json {   "templateId": {     "entityType": "NOTIFICATION_TEMPLATE",     "id": "6dbc3670-e4dd-11ed-9401-dbcc5dff78be"   },   "targets": [     "320e3ed0-d785-11ed-a06c-21dd57dd88ca"   ],   "additionalConfig": {     "sendingDelayInSec": 0   } } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationRequest body:
:return: NotificationRequest
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.create_notification_request(body=deserialize_param(body_json, 'NotificationRequest'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'create_notification_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_ai_model_by_id(ai_model_id_json: str) -> str:
    """
    Delete AI model by ID (deleteAiModelById)  # noqa: E501

Deletes the AI model record by its `id`. If a record with the specified `id` exists, the record is deleted and the endpoint returns `true`. If no such record exists, the endpoint returns `false`.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str model_uuid: ID of the AI model record (required)
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_ai_model_by_id(ai_model_id=deserialize_param(ai_model_id_json, 'AiModelId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_ai_model_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_alarm(alarm_id_json: str) -> str:
    """
    Delete Alarm (deleteAlarm)  # noqa: E501

Deletes the Alarm. Referencing non-existing Alarm Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_alarm(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_alarm_comment(alarm_id_json: str, comment_id_json: str) -> str:
    """
    Delete Alarm comment (deleteAlarmComment)  # noqa: E501

Deletes the Alarm comment. Referencing non-existing Alarm comment Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str comment_id: A string value representing the alarm comment id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_alarm_comment(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'), comment_id=deserialize_param(comment_id_json, 'AlarmCommentId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_alarm_comment'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_asset(asset_id_json: str) -> str:
    """
    Delete asset (deleteAsset)  # noqa: E501

Deletes the asset and all the relations (from and to the asset). Referencing non-existing asset Id will cause an error.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_id: A string value representing the asset id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_asset(asset_id=deserialize_param(asset_id_json, 'AssetId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_asset'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_asset_profile(asset_profile_id_json: str) -> str:
    """
    Delete asset profile (deleteAssetProfile)  # noqa: E501

Deletes the asset profile. Referencing non-existing asset profile Id will cause an error. Can't delete the asset profile if it is referenced by existing assets.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_asset_profile(asset_profile_id=deserialize_param(asset_profile_id_json, 'AssetProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_asset_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_auto_commit_settings() -> str:
    """
    Delete auto commit settings (deleteAutoCommitSettings)  # noqa: E501

Deletes the auto commit settings.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_auto_commit_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_auto_commit_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_blob_entity(blob_entity_id_json: str) -> str:
    """
    Delete Blob Entity (deleteBlobEntity)  # noqa: E501

Delete Blob entity based on the provided Blob entity Id. Referencing non-existing Blob entity Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str blob_entity_id: A string value representing the blob entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_blob_entity(blob_entity_id=deserialize_param(blob_entity_id_json, 'BlobEntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_blob_entity'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_calculated_field(calculated_field_id: str) -> str:
    """
    Delete Calculated Field (deleteCalculatedField)  # noqa: E501

Deletes the calculated field. Referencing non-existing Calculated Field Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object calculated_field_id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_calculated_field(calculated_field_id=calculated_field_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_calculated_field'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_client_registration_template(client_registration_template_id_json: str) -> str:
    """
    Delete OAuth2 client registration template by id (deleteClientRegistrationTemplate)  Available for users with 'SYS_ADMIN' authority.  # noqa: E501

Client registration template is OAuth2 provider configuration template with default settings for registering new OAuth2 clients  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str client_registration_template_id: String representation of client registration template id to delete (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_client_registration_template(client_registration_template_id=deserialize_param(client_registration_template_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_client_registration_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_converter(converter_id_json: str) -> str:
    """
    Delete converter (deleteConverter)  # noqa: E501

Deletes the converter and all the relations (from and to the converter). Referencing non-existing converter Id will cause an error. If the converter is associated with the integration, it will not be allowed for deletion.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str converter_id: A string value representing the converter id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_converter(converter_id=deserialize_param(converter_id_json, 'ConverterId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_current_login_white_label_params(customer_id_json: str) -> str:
    """
    Delete Login White Labeling configuration (deleteCurrentLoginWhiteLabelParams)  # noqa: E501

Delete the Login White Labeling configuration that corresponds to the authority of the user.   Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_current_login_white_label_params(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_current_login_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_current_white_label_params(customer_id_json: str) -> str:
    """
    Delete General White Labeling configuration (deleteCurrentWhiteLabelParams)  # noqa: E501

Delete the White Labeling configuration that corresponds to the authority of the user.   Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_current_white_label_params(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_current_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_custom_menu(custom_menu_id_json: str, force: Optional[str] = None) -> str:
    """
    Delete custom menu (deleteCustomMenu)  # noqa: E501

Deletes the custom menu based on the provided Custom Menu Id. Referencing non-existing custom menu Id will cause an error. If the custom menu is assigned to the list of users or customers bad request is returned.To delete a custom menu that has assignee list set 'force' request param to true   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool force: Force set to true will unassign menu before deletion
:return: CustomMenuDeleteResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_custom_menu(custom_menu_id=deserialize_param(custom_menu_id_json, 'CustomMenuId'), force=force)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_custom_menu'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_custom_translation(locale_code: str) -> str:
    """
    Delete Custom Translation for specified locale (deleteCustomTranslation)  # noqa: E501

Delete entire custom translation settings for end-user  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_custom_translation(locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_custom_translation_key(locale_code: str, key_path: str) -> str:
    """
    Delete specified key of Custom Translation (deleteCustomTranslationKey)   # noqa: E501

The API call is designed to delete specified key of the custom translation and return as a result parent translation.(e.g. if tenant translation for key is 'value1' and customer translation is 'value2' then by deletinf key onn customer level you will get 'value1' in response)   Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:param str key_path: A string value representing key of the custom translation (e.g. 'notification.active'). (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_custom_translation_key(locale_code=locale_code, key_path=key_path)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_custom_translation_key'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_customer(customer_id_json: str) -> str:
    """
    Delete Customer (deleteCustomer)  # noqa: E501

Deletes the Customer and all customer Users. All assigned Dashboards, Assets, Devices, etc. will be unassigned but not deleted. Referencing non-existing Customer Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_customer(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_customer'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_dashboard(dashboard_id_json: str) -> str:
    """
    Delete the Dashboard (deleteDashboard)  # noqa: E501

Delete the Dashboard. Only users with 'TENANT_ADMIN') authority may delete the dashboards.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str dashboard_id: A string value representing the dashboard id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_dashboard(dashboard_id=deserialize_param(dashboard_id_json, 'DashboardId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_dashboard'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_device(device_id_json: str) -> str:
    """
    Delete device (deleteDevice)  # noqa: E501

Deletes the device, it's credentials and all the relations (from and to the device). Referencing non-existing device Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_device(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_device_attributes(device_id_json: str, scope: str, keys: str) -> str:
    """
    Delete device attributes (deleteDeviceAttributes)  # noqa: E501

Delete device attributes using provided Device Id, scope and a list of keys. Referencing a non-existing Device Id will cause an error  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param str keys: A string value representing the comma-separated list of attributes keys. For example, 'active,inactivityAlarmTime'. (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_device_attributes(device_id=deserialize_param(device_id_json, 'DeviceId'), scope=scope, keys=keys)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_device_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_device_group_ota_package(id: str) -> str:
    """
    deleteDeviceGroupOtaPackage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_device_group_ota_package(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_device_group_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_device_profile(device_profile_id_json: str) -> str:
    """
    Delete device profile (deleteDeviceProfile)  # noqa: E501

Deletes the device profile. Referencing non-existing device profile Id will cause an error. Can't delete the device profile if it is referenced by existing devices.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_device_profile(device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_device_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_domain(domain_id_json: str) -> str:
    """
    Delete Domain by ID (deleteDomain)  # noqa: E501

Deletes Domain by ID. Referencing non-existing domain Id will cause an error.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_domain(domain_id=deserialize_param(domain_id_json, 'DomainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_domain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_edge(edge_id_json: str) -> str:
    """
    Delete edge (deleteEdge)  # noqa: E501

Deletes the edge. Referencing non-existing edge Id will cause an error.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_entity_attributes(entity_id_json: str, scope: str, keys: str) -> str:
    """
    Delete entity attributes (deleteEntityAttributes)  # noqa: E501

Delete entity attributes using provided Entity Id, scope and a list of keys. Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param str keys: A string value representing the comma-separated list of attributes keys. For example, 'active,inactivityAlarmTime'. (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_entity_attributes(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, keys=keys)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_entity_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_entity_group(entity_group_id_json: str) -> str:
    """
    Delete Entity Group (deleteEntityGroup)  # noqa: E501

Deletes the entity group but does not delete the entities in the group, since they are also present in reserved group 'All'. Referencing non-existing Entity Group Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'DELETE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_entity_group(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_entity_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_entity_timeseries(entity_id_json: str, keys: str, delete_all_data_for_keys: Optional[str] = None, start_ts: Optional[str] = None, end_ts: Optional[str] = None, delete_latest: Optional[str] = None, rewrite_latest_if_deleted: Optional[str] = None) -> str:
    """
    Delete entity time-series data (deleteEntityTimeseries)  # noqa: E501

Delete time-series for selected entity based on entity id, entity type and keys. Use 'deleteAllDataForKeys' to delete all time-series data. Use 'startTs' and 'endTs' to specify time-range instead.  Use 'deleteLatest' to delete latest value (stored in separate table for performance) if the value's timestamp matches the time-range.  Use 'rewriteLatestIfDeleted' to rewrite latest value (stored in separate table for performance) if the value's timestamp matches the time-range and 'deleteLatest' param is true. The replacement value will be fetched from the 'time-series' table, and its timestamp will be the most recent one before the defined time-range.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str keys: A string value representing the comma-separated list of telemetry keys. If keys are not selected, the result will return all latest timeseries. For example, 'temperature,humidity'. (required)
:param bool delete_all_data_for_keys: A boolean value to specify if should be deleted all data for selected keys or only data that are in the selected time range.
:param int start_ts: A long value representing the start timestamp of removal time range in milliseconds.
:param int end_ts: A long value representing the end timestamp of removal time range in milliseconds.
:param bool delete_latest: If the parameter is set to true, the latest telemetry can be removed, otherwise, in case that parameter is set to false the latest value will not removed.
:param bool rewrite_latest_if_deleted: If the parameter is set to true, the latest telemetry will be rewritten in case that current latest value was removed, otherwise, in case that parameter is set to false the new latest value will not set.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_entity_timeseries(entity_id=deserialize_param(entity_id_json, 'EntityId'), keys=keys, delete_all_data_for_keys=delete_all_data_for_keys, start_ts=start_ts, end_ts=end_ts, delete_latest=delete_latest, rewrite_latest_if_deleted=rewrite_latest_if_deleted)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_entity_timeseries'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_entity_view(entity_view_id_json: str) -> str:
    """
    Delete entity view (deleteEntityView)  # noqa: E501

Delete the EntityView object based on the provided entity view id.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_view_id: A string value representing the entity view id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_entity_view(entity_view_id=deserialize_param(entity_view_id_json, 'EntityViewId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_entity_view'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_group_permission(group_permission_id_json: str) -> str:
    """
    Delete group permission (deleteGroupPermission)  # noqa: E501

Deletes the group permission. Referencing non-existing group permission Id will cause an error.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_permission_id: A string value representing the group permission id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_group_permission(group_permission_id=deserialize_param(group_permission_id_json, 'GroupPermissionId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_group_permission'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_image(_type: str, key: str, force: Optional[str] = None) -> str:
    """
    deleteImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param bool force: force
:return: TbImageDeleteResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_image(_type=_type, key=key, force=force)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_integration(integration_id_json: str) -> str:
    """
    Delete integration (deleteIntegration)  # noqa: E501

Deletes the integration and all the relations (from and to the integration). Referencing non-existing integration Id will cause an error.    Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_id: A string value representing the integration id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_integration(integration_id=deserialize_param(integration_id_json, 'IntegrationId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_integration'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_job(id_json: str) -> str:
    """
    deleteJob  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_job(id=deserialize_param(id_json, 'JobId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_job'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_mobile_app(mobile_app_id_json: str) -> str:
    """
    Delete Mobile App by ID (deleteMobileApp)  # noqa: E501

Deletes Mobile App by ID. Referencing non-existing mobile app Id will cause an error.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_mobile_app(mobile_app_id=deserialize_param(mobile_app_id_json, 'MobileAppId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_mobile_app'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_mobile_app_bundle(mobile_app_bundle_id_json: str) -> str:
    """
    Delete Mobile App Bundle by ID (deleteMobileAppBundle)  # noqa: E501

Deletes Mobile App Bundle by ID. Referencing non-existing mobile app bundle Id will cause an error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_mobile_app_bundle(mobile_app_bundle_id=deserialize_param(mobile_app_bundle_id_json, 'MobileAppBundleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_mobile_app_bundle'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_notification(id: str) -> str:
    """
    Delete notification (deleteNotification)  # noqa: E501

Deletes notification by its id.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_notification(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_notification'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_notification_request(id: str) -> str:
    """
    Delete notification request (deleteNotificationRequest)  # noqa: E501

Deletes notification request by its id.  If the request has status `SENT` - all sent notifications for this request will be deleted. If it is `SCHEDULED`, the request will be cancelled.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_notification_request(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_notification_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_notification_rule(id: str) -> str:
    """
    Delete notification rule (deleteNotificationRule)  # noqa: E501

Deletes notification rule by id. Cancels all related scheduled notification requests (e.g. due to escalation table)  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_notification_rule(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_notification_rule'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_notification_target_by_id(id: str) -> str:
    """
    Delete notification target by id (deleteNotificationTargetById)  # noqa: E501

Deletes notification target by its id.  This target cannot be referenced by existing scheduled notification requests or any notification rules.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_notification_target_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_notification_target_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_notification_template_by_id(id: str) -> str:
    """
    Delete notification template by id (deleteNotificationTemplateById  # noqa: E501

Deletes notification template by its id.  This template cannot be referenced by existing scheduled notification requests or any notification rules.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_notification_template_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_notification_template_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_oauth2_client(id_json: str) -> str:
    """
    Delete oauth2 client (deleteOauth2Client)  # noqa: E501

Deletes the oauth2 client. Referencing non-existing oauth2 client Id will cause an error.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_oauth2_client(id=deserialize_param(id_json, 'OAuth2ClientId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_oauth2_client'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_ota_package(ota_package_id_json: str) -> str:
    """
    Delete OTA Package (deleteOtaPackage)  # noqa: E501

Deletes the OTA Package. Referencing non-existing OTA Package Id will cause an error. Can't delete the OTA Package if it is referenced by existing devices or device profile.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_id: A string value representing the ota package id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_ota_package(ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_queue(queue_id_json: str) -> str:
    """
    Delete Queue (deleteQueue)  # noqa: E501

Deletes the Queue.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str queue_id: A string value representing the queue id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_queue(queue_id=deserialize_param(queue_id_json, 'QueueId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_queue'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_relation(from_id_json: str, relation_type: str, to_id_json: str, relation_type_group: Optional[str] = None) -> str:
    """
    Delete Relation (deleteRelation)  # noqa: E501

Deletes a relation between two entities in the platform.   If the user has the authority of 'System Administrator', the server checks that 'from' and 'to' entities are owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that 'from' and 'to' entities are owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the 'from' and 'to' entities are assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str from_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str from_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type: A string value representing relation type between entities. For example, 'Contains', 'Manages'. It can be any string value. (required)
:param str to_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str to_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_relation(from_id=deserialize_param(from_id_json, 'EntityId'), relation_type=relation_type, to_id=deserialize_param(to_id_json, 'EntityId'), relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_relation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_relations(entity_id_json: str) -> str:
    """
    Delete common relations (deleteCommonRelations)  # noqa: E501

Deletes all the relations ('from' and 'to' direction) for the specified entity and relation type group: 'COMMON'.   If the user has the authority of 'System Administrator', the server checks that the entity is owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that the entity is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the entity is assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_relations(entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_relations'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_report_template(report_template_id_json: str) -> str:
    """
    Delete Report Template (deleteReportTemplate)  # noqa: E501

Deletes the report template. Referencing non-existing Report Template Id will cause 'Not Found' error.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str report_template_id: A string value representing the report template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_report_template(report_template_id=deserialize_param(report_template_id_json, 'ReportTemplateId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_report_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_repository_settings() -> str:
    """
    Delete repository settings (deleteRepositorySettings)  # noqa: E501

Deletes the repository settings.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: DeferredResultVoid
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_repository_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_repository_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_role(role_id_json: str) -> str:
    """
    Delete role (deleteRole)  # noqa: E501

Deletes the role. Referencing non-existing role Id will cause an error.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str role_id: A string value representing the role id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_role(role_id=deserialize_param(role_id_json, 'RoleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_role'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_rpc(rpc_id_json: str) -> str:
    """
    Delete persistent RPC  # noqa: E501

Deletes the persistent RPC request.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rpc_id: A string value representing the rpc id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_rpc(rpc_id=deserialize_param(rpc_id_json, 'RpcId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_rpc'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_rule_chain(rule_chain_id_json: str) -> str:
    """
    Delete rule chain (deleteRuleChain)  # noqa: E501

Deletes the rule chain. Referencing non-existing rule chain Id will cause an error. Referencing rule chain that is used in the device profiles will cause an error.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_rule_chain(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_scheduler_event(scheduler_event_id_json: str) -> str:
    """
    Delete Scheduler Event (deleteSchedulerEvent)  # noqa: E501

Deletes the scheduler event. Referencing non-existing Scheduler Event Id will cause 'Not Found' error.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_scheduler_event(scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_scheduler_event'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_secret(secret_id_json: str) -> str:
    """
    Delete secret by ID (deleteSecret)  # noqa: E501

Deletes the secret. Referencing non-existing Secret Id will cause an error.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: TbSecretDeleteResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_secret(secret_id=deserialize_param(secret_id_json, 'SecretId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_secret'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_self_registration_params(domain_name: str) -> str:
    """
    deleteSelfRegistrationParams  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str domain_name: domainName (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_self_registration_params(domain_name=domain_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_tenant(tenant_id_json: str) -> str:
    """
    Delete Tenant (deleteTenant)  # noqa: E501

Deletes the tenant, it's customers, rule chains, devices and all other related entities. Referencing non-existing tenant Id will cause an error.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_tenant(tenant_id=deserialize_param(tenant_id_json, 'TenantId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_tenant'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_tenant_profile(tenant_profile_id_json: str) -> str:
    """
    Delete Tenant Profile (deleteTenantProfile)  # noqa: E501

Deletes the tenant profile. Referencing non-existing tenant profile Id will cause an error. Referencing profile that is used by the tenants will cause an error.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_profile_id: A string value representing the tenant profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_tenant_profile(tenant_profile_id=deserialize_param(tenant_profile_id_json, 'TenantProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_tenant_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_two_fa_account_config(provider_type: str) -> str:
    """
    Delete 2FA account config (deleteTwoFaAccountConfig)  # noqa: E501

Delete 2FA config for a given 2FA provider type.  Returns whole account's 2FA settings object.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str provider_type: providerType (required)
:return: AccountTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_two_fa_account_config(provider_type=provider_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_two_fa_account_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_user(user_id_json: str) -> str:
    """
    Delete User (deleteUser)  # noqa: E501

Deletes the User, it's credentials and all the relations (from and to the User). Referencing non-existing User Id will cause an error.    Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_user(user_id=deserialize_param(user_id_json, 'UserId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_user'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_user_settings(paths_json: str, type: str) -> str:
    """
    Delete user settings (deleteUserSettings)  # noqa: E501

Delete user settings by specifying list of json element xpaths.   Example: to delete B and C element in { "A": {"B": 5}, "C": 15} send A.B,C in jsonPaths request parameter  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str paths: paths (required)
:param str type: Settings type, case insensitive, one of: "general", "quick_links", "doc_links" or "dashboards". (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_user_settings(paths=json.loads(paths_json) if paths_json else None, type=type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_user_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_web_self_registration_params() -> str:
    """
    deleteWebSelfRegistrationParams  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_web_self_registration_params()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_web_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_widget_type(widget_type_id_json: str) -> str:
    """
    Delete widget type (deleteWidgetType)  # noqa: E501

Deletes the  Widget Type. Referencing non-existing Widget Type Id will cause an error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widget_type_id: A string value representing the widget type id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_widget_type(widget_type_id=deserialize_param(widget_type_id_json, 'WidgetTypeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_widget_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def delete_widgets_bundle(widgets_bundle_id_json: str) -> str:
    """
    Delete widgets bundle (deleteWidgetsBundle)  # noqa: E501

Deletes the widget bundle. Referencing non-existing Widget Bundle Id will cause an error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: A string value representing the widget bundle id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.delete_widgets_bundle(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'delete_widgets_bundle'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_blob_entity(blob_entity_id_json: str) -> str:
    """
    Download Blob Entity By Id (downloadBlobEntity)  # noqa: E501

Download report file based on the provided Blob entity Id. Referencing non-existing Blob entity Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str blob_entity_id: A string value representing the blob entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Resource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_blob_entity(blob_entity_id=deserialize_param(blob_entity_id_json, 'BlobEntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_blob_entity'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_dashboard_report(dashboard_id_json: str, body_json: str = None) -> str:
    """
    Download dashboard report (downloadDashboardReport)  # noqa: E501

Generate and download a report from the specified dashboard. The request payload is a JSON object with params of report. For example:  ```json {     "type": "pdf",     "timezone": "Europe/Kiev",     "timewindow": {         "displayValue": "",         "hideInterval": false,         "hideLastInterval": false,         "hideQuickInterval": false,         "hideAggregation": false,         "hideAggInterval": false,         "hideTimezone": false,         "selectedTab": 0,         "realtime": {             "realtimeType": 0,             "interval": 1000,             "timewindowMs": 60000,             "quickInterval": "CURRENT_DAY"         },         "history": {             "historyType": 0,             "interval": 1000,             "timewindowMs": 60000,             "fixedTimewindow": {                 "startTimeMs": 1703687976592,                 "endTimeMs": 1703774376592             },             "quickInterval": "CURRENT_DAY"         },         "aggregation": {             "type": "AVG",             "limit": 25000         }     },     "state": null } ```   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: (required)
:param str dashboard_id: A string value representing the dashboard id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_dashboard_report(dashboard_id=deserialize_param(dashboard_id_json, 'DashboardId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_dashboard_report'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_full_translation(locale_code: str) -> str:
    """
    Download end-user all-to-one translation (downloadFullTranslation)  # noqa: E501

Fetch the end-user translation for the specified locale. The result is a json file with merged user custom translation, system language translation and default locale translation.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_full_translation(locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_full_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_gateway_docker_compose(device_id_json: str) -> str:
    """
    Download generated docker-compose.yml file for gateway (downloadGatewayDockerCompose)  # noqa: E501

Download generated docker-compose.yml for gateway.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Resource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_gateway_docker_compose(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_gateway_docker_compose'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_image(_type: str, key: str, if_none_match: str = "") -> str:
    """
    downloadImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_image(_type=_type, key=key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_image_preview(_type: str, key: str, if_none_match: str = "") -> str:
    """
    downloadImagePreview  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_image_preview(_type=_type, key=key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_image_preview'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_jks_resource_if_changed(resource_id_json: str, if_none_match: str = "") -> str:
    """
    Download JKS Resource (downloadJksResourceIfChanged)  # noqa: E501

Download Resource based on the provided Resource Id or return 304 status code if resource was not changed.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_jks_resource_if_changed(resource_id=deserialize_param(resource_id_json, 'EntityId'), if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_jks_resource_if_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_js_resource_if_changed(resource_id_json: str, if_none_match: str = "") -> str:
    """
    Download JS Resource (downloadJsResourceIfChanged)  # noqa: E501

Download Resource based on the provided Resource Id or return 304 status code if resource was not changed.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_js_resource_if_changed(resource_id=deserialize_param(resource_id_json, 'EntityId'), if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_js_resource_if_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_login_favicon(type: str, key: str, if_none_match: str) -> str:
    """
    downloadLoginFavicon  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_login_favicon(type=type, key=key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_login_favicon'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_login_logo(type: str, key: str, if_none_match: str) -> str:
    """
    downloadLoginLogo  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_login_logo(type=type, key=key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_login_logo'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_lwm2m_resource_if_changed(resource_id_json: str, if_none_match: str = "") -> str:
    """
    Download LWM2M Resource (downloadLwm2mResourceIfChanged)  # noqa: E501

Download Resource based on the provided Resource Id or return 304 status code if resource was not changed.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_lwm2m_resource_if_changed(resource_id=deserialize_param(resource_id_json, 'EntityId'), if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_lwm2m_resource_if_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_ota_package(ota_package_id_json: str) -> str:
    """
    Download OTA Package (downloadOtaPackage)  # noqa: E501

Download OTA Package based on the provided OTA Package Id.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_id: A string value representing the ota package id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Resource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_ota_package(ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_pkcs12_resource_if_changed(resource_id_json: str, if_none_match: str = "") -> str:
    """
    Download PKCS_12 Resource (downloadPkcs12ResourceIfChanged)  # noqa: E501

Download Resource based on the provided Resource Id or return 304 status code if resource was not changed.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_pkcs12_resource_if_changed(resource_id=deserialize_param(resource_id_json, 'EntityId'), if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_pkcs12_resource_if_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_public_image(public_resource_key: str, if_none_match: str = "") -> str:
    """
    downloadPublicImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str public_resource_key: publicResourceKey (required)
:param str if_none_match: If-None-Match
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_public_image(public_resource_key=public_resource_key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_public_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_resource(resource_id_json: str) -> str:
    """
    Download Resource (downloadResource)  # noqa: E501

Download Resource based on the provided Resource Id.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: ByteArrayResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_resource(resource_id=deserialize_param(resource_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_resource'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_resource_if_changed(resource_type: str, scope: str, key: str, if_none_match: str = "") -> str:
    """
    Download resource (downloadResource)  # noqa: E501

Download resource with a given type and key for the given scope  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object resource_type: Type of the resource (required)
:param object scope: Scope of the resource (required)
:param object key: Key of the resource, e.g. 'extension.js' (required)
:param object if_none_match:
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_resource_if_changed(resource_type=resource_type, scope=scope, key=key, if_none_match=if_none_match)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_resource_if_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_server_certificate(protocol: str) -> str:
    """
    Download server certificate using file path defined in device.connectivity properties (downloadServerCertificate)  # noqa: E501

Download server certificate.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str protocol: A string value representing the device connectivity protocol. Possible values: 'mqtt', 'mqtts', 'http', 'https', 'coap', 'coaps' (required)
:return: Resource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_server_certificate(protocol=protocol)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_server_certificate'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def download_test_report(body: str, reports_server_endpoint_url: Optional[str] = None) -> str:
    """
    Download test report (downloadTestReport)  # noqa: E501

Generate and download test report.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param DashboardReportConfig body: (required)
:param str reports_server_endpoint_url: A string value representing the report server endpoint.
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.download_test_report(body=body, reports_server_endpoint_url=reports_server_endpoint_url)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'download_test_report'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def enable_scheduler_event(scheduler_event_id_json: str, enabled_value: bool) -> str:
    """
    Enable or disable Scheduler Event (enableSchedulerEvent)  # noqa: E501

Updates scheduler event with enabled = true/false. Scheduler Event extends Scheduler Event Info object and adds 'configuration' - a JSON structure of scheduler event configuration. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool enabled_value: Enabled or disabled scheduler (required)
:return: SchedulerEvent
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.enable_scheduler_event(scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'), enabled_value=enabled_value)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'enable_scheduler_event'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def export_group_dashboards(entity_group_id_json: str, limit: int) -> str:
    """
    Export Dashboards (exportGroupDashboards)  # noqa: E501

Export the dashboards that belong to specified group id.The Dashboard object is a heavyweight object that contains information about the dashboard (e.g. title, image, assigned customers) and also configuration JSON (e.g. layouts, widgets, entity aliases).  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int limit: Limit of the entities to export (required)
:return: list[Dashboard]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.export_group_dashboards(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), limit=limit)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'export_group_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def export_image(_type: str, key: str) -> str:
    """
    exportImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:return: ImageExportData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.export_image(_type=_type, key=key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'export_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def export_rule_chains(limit: int) -> str:
    """
    Export Rule Chains  # noqa: E501

Exports all tenant rule chains as one JSON.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int limit: A limit of rule chains to export. (required)
:return: RuleChainData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.export_rule_chains(limit=limit)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'export_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_alarm_data_by_query(body: Optional[str] = None) -> str:
    """
    Find Alarms by Query  # noqa: E501

This method description defines how Alarm Data Query extends the Entity Data Query. See method 'Find Entity Data by Query' first to get the info about 'Entity Data Query'.   The platform will first search the entities that match the entity and key filters. Then, the platform will use 'Alarm Page Link' to filter the alarms related to those entities. Finally, platform fetch the properties of alarm that are defined in the **'alarmFields'** and combine them with the other entity, attribute and latest time-series fields to return the result.    See example of the alarm query below. The query will search first 100 active alarms with type 'Temperature Alarm' or 'Fire Alarm' for any device with current temperature > 0. The query will return combination of the entity fields: name of the device, device model and latest temperature reading and alarms fields: createdTime, type, severity and status:   ```json {   "entityFilter": {     "type": "entityType",     "resolveMultiple": true,     "entityType": "DEVICE"   },   "pageLink": {     "page": 0,     "pageSize": 100,     "textSearch": null,     "searchPropagatedAlarms": false,     "statusList": [       "ACTIVE"     ],     "severityList": [       "CRITICAL",       "MAJOR"     ],     "typeList": [       "Temperature Alarm",       "Fire Alarm"     ],     "sortOrder": {       "key": {         "key": "createdTime",         "type": "ALARM_FIELD"       },       "direction": "DESC"     },     "timeWindow": 86400000   },   "keyFilters": [     {       "key": {         "type": "TIME_SERIES",         "key": "temperature"       },       "valueType": "NUMERIC",       "predicate": {         "operation": "GREATER",         "value": {           "defaultValue": 0,           "dynamicValue": null         },         "type": "NUMERIC"       }     }   ],   "alarmFields": [     {       "type": "ALARM_FIELD",       "key": "createdTime"     },     {       "type": "ALARM_FIELD",       "key": "type"     },     {       "type": "ALARM_FIELD",       "key": "severity"     },     {       "type": "ALARM_FIELD",       "key": "status"     }   ],   "entityFields": [     {       "type": "ENTITY_FIELD",       "key": "name"     }   ],   "latestValues": [     {       "type": "ATTRIBUTE",       "key": "model"     },     {       "type": "TIME_SERIES",       "key": "temperature"     }   ] } ```  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AlarmDataQuery body:
:return: PageDataAlarmData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_alarm_data_by_query(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_alarm_data_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_all_related_edges_missing_attributes(integration_id_json: str) -> str:
    """
    Find missing attributes for all related edges (findAllRelatedEdgesMissingAttributes)  # noqa: E501

Returns list of attribute names of all related edges that are missing in the integration configuration.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_id: A string value representing the integration id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_all_related_edges_missing_attributes(integration_id=deserialize_param(integration_id_json, 'IntegrationId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_all_related_edges_missing_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_from(from_id_json: str, relation_type: str, relation_type_group: Optional[str] = None) -> str:
    """
    Get List of Relations (findByFrom)  # noqa: E501

Returns list of relation objects for the specified entity by the 'from' direction and relation type.   If the user has the authority of 'System Administrator', the server checks that the entity is owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that the entity is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the entity is assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str from_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str from_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type: A string value representing relation type between entities. For example, 'Contains', 'Manages'. It can be any string value. (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: list[EntityRelation]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_by_from(from_id=deserialize_param(from_id_json, 'EntityId'), relation_type=relation_type, relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_from'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_from_v1(from_id_json: str, from_type: str, relation_type_group: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_from_v1(from_id=deserialize_param(from_id_json, 'EntityId'), from_type=from_type, relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_from_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_query(body: Optional[str] = None) -> str:
    """
    Find related assets (findByQuery)  # noqa: E501

Returns all assets that are related to the specific entity. The entity id, relation type, asset types, depth of the search, and other query parameters defined using complex 'AssetSearchQuery' object. See 'Model' tab of the Parameters for more info.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AssetSearchQuery body:
:return: list[Asset]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_by_query(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_query_v1(body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_query_v1(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_query_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_query_v2(body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_query_v2(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_query_v2'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_query_v3(body_json: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_query_v3(body=deserialize_param(body_json, 'EntityRelationsQuery'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_query_v3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_query_v4(body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_query_v4(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_query_v4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_to(to_id_json: str, relation_type: str, relation_type_group: Optional[str] = None) -> str:
    """
    Get List of Relations (findByTo)  # noqa: E501

Returns list of relation objects for the specified entity by the 'to' direction and relation type.   If the user has the authority of 'System Administrator', the server checks that the entity is owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that the entity is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the entity is assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str to_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str to_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type: A string value representing relation type between entities. For example, 'Contains', 'Manages'. It can be any string value. (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: list[EntityRelation]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_by_to(to_id=deserialize_param(to_id_json, 'EntityId'), relation_type=relation_type, relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_to'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_by_to_v1(to_id_json: str, to_type: str, relation_type_group: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_by_to_v1(to_id=deserialize_param(to_id_json, 'EntityId'), to_type=to_type, relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_by_to_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_edge_missing_attributes_get(edge_id_json: str, integration_ids: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.find_edge_missing_attributes_get(edge_id=deserialize_param(edge_id_json, 'EdgeId'), integration_ids=integration_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_edge_missing_attributes_get'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_entity_data_by_query(body: Optional[str] = None) -> str:
    """
    Find Entity Data by Query  # noqa: E501

Allows to run complex queries over platform entities (devices, assets, customers, etc) based on the combination of main entity filter and multiple key filters. Returns the paginated result of the query that contains requested entity fields and latest values of requested attributes and time-series data.  # Query Definition    Main **entity filter** is mandatory and defines generic search criteria. For example, "find all devices with profile 'Moisture Sensor'" or "Find all devices related to asset 'Building A'"  Optional **key filters** allow to filter results of the **entity filter** by complex criteria against main entity fields (name, label, type, etc), attributes and telemetry. For example, "temperature > 20 or temperature< 10" or "name starts with 'T', and attribute 'model' is 'T1000', and timeseries field 'batteryLevel' > 40".  The **entity fields** and **latest values** contains list of entity fields and latest attribute/telemetry fields to fetch for each entity.  The **page link** contains information about the page to fetch and the sort ordering.  Let's review the example:  ```json {   "entityFilter": {     "type": "entityType",     "resolveMultiple": true,     "entityType": "DEVICE"   },   "keyFilters": [     {       "key": {         "type": "TIME_SERIES",         "key": "temperature"       },       "valueType": "NUMERIC",       "predicate": {         "operation": "GREATER",         "value": {           "defaultValue": 0,           "dynamicValue": {             "sourceType": "CURRENT_USER",             "sourceAttribute": "temperatureThreshold",             "inherit": false           }         },         "type": "NUMERIC"       }     }   ],   "entityFields": [     {       "type": "ENTITY_FIELD",       "key": "name"     },     {       "type": "ENTITY_FIELD",       "key": "label"     },     {       "type": "ENTITY_FIELD",       "key": "additionalInfo"     }   ],   "latestValues": [     {       "type": "ATTRIBUTE",       "key": "model"     },     {       "type": "TIME_SERIES",       "key": "temperature"     }   ],   "pageLink": {     "page": 0,     "pageSize": 10,     "sortOrder": {       "key": {         "key": "name",         "type": "ENTITY_FIELD"       },       "direction": "ASC"     }   } } ```   Example mentioned above search all devices which have attribute 'active' set to 'true'. Now let's review available entity filters and key filters syntax:   # Entity Filters Entity Filter body depends on the 'type' parameter. Let's review available entity filter types. In fact, they do correspond to available dashboard aliases.  ## Single Entity  Allows to filter only one entity based on the id. For example, this entity filter selects certain device:  ```json {   "type": "singleEntity",   "singleEntity": {     "id": "d521edb0-2a7a-11ec-94eb-213c95f54092",     "entityType": "DEVICE"   } } ```  ## Entity List Filter  Allows to filter entities of the same type using their ids. For example, this entity filter selects two devices:  ```json {   "type": "entityList",   "entityType": "DEVICE",   "entityList": [     "e6501f30-2a7a-11ec-94eb-213c95f54092",     "e6657bf0-2a7a-11ec-94eb-213c95f54092"   ] } ```  ## Entity Name Filter  Allows to filter entities of the same type using the **'starts with'** expression over entity name. For example, this entity filter selects all devices which name starts with 'Air Quality':  ```json {   "type": "entityName",   "entityType": "DEVICE",   "entityNameFilter": "Air Quality" } ```  ## Entity Type Filter  Allows to filter entities based on their type (CUSTOMER, USER, DASHBOARD, ASSET, DEVICE, etc)For example, this entity filter selects all tenant customers:  ```json {   "type": "entityType",   "entityType": "CUSTOMER" } ```  ## Asset Type Filter  Allows to filter assets based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'charging station' assets which name starts with 'Tesla':  ```json {   "type": "assetType",   "assetType": "charging station",   "assetNameFilter": "Tesla" } ```  ## Device Type Filter  Allows to filter devices based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Temperature Sensor' devices which name starts with 'ABC':  ```json {   "type": "deviceType",   "deviceType": "Temperature Sensor",   "deviceNameFilter": "ABC" } ```  ## Edge Type Filter  Allows to filter edge instances based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Factory' edge instances which name starts with 'Nevada':  ```json {   "type": "edgeType",   "edgeType": "Factory",   "edgeNameFilter": "Nevada" } ```  ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```  ## Api Usage Filter  Allows to query for Api Usage based on optional customer id. If the customer id is not set, returns current tenant API usage.For example, this entity filter selects the 'Api Usage' entity for customer with id 'e6501f30-2a7a-11ec-94eb-213c95f54092':  ```json {   "type": "apiUsageState",   "customerId": {     "id": "d521edb0-2a7a-11ec-94eb-213c95f54092",     "entityType": "CUSTOMER"   } } ```  ## Relations Query Filter  Allows to filter entities that are related to the provided root entity. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'filter' object allows you to define the relation type and set of acceptable entity types to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only those who match the 'filters'.  For example, this entity filter selects all devices and assets which are related to the asset with id 'e51de0c0-2a7a-11ec-94eb-213c95f54092':  ```json {   "type": "relationsQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e51de0c0-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "filters": [     {       "relationType": "Contains",       "entityTypes": [         "DEVICE",         "ASSET"       ]     }   ] } ```  ## Asset Search Query  Allows to filter assets that are related to the provided root entity. Filters related assets based on the relation type and set of asset types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'assetTypes' defines the type of the asset to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only assets that match 'relationType' and 'assetTypes' conditions.  For example, this entity filter selects 'charging station' assets which are related to the asset with id 'e51de0c0-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "assetSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e51de0c0-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "relationType": "Contains",   "assetTypes": [     "charging station"   ] } ```  ## Device Search Query  Allows to filter devices that are related to the provided root entity. Filters related devices based on the relation type and set of device types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'deviceTypes' defines the type of the device to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Charging port' and 'Air Quality Sensor' devices which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "deviceSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 2,   "fetchLastLevelOnly": true,   "relationType": "Contains",   "deviceTypes": [     "Air Quality Sensor",     "Charging port"   ] } ```  ## Entity View Query  Allows to filter entity views that are related to the provided root entity. Filters related entity views based on the relation type and set of entity view types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'entityViewTypes' defines the type of the entity view to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Concrete mixer' entity views which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "entityViewSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 1,   "fetchLastLevelOnly": false,   "relationType": "Contains",   "entityViewTypes": [     "Concrete mixer"   ] } ```  ## Edge Search Query  Allows to filter edge instances that are related to the provided root entity. Filters related edge instances based on the relation type and set of edge types. Possible direction values are 'TO' and 'FROM'. The 'maxLevel' defines how many relation levels should the query search 'recursively'. Assuming the 'maxLevel' is > 1, the 'fetchLastLevelOnly' defines either to return all related entities or only entities that are on the last level of relations. The 'relationType' defines the type of the relation to search for. The 'deviceTypes' defines the type of the device to search for. The relation query calculates all related entities, even if they are filtered using different relation types, and then extracts only devices that match 'relationType' and 'deviceTypes' conditions.  For example, this entity filter selects 'Factory' edge instances which are related to the asset with id 'e52b0020-2a7a-11ec-94eb-213c95f54092' using 'Contains' relation:  ```json {   "type": "deviceSearchQuery",   "rootEntity": {     "entityType": "ASSET",     "id": "e52b0020-2a7a-11ec-94eb-213c95f54092"   },   "direction": "FROM",   "maxLevel": 2,   "fetchLastLevelOnly": true,   "relationType": "Contains",   "edgeTypes": [     "Factory"   ] } ```   # Key Filters Key Filter allows you to define complex logical expressions over entity field, attribute or latest time-series value. The filter is defined using 'key', 'valueType' and 'predicate' objects. Single Entity Query may have zero, one or multiple predicates. If multiple filters are defined, they are evaluated using logical 'AND'. The example below checks that temperature of the entity is above 20 degrees:  ```json {   "key": {     "type": "TIME_SERIES",     "key": "temperature"   },   "valueType": "NUMERIC",   "predicate": {     "operation": "GREATER",     "value": {       "defaultValue": 20,       "dynamicValue": null     },     "type": "NUMERIC"   } } ```   Now let's review 'key', 'valueType' and 'predicate' objects in detail.  ## Filter Key  Filter Key defines either entity field, attribute or telemetry. It is a JSON object that consists the key name and type. The following filter key types are supported:    * 'CLIENT_ATTRIBUTE' - used for client attributes;   * 'SHARED_ATTRIBUTE' - used for shared attributes;   * 'SERVER_ATTRIBUTE' - used for server attributes;   * 'ATTRIBUTE' - used for any of the above;   * 'TIME_SERIES' - used for time-series values;   * 'ENTITY_FIELD' - used for accessing entity fields like 'name', 'label', etc. The list of available fields depends on the entity type;   * 'ALARM_FIELD' - similar to entity field, but is used in alarm queries only;     Let's review the example:  ```json {   "type": "TIME_SERIES",   "key": "temperature" } ```  ## Value Type and Operations  Provides a hint about the data type of the entity field that is defined in the filter key. The value type impacts the list of possible operations that you may use in the corresponding predicate. For example, you may use 'STARTS_WITH' or 'END_WITH', but you can't use 'GREATER_OR_EQUAL' for string values.The following filter value types and corresponding predicate operations are supported:    * 'STRING' - used to filter any 'String' or 'JSON' values. Operations: EQUAL, NOT_EQUAL, STARTS_WITH, ENDS_WITH, CONTAINS, NOT_CONTAINS;   * 'NUMERIC' - used for 'Long' and 'Double' values. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;   * 'BOOLEAN' - used for boolean values. Operations: EQUAL, NOT_EQUAL;  * 'DATE_TIME' - similar to numeric, transforms value to milliseconds since epoch. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;    ## Filter Predicate  Filter Predicate defines the logical expression to evaluate. The list of available operations depends on the filter value type, see above. Platform supports 4 predicate types: 'STRING', 'NUMERIC', 'BOOLEAN' and 'COMPLEX'. The last one allows to combine multiple operations over one filter key.  Simple predicate example to check 'value < 100':   ```json {   "operation": "LESS",   "value": {     "defaultValue": 100,     "dynamicValue": null   },   "type": "NUMERIC" } ```  Complex predicate example, to check 'value < 10 or value > 20':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "operation": "GREATER",       "value": {         "defaultValue": 20,         "dynamicValue": null       },       "type": "NUMERIC"     }   ] } ```  More complex predicate example, to check 'value < 10 or (value > 50 && value < 60)':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "type": "COMPLEX",       "operation": "AND",       "predicates": [         {           "operation": "GREATER",           "value": {             "defaultValue": 50,             "dynamicValue": null           },           "type": "NUMERIC"         },         {           "operation": "LESS",           "value": {             "defaultValue": 60,             "dynamicValue": null           },           "type": "NUMERIC"         }       ]     }   ] } ```   You may also want to replace hardcoded values (for example, temperature > 20) with the more dynamic expression (for example, temperature > 'value of the tenant attribute with key 'temperatureThreshold'). It is possible to use 'dynamicValue' to define attribute of the tenant, customer or user that is performing the API call. See example below:   ```json {   "operation": "GREATER",   "value": {     "defaultValue": 0,     "dynamicValue": {       "sourceType": "CURRENT_USER",       "sourceAttribute": "temperatureThreshold"     }   },   "type": "NUMERIC" } ```   Note that you may use 'CURRENT_USER', 'CURRENT_CUSTOMER' and 'CURRENT_TENANT' as a 'sourceType'. The 'defaultValue' is used when the attribute with such a name is not defined for the chosen source.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityDataQuery body:
:return: PageDataEntityData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_entity_data_by_query(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_entity_data_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_entity_timeseries_and_attributes_keys_by_query(timeseries: bool, attributes: bool, body: str, scope: Optional[str] = None) -> str:
    """
    Find Entity Keys by Query  # noqa: E501

Uses entity data query (see 'Find Entity Data by Query') to find first 100 entities. Then fetch and return all unique time-series and/or attribute keys. Used mostly for UI hints.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool timeseries: Include all unique time-series keys to the result. (required)
:param bool attributes: Include all unique attribute keys to the result. (required)
:param EntityDataQuery body:
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_entity_timeseries_and_attributes_keys_by_query(timeseries=timeseries, attributes=attributes, body=body, scope=scope)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_entity_timeseries_and_attributes_keys_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_info_by_from(from_id_json: str, relation_type_group: Optional[str] = None) -> str:
    """
    Get List of Relation Infos (findInfoByFrom)  # noqa: E501

Returns list of relation info objects for the specified entity by the 'from' direction.   If the user has the authority of 'System Administrator', the server checks that the entity is owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that the entity is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the entity is assigned to the same customer. Relation Info is an extension of the default Relation object that contains information about the 'from' and 'to' entity names.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str from_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str from_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: list[EntityRelationInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_info_by_from(from_id=deserialize_param(from_id_json, 'EntityId'), relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_info_by_from'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_info_by_query(body_json: str) -> str:
    """
    Find related entity infos (findInfoByQuery)  # noqa: E501

Returns all entity infos that are related to the specific entity. The entity id, relation type, entity types, depth of the search, and other query parameters defined using complex 'EntityRelationsQuery' object. See 'Model' tab of the Parameters for more info. Relation Info is an extension of the default Relation object that contains information about the 'from' and 'to' entity names.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityRelationsQuery body:
:return: list[EntityRelationInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_info_by_query(body=deserialize_param(body_json, 'EntityRelationsQuery'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_info_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_info_by_to(to_id_json: str, to_type: str, relation_type_group: Optional[str] = None) -> str:
    """
    Get List of Relation Infos (findInfoByTo)  # noqa: E501

Returns list of relation info objects for the specified entity by the 'to' direction.   If the user has the authority of 'System Administrator', the server checks that the entity is owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that the entity is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the entity is assigned to the same customer. Relation Info is an extension of the default Relation object that contains information about the 'from' and 'to' entity names.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str to_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str to_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: list[EntityRelationInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_info_by_to(to_id=deserialize_param(to_id_json, 'EntityId'), to_type=to_type, relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_info_by_to'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_missing_to_related_rule_chains(edge_id_json: str) -> str:
    """
    Find missing rule chains (findMissingToRelatedRuleChains)  # noqa: E501

Returns list of rule chains ids that are not assigned to particular edge, but these rule chains are present in the already assigned rule chains to edge.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_missing_to_related_rule_chains(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_missing_to_related_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_tenant_o_auth2_client_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get OAuth2 Client infos (findTenantOAuth2ClientInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on client's title
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataOAuth2ClientInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_tenant_o_auth2_client_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_tenant_o_auth2_client_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_tenant_o_auth2_client_infos_by_ids(ids_json: str) -> str:
    """
    Get OAuth2 Client infos By Ids (findTenantOAuth2ClientInfosByIds)  # noqa: E501

Fetch OAuth2 Client info objects based on the provided ids.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str client_ids: A list of oauth2 ids, separated by comma ',' (required)
:return: list[OAuth2ClientInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_tenant_o_auth2_client_infos_by_ids(ids=json.loads(ids_json) if ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_tenant_o_auth2_client_infos_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def find_users_by_query(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Find users by query (findUsersByQuery)  # noqa: E501

Returns page of user data objects. Search is been executed by email, firstName and lastName fields. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUserEmailInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.find_users_by_query(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'find_users_by_query'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def generate_two_fa_account_config(provider_type: str) -> str:
    """
    Generate 2FA account config (generateTwoFaAccountConfig)  # noqa: E501

Generate new 2FA account config template for specified provider type.   For TOTP, this will return a corresponding account config template with a generated OTP auth URL (with new random secret key for each API call) that can be then converted to a QR code to scan with an authenticator app. Example: ``` {   "providerType": "TOTP",   "useByDefault": false,   "authUrl": "otpauth://totp/TB%202FA:tenant@thingsboard.org?issuer=TB+2FA&secret=PNJDNWJVAK4ZTUYT7RFGPQLXA7XGU7PX" } ```  For EMAIL, the generated config will contain email from user's account: ``` {   "providerType": "EMAIL",   "useByDefault": false,   "email": "tenant@thingsboard.org" } ```  For SMS 2FA this method will just return a config with empty/default values as there is nothing to generate/preset: ``` {   "providerType": "SMS",   "useByDefault": false,   "phoneNumber": null } ```  Will throw an error (Bad Request) if the provider is not configured for usage.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str provider_type: 2FA provider type to generate new account config for (required)
:return: TwoFaAccountConfig
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.generate_two_fa_account_config(provider_type=provider_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'generate_two_fa_account_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def getName() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.getName()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'getName'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_account_two_fa_settings() -> str:
    """
    Get account 2FA settings (getAccountTwoFaSettings)  # noqa: E501

Get user's account 2FA configuration. Configuration contains configs for different 2FA providers.  Example: ``` {   "configs": {     "EMAIL": {       "providerType": "EMAIL",       "useByDefault": true,       "email": "tenant@thingsboard.org"     },     "TOTP": {       "providerType": "TOTP",       "useByDefault": false,       "authUrl": "otpauth://totp/TB%202FA:tenant@thingsboard.org?issuer=TB+2FA&secret=P6Z2TLYTASOGP6LCJZAD24ETT5DACNNX"     },     "SMS": {       "providerType": "SMS",       "useByDefault": false,       "phoneNumber": "+380501253652"     }   } } ```  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: AccountTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_account_two_fa_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_account_two_fa_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_activation_link(user_id_json: str) -> str:
    """
    Get the activation link (getActivationLink)  # noqa: E501

Get the activation link for the user. The base url for activation link is configurable in the general settings of system administrator.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_activation_link(user_id=deserialize_param(user_id_json, 'UserId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_activation_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_activation_link_info(user_id_json: str) -> str:
    """
    Get activation link info (getActivationLinkInfo)  # noqa: E501

Get the activation link info for the user. The base url for activation link is configurable in the general settings of system administrator.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: UserActivationLink
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_activation_link_info(user_id=deserialize_param(user_id_json, 'UserId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_activation_link_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_admin_settings(key: str, system_by_default: Optional[str] = None) -> str:
    """
    Get the Administration Settings object using key (getAdminSettings)  # noqa: E501

Get the Administration Settings object using specified string key. Referencing non-existing key will cause an error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str key: A string value of the key (e.g. 'general' or 'mail'). (required)
:param bool system_by_default: Use system settings if settings are not defined on tenant level.
:return: AdminSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_admin_settings(key=key, system_by_default=system_by_default)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_admin_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ai_model_by_id(ai_model_id_json: str) -> str:
    """
    Get AI model by ID (getAiModelById)  # noqa: E501

Fetches an AI model record by its `id`.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str model_uuid: ID of the AI model record (required)
:return: AiModel
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_ai_model_by_id(ai_model_id=deserialize_param(ai_model_id_json, 'AiModelId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ai_model_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ai_models(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get AI models (getAiModels)  # noqa: E501

Returns a page of AI models. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See response schema for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the AI model name, provider and model ID.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAiModel
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_ai_models(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ai_models'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_alarm_by_id(alarm_id_json: str) -> str:
    """
    Get Alarm (getAlarmById)  # noqa: E501

Fetch the Alarm object based on the provided Alarm Id. If the user has the authority of 'Tenant Administrator', the server checks that the originator of alarm is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the originator of alarm belongs to the customer.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Alarm
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_alarm_by_id(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_alarm_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_alarm_comments(alarm_id_json: str, page_size: int, page: int, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Alarm comments (getAlarmComments)  # noqa: E501

Returns a page of alarm comments for specified alarm. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAlarmCommentInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_alarm_comments(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'), page_size=page_size, page=page, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_alarm_comments'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_alarm_info_by_id(alarm_id_json: str) -> str:
    """
    Get Alarm Info (getAlarmInfoById)  # noqa: E501

Fetch the Alarm Info object based on the provided Alarm Id. If the user has the authority of 'Tenant Administrator', the server checks that the originator of alarm is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the originator of alarm belongs to the customer. Alarm Info is an extension of the default Alarm object that also contains name of the alarm originator.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AlarmInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_alarm_info_by_id(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_alarm_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_alarm_types(page_size: int, page: int, text_search: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Alarm Types (getAlarmTypes)  # noqa: E501

Returns a set of unique alarm types based on alarms that are either owned by the tenant or assigned to the customer which user is performing the request.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on of next alarm fields: type, severity or status
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntitySubtype
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_alarm_types(page_size=page_size, page=page, text_search=text_search, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_alarm_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_alarms(entity_id_json: str, page_size: int, page: int, search_status: Optional[str] = None, status: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, fetch_originator: Optional[str] = None, assignee_id: Optional[str] = None) -> str:
    """
    Get Alarms (getAlarms)  # noqa: E501

Returns a page of alarms for the selected entity. Specifying both parameters 'searchStatus' and 'status' at the same time will cause an error. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str search_status: A string value representing one of the AlarmSearchStatus enumeration value
:param str status: A string value representing one of the AlarmStatus enumeration value
:param str assignee_id: A string value representing the assignee user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param str text_search: The case insensitive 'substring' filter based on of next alarm fields: type, severity or status
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the Alarm class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the Alarm class field: 'createdTime'.
:param bool fetch_originator: A boolean value to specify if the alarm originator name will be filled in the AlarmInfo object  field: 'originatorName' or will returns as null.
:return: PageDataAlarmInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_alarms(entity_id=deserialize_param(entity_id_json, 'EntityId'), page_size=page_size, page=page, search_status=search_status, status=status, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, fetch_originator=fetch_originator, assignee_id=assignee_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_alarms'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_alarms(page_size: int, page: int, search_status: Optional[str] = None, status: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, fetch_originator: Optional[str] = None) -> str:
    """
    Get All Alarms (getAllAlarms)  # noqa: E501

Returns a page of alarms that belongs to the current user owner. If the user has the authority of 'Tenant Administrator', the server returns alarms that belongs to the tenant of current user. If the user has the authority of 'Customer User', the server returns alarms that belongs to the customer of current user. Specifying both parameters 'searchStatus' and 'status' at the same time will cause an error. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str search_status: A string value representing one of the AlarmSearchStatus enumeration value
:param str status: A string value representing one of the AlarmStatus enumeration value
:param str assignee_id: A string value representing the assignee user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param str text_search: The case insensitive 'substring' filter based on of next alarm fields: type, severity or status
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the Alarm class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the Alarm class field: 'createdTime'.
:param bool fetch_originator: A boolean value to specify if the alarm originator name will be filled in the AlarmInfo object  field: 'originatorName' or will returns as null.
:return: PageDataAlarmInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_alarms(page_size=page_size, page=page, search_status=search_status, status=status, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, fetch_originator=fetch_originator)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_alarms'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_asset_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None, asset_profile_id: Optional[str] = None) -> str:
    """
    Get All Asset Infos for current user (getAllAssetInfos)  # noqa: E501

Returns a page of asset info objects owned by the tenant or the customer of a current user. Asset Info is an extension of the default Asset object that contains information about the owner name.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAssetInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_asset_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers, asset_profile_id=asset_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_asset_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_customer_infos(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get All Customer Infos for current user (getAllCustomerInfos)  # noqa: E501

Returns a page of customer info objects owned by the tenant or the customer of a current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_customer_infos(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_customer_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_customer_users(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Users (getCustomerUsers)  # noqa: E501

Returns a page of users for the current tenant with authority 'CUSTOMER_USER'. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_customer_users(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_customer_users'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_dashboards(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get All Dashboards for current user (getAllDashboards)  # noqa: E501

Returns a page of dashboard info objects owned by the tenant or the customer of a current user. The Dashboard Info object contains lightweight information about the dashboard (e.g. title, image, assigned customers) but does not contain the heavyweight configuration JSON. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the dashboard title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_dashboards(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_device_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None, device_profile_id: Optional[str] = None) -> str:
    """
    Get All Device Infos for current user (getAllDeviceInfos)  # noqa: E501

Returns a page of device info objects owned by the tenant or the customer of a current user. Device Info is an extension of the default Device object that contains information about the owner name.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param bool active: A boolean value representing the device active flag.
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDeviceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_device_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers, device_profile_id=device_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_device_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_edge_entity_groups(edge_id_json: str, group_type: str) -> str:
    """
    Get All Edge Entity Groups by entity type (getAllEdgeEntityGroups)  # noqa: E501

Fetch the list of Entity Group Info objects based on the provided Entity Type and assigned to the provided Edge entity. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str group_type: EntityGroup type (required)
:return: list[EntityGroupInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_edge_entity_groups(edge_id=deserialize_param(edge_id_json, 'EdgeId'), group_type=group_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_edge_entity_groups'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_entity_view_infos(page_size: int, page: int, include_customers: Optional[str] = None, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get All Entity View Infos for current user (getAllEntityViewInfos)  # noqa: E501

Returns a page of entity view info objects owned by the tenant or the customer of a current user. Entity Views Info extends the Entity View with owner name. Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str type:   ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityViewInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_entity_view_infos(page_size=page_size, page=page, include_customers=include_customers, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_entity_view_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_report_template_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, type_list: Optional[str] = None, format_list: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get All Report Templates for current user (getAllReportTemplateInfos)  # noqa: E501

Returns a page of report template info objects owned by the tenant or the customer of a current user. Report Templates allows you to create reports according to the report template configuration. Report service uses report template configuration to generate report. See the 'Model' tab of the Response Class for more details.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See response schema for more details.   Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type_list: A list of string values separated by comma ',' representing one of the ReportTemplateType enumeration value.
:param str format_list: A list of string values separated by comma ',' representing one of the TbReportFormat enumeration value.
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the report template name or customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataReportTemplateInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_report_template_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, type_list=type_list, format_list=format_list, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_report_template_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_scheduler_events(edge_id_json: str) -> str:
    """
    Get All Edge Scheduler Events (getAllSchedulerEvents)  # noqa: E501

Fetch the list of Scheduler Event Info objects based on the provided Edge entity. Scheduler Event extends Scheduler Event Info object and adds 'configuration' - a JSON structure of scheduler event configuration. See the 'Model' tab of the Response Class for more details. Scheduler Events allows you to schedule various types of events with flexible schedule configuration. Scheduler fires configured scheduler events according to their schedule. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[SchedulerEventInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_scheduler_events(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_scheduler_events'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_all_user_infos(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get All User Infos for current user (getAllUserInfos)  # noqa: E501

Returns a page of user info objects owned by the tenant or the customer of a current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUserInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_all_user_infos(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_all_user_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_allowed_permissions() -> str:
    """
    Get Permissions (getAllowedPermissions)  # noqa: E501

Returns a complex object that describes:   * all possible (both granted and not granted) permissions for the authority of the user (Tenant or Customer);  * all granted permissions for the user;   The result impacts UI behavior and hides certain UI elements if user has no permissions to invoke the related operations. Nevertheless, all API calls check the permissions each time they are executed on the server side.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: AllowedPermissionsInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_allowed_permissions()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_allowed_permissions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_application_redirect(user_agent: str) -> str:
    """
    getApplicationRedirect  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object user_agent: (required)
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_application_redirect(user_agent=user_agent)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_application_redirect'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_by_id(asset_id_json: str) -> str:
    """
    Get Asset (getAssetById)  # noqa: E501

Fetch the Asset object based on the provided Asset Id. If the user has the authority of 'Tenant Administrator', the server checks that the asset is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the asset is assigned to the same customer.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_id: A string value representing the asset id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Asset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_by_id(asset_id=deserialize_param(asset_id_json, 'AssetId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_info_by_id(asset_id_json: str) -> str:
    """
    Get Asset Info (getAssetInfoById)  # noqa: E501

Fetch the Asset Info object based on the provided Asset Id. If the user has the authority of 'Tenant Administrator', the server checks that the asset is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the asset is assigned to the same customer.Asset Info is an extension of the default Asset object that contains information about the owner name.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_id: A string value representing the asset id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AssetInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_info_by_id(asset_id=deserialize_param(asset_id_json, 'AssetId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profile_by_id(asset_profile_id: str, inline_images: Optional[str] = None) -> str:
    """
    Get Asset Profile (getAssetProfileById)  # noqa: E501

Fetch the Asset Profile object based on the provided Asset Profile Id. The server checks that the asset profile is owned by the same tenant.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: AssetProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_profile_by_id(asset_profile_id=asset_profile_id, inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profile_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profile_by_names(active_only: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_asset_profile_by_names(active_only=active_only)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profile_by_names'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profile_info_by_id(asset_profile_id_json: str) -> str:
    """
    Get Asset Profile Info (getAssetProfileInfoById)  # noqa: E501

Fetch the Asset Profile Info object based on the provided Asset Profile Id. Asset Profile Info is a lightweight object that includes main information about Asset Profile.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AssetProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_profile_info_by_id(asset_profile_id=deserialize_param(asset_profile_id_json, 'AssetProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profile_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profile_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Asset Profile infos (getAssetProfileInfos)  # noqa: E501

Returns a page of asset profile info objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. Asset Profile Info is a lightweight object that includes main information about Asset Profile.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the asset profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAssetProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_profile_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profile_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profiles(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Asset Profiles (getAssetProfiles)  # noqa: E501

Returns a page of asset profile objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the asset profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAssetProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_profiles(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profiles'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_profiles_by_ids(asset_profile_ids_json: str) -> str:
    """
    Get Asset Profiles By Ids (getAssetProfilesByIds)  # noqa: E501

Requested asset profiles must be owned by tenant which is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_profile_ids: A list of asset profile ids, separated by comma ',' (required)
:return: list[AssetProfileInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_profiles_by_ids(asset_profile_ids=json.loads(asset_profile_ids_json) if asset_profile_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_profiles_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_asset_types() -> str:
    """
    Get Asset Types (getAssetTypes)  # noqa: E501

Deprecated. See 'getAssetProfileNames' API from Asset Profile Controller instead.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[EntitySubtype]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_asset_types()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_asset_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_assets_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get assets by Entity Group Id (getAssetsByEntityGroupId)  # noqa: E501

Returns a page of asset objects that belongs to specified Entity Group Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAsset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_assets_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_assets_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_assets_by_ids(asset_ids_json: str) -> str:
    """
    Get Assets By Ids (getAssetsByIds)  # noqa: E501

Requested assets must be owned by tenant or assigned to customer which user is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_ids: A list of asset ids, separated by comma ',' (required)
:return: list[Asset]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_assets_by_ids(asset_ids=json.loads(asset_ids_json) if asset_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_assets_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_attribute_keys(entity_id_json: str) -> str:
    """
    Get all attribute keys (getAttributeKeys)  # noqa: E501

Returns a set of unique attribute key names for the selected entity. The response will include merged key names set for all attribute scopes:   * SERVER_SCOPE - supported for all entity types;  * CLIENT_SCOPE - supported for devices;  * SHARED_SCOPE - supported for devices.   Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_attribute_keys(entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_attribute_keys'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_attribute_keys_by_scope(entity_id_json: str, scope: str) -> str:
    """
    Get all attribute keys by scope (getAttributeKeysByScope)  # noqa: E501

Returns a set of unique attribute key names for the selected entity and attributes scope:    * SERVER_SCOPE - supported for all entity types;  * CLIENT_SCOPE - supported for devices;  * SHARED_SCOPE - supported for devices.   Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_attribute_keys_by_scope(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_attribute_keys_by_scope'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_attributes(entity_id_json: str, keys: Optional[str] = None) -> str:
    """
    Get attributes (getAttributes)  # noqa: E501

Returns all attributes that belong to specified entity. Use optional 'keys' parameter to return specific attributes.  Example of the result:   ```json [   {"key": "stringAttributeKey", "value": "value", "lastUpdateTs": 1609459200000},   {"key": "booleanAttributeKey", "value": false, "lastUpdateTs": 1609459200001},   {"key": "doubleAttributeKey", "value": 42.2, "lastUpdateTs": 1609459200002},   {"key": "longKeyExample", "value": 73, "lastUpdateTs": 1609459200003},   {"key": "jsonKeyExample",     "value": {       "someNumber": 42,       "someArray": [1,2,3],       "someNestedObject": {"key": "value"}     },     "lastUpdateTs": 1609459200004   } ] ```   Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str keys: A string value representing the comma-separated list of attributes keys. For example, 'active,inactivityAlarmTime'.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_attributes(entity_id=deserialize_param(entity_id_json, 'EntityId'), keys=keys)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_attributes_by_scope(entity_id_json: str, scope: str, keys: Optional[str] = None) -> str:
    """
    Get attributes by scope (getAttributesByScope)  # noqa: E501

Returns all attributes of a specified scope that belong to specified entity. List of possible attribute scopes depends on the entity type:    * SERVER_SCOPE - supported for all entity types;  * SHARED_SCOPE - supported for devices;  * CLIENT_SCOPE - supported for devices.   Use optional 'keys' parameter to return specific attributes.  Example of the result:   ```json [   {"key": "stringAttributeKey", "value": "value", "lastUpdateTs": 1609459200000},   {"key": "booleanAttributeKey", "value": false, "lastUpdateTs": 1609459200001},   {"key": "doubleAttributeKey", "value": 42.2, "lastUpdateTs": 1609459200002},   {"key": "longKeyExample", "value": 73, "lastUpdateTs": 1609459200003},   {"key": "jsonKeyExample",     "value": {       "someNumber": 42,       "someArray": [1,2,3],       "someNestedObject": {"key": "value"}     },     "lastUpdateTs": 1609459200004   } ] ```   Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param str keys: A string value representing the comma-separated list of attributes keys. For example, 'active,inactivityAlarmTime'.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_attributes_by_scope(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, keys=keys)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_attributes_by_scope'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_attributes_keys(device_profile_id: Optional[str] = None) -> str:
    """
    Get attribute keys (getAttributesKeys)  # noqa: E501

Get a set of unique attribute keys used by devices that belong to specified profile. If profile is not set returns a list of unique keys among all profiles. The call is used for auto-complete in the UI forms. The implementation limits the number of devices that participate in search to 100 as a trade of between accurate results and time-consuming queries.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_attributes_keys(device_profile_id=device_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_attributes_keys'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_audit_logs(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, action_types: Optional[str] = None) -> str:
    """
    Get all audit logs (getAuditLogs)  # noqa: E501

Returns a page of audit logs related to all entities in the scope of the current user's Tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on one of the next properties: entityType, entityName, userName, actionType, actionStatus.
:param str sort_property: Property of audit log to sort by. See the 'Model' tab of the Response Class for more details. Note: entityType sort property is not defined in the AuditLog class, however, it can be used to sort audit logs by types of entities that were logged.
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param str action_types: A String value representing comma-separated list of action types. This parameter is optional, but it can be used to filter results to fetch only audit logs of specific action types. For example, 'LOGIN', 'LOGOUT'. See the 'Model' tab of the Response Class for more details.
:return: PageDataAuditLog
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_audit_logs(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, action_types=action_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_audit_logs'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_audit_logs_by_customer_id(customer_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, action_types: Optional[str] = None) -> str:
    """
    Get audit logs by customer id (getAuditLogsByCustomerId)  # noqa: E501

Returns a page of audit logs related to the targeted customer entities (devices, assets, etc.), and users actions (login, logout, etc.) that belong to this customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on one of the next properties: entityType, entityName, userName, actionType, actionStatus.
:param str sort_property: Property of audit log to sort by. See the 'Model' tab of the Response Class for more details. Note: entityType sort property is not defined in the AuditLog class, however, it can be used to sort audit logs by types of entities that were logged.
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param str action_types: A String value representing comma-separated list of action types. This parameter is optional, but it can be used to filter results to fetch only audit logs of specific action types. For example, 'LOGIN', 'LOGOUT'. See the 'Model' tab of the Response Class for more details.
:return: PageDataAuditLog
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_audit_logs_by_customer_id(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, action_types=action_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_audit_logs_by_customer_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_audit_logs_by_entity_id(entity_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, action_types: Optional[str] = None) -> str:
    """
    Get audit logs by entity id (getAuditLogsByEntityId)  # noqa: E501

Returns a page of audit logs related to the actions on the targeted entity. Basically, this API call is used to get the full lifecycle of some specific entity. For example to see when a device was created, updated, assigned to some customer, or even deleted from the system. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on one of the next properties: entityType, entityName, userName, actionType, actionStatus.
:param str sort_property: Property of audit log to sort by. See the 'Model' tab of the Response Class for more details. Note: entityType sort property is not defined in the AuditLog class, however, it can be used to sort audit logs by types of entities that were logged.
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param str action_types: A String value representing comma-separated list of action types. This parameter is optional, but it can be used to filter results to fetch only audit logs of specific action types. For example, 'LOGIN', 'LOGOUT'. See the 'Model' tab of the Response Class for more details.
:return: PageDataAuditLog
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_audit_logs_by_entity_id(entity_id=deserialize_param(entity_id_json, 'EntityId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, action_types=action_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_audit_logs_by_entity_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_audit_logs_by_user_id(user_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None, action_types: Optional[str] = None) -> str:
    """
    Get audit logs by user id (getAuditLogsByUserId)  # noqa: E501

Returns a page of audit logs related to the actions of targeted user. For example, RPC call to a particular device, or alarm acknowledgment for a specific device, etc. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on one of the next properties: entityType, entityName, userName, actionType, actionStatus.
:param str sort_property: Property of audit log to sort by. See the 'Model' tab of the Response Class for more details. Note: entityType sort property is not defined in the AuditLog class, however, it can be used to sort audit logs by types of entities that were logged.
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the AuditLog class field: 'createdTime'.
:param str action_types: A String value representing comma-separated list of action types. This parameter is optional, but it can be used to filter results to fetch only audit logs of specific action types. For example, 'LOGIN', 'LOGOUT'. See the 'Model' tab of the Response Class for more details.
:return: PageDataAuditLog
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_audit_logs_by_user_id(user_id=deserialize_param(user_id_json, 'UserId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time, action_types=action_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_audit_logs_by_user_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_authorization_url() -> str:
    """
    Redirect user to mail provider login page.   # noqa: E501

After user logged in and provided accessprovider sends authorization code to specified redirect uri.)  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_authorization_url()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_authorization_url'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_auto_assign_to_edge_rule_chains() -> str:
    """
    Get Auto Assign To Edge Rule Chains (getAutoAssignToEdgeRuleChains)  # noqa: E501

Returns a list of Rule Chains that will be assigned to a newly created edge. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[RuleChain]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_auto_assign_to_edge_rule_chains()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_auto_assign_to_edge_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_auto_commit_settings() -> str:
    """
    Get auto commit settings (getAutoCommitSettings)  # noqa: E501

Get the auto commit settings object.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: dict(str, AutoVersionCreateConfig)
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_auto_commit_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_auto_commit_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_available_delivery_methods() -> str:
    """
    Get available delivery methods (getAvailableDeliveryMethods)  # noqa: E501

Returns the list of delivery methods that are properly configured and are allowed to be used for sending notifications.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_available_delivery_methods()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_available_delivery_methods'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_available_java_locales() -> str:
    """
    Get list of available java locales (getAvailableJavaLocales)  # noqa: E501

The result is map where key is locale code and value is locale language and country  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_available_java_locales()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_available_java_locales'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_available_locales() -> str:
    """
    Get list of available locales (getAvailableLocales)  # noqa: E501

Fetch the list of customized locales from all levels  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_available_locales()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_available_locales'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_available_two_fa_providers() -> str:
    """
    Get available 2FA providers (getAvailableTwoFaProviders)  # noqa: E501

Get the list of provider types available for user to use (the ones configured by tenant or sysadmin). Example of response: ``` [   "TOTP",   "EMAIL",   "SMS" ] ```  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_available_two_fa_providers()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_available_two_fa_providers'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_available_two_fa_providers_v1() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_available_two_fa_providers_v1()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_available_two_fa_providers_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_blob_entities(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    Get Blob Entities (getBlobEntities)  # noqa: E501

Returns a page of BlobEntityWithCustomerInfo object that are available for the current user. The platform uses Blob(binary large object) entities in the reporting feature, in order to store Dashboard states snapshots of different content types in base64 format. BlobEntityWithCustomerInfo represents an object that contains base info about the blob entity(name, type, contentType, etc.) and info about the customer(customerTitle, customerIsPublic) of the user that scheduled generation of the dashboard report. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: A string value representing the blob entity type. For example, 'report'
:param str text_search: The case insensitive 'startsWith' filter based on the blob entity name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: The start timestamp in milliseconds of the search time range over the BlobEntityWithCustomerInfo class field: 'createdTime'.
:param int end_time: The end timestamp in milliseconds of the search time range over the BlobEntityWithCustomerInfo class field: 'createdTime'.
:return: PageDataBlobEntityWithCustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_blob_entities(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_blob_entities'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_blob_entities_by_ids(blob_entity_ids_json: str) -> str:
    """
    Get Blob Entities By Ids (getBlobEntitiesByIds)  # noqa: E501

Requested blob entities must be owned by tenant or assigned to customer which user is performing the request. The platform uses Blob(binary large object) entities in the reporting feature, in order to store Dashboard states snapshots of different content types in base64 format. BlobEntityInfo represents an object that contains base info about the blob entity(name, type, contentType, etc.). See the 'Model' tab of the Response Class for more details.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str blob_entity_ids: A list of blob entity ids, separated by comma ',' (required)
:return: list[BlobEntityInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_blob_entities_by_ids(blob_entity_ids=json.loads(blob_entity_ids_json) if blob_entity_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_blob_entities_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_blob_entity_info_by_id(blob_entity_id_json: str) -> str:
    """
    Get Blob Entity With Customer Info (getBlobEntityInfoById)  # noqa: E501

Fetch the BlobEntityWithCustomerInfo object based on the provided Blob entity Id. The platform uses Blob(binary large object) entities in the reporting feature, in order to store Dashboard states snapshots of different content types in base64 format. BlobEntityWithCustomerInfo represents an object that contains base info about the blob entity(name, type, contentType, etc.) and info about the customer(customerTitle, customerIsPublic) of the user that scheduled generation of the dashboard report. Referencing non-existing Blob entity Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str blob_entity_id: A string value representing the blob entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: BlobEntityWithCustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_blob_entity_info_by_id(blob_entity_id=deserialize_param(blob_entity_id_json, 'BlobEntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_blob_entity_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_type_fqns(widgets_bundle_id_json: str) -> str:
    """
    Get all Widget type fqns for specified Bundle (getBundleWidgetTypeFqns)  # noqa: E501

Returns an array of Widget Type fqns that belong to specified Widget Bundle.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: Widget Bundle Id (required)
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_type_fqns(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_type_fqns'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types(widgets_bundle_id_json: str) -> str:
    """
    Get all Widget types for specified Bundle (getBundleWidgetTypes)  # noqa: E501

Returns an array of Widget Type objects that belong to specified Widget Bundle.Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: Widget Bundle Id (required)
:return: list[WidgetType]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types_by_bundle_alias(is_system: bool, bundle_alias: str) -> str:
    """
    Get all Widget types for specified Bundle (getBundleWidgetTypesByBundleAlias) (Deprecated)  # noqa: E501

Returns an array of Widget Type objects that belong to specified Widget Bundle.Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool is_system: System or Tenant (required)
:param str bundle_alias: Widget Bundle alias (required)
:return: list[WidgetType]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types_by_bundle_alias(is_system=is_system, bundle_alias=bundle_alias)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types_by_bundle_alias'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types_details(widgets_bundle_id_json: str, inline_images: Optional[str] = None) -> str:
    """
    Get all Widget types details for specified Bundle (getBundleWidgetTypesDetails)  # noqa: E501

Returns an array of Widget Type Details objects that belong to specified Widget Bundle.Widget Type Details extend Widget Type and add image and description properties. Those properties are useful to edit the Widget Type but they are not required for Dashboard rendering.    Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: Widget Bundle Id (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: list[WidgetTypeDetails]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types_details(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'), inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types_details'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types_details_by_bundle_alias(is_system: bool, bundle_alias: str) -> str:
    """
    Get all Widget types details for specified Bundle (getBundleWidgetTypesDetailsByBundleAlias) (Deprecated)  # noqa: E501

Returns an array of Widget Type Details objects that belong to specified Widget Bundle.Widget Type Details extend Widget Type and add image and description properties. Those properties are useful to edit the Widget Type but they are not required for Dashboard rendering.    Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool is_system: System or Tenant (required)
:param str bundle_alias: Widget Bundle alias (required)
:return: list[WidgetTypeDetails]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types_details_by_bundle_alias(is_system=is_system, bundle_alias=bundle_alias)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types_details_by_bundle_alias'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types_infos(widgets_bundle_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, full_search: Optional[str] = None, deprecated_filter: Optional[str] = None, widget_type_list_json: str = None) -> str:
    """
    Get Widget Type Info objects (getBundleWidgetTypesInfos)  # noqa: E501

Get the Widget Type Info objects based on the provided parameters. Widget Type Info is a lightweight object that represents Widget Type but does not contain the heavyweight widget descriptor JSON  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: Widget Bundle Id (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the widget type name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param bool full_search: Optional boolean parameter indicating whether search widgets by description not only by name
:param str deprecated_filter: Optional string parameter indicating whether to include deprecated widgets
:param str widget_type_list: A list of string values separated by comma ',' representing one of the widget type value
:return: PageDataWidgetTypeInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types_infos(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, full_search=full_search, deprecated_filter=deprecated_filter, widget_type_list=json.loads(widget_type_list_json) if widget_type_list_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_bundle_widget_types_infos_by_bundle_alias(is_system: bool, bundle_alias: str) -> str:
    """
    Get Widget Type Info objects (getBundleWidgetTypesInfosByBundleAlias) (Deprecated)  # noqa: E501

Get the Widget Type Info objects based on the provided parameters. Widget Type Info is a lightweight object that represents Widget Type but does not contain the heavyweight widget descriptor JSON  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool is_system: System or Tenant (required)
:param str bundle_alias: Widget Bundle alias (required)
:return: list[WidgetTypeInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_bundle_widget_types_infos_by_bundle_alias(is_system=is_system, bundle_alias=bundle_alias)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_bundle_widget_types_infos_by_bundle_alias'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_calculated_field_by_id(calculated_field_id: str) -> str:
    """
    Get Calculated Field (getCalculatedFieldById)  # noqa: E501

Fetch the Calculated Field object based on the provided Calculated Field Id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object calculated_field_id: (required)
:return: CalculatedField
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_calculated_field_by_id(calculated_field_id=calculated_field_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_calculated_field_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_calculated_fields_by_entity_id(entity_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Calculated Fields by Entity Id (getCalculatedFieldsByEntityId)  # noqa: E501

Fetch the Calculated Fields based on the provided Entity Id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param object entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param object page_size: Maximum amount of entities in a one page (required)
:param object page: Sequence number of page starting from 0 (required)
:param object text_search: The case insensitive 'substring' filter based on the calculated field name.
:param object sort_property: Property of entity to sort by
:param object sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCalculatedField
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_calculated_fields_by_entity_id(entity_id=deserialize_param(entity_id_json, 'EntityId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_calculated_fields_by_entity_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_client_registration_templates() -> str:
    """
    Get the list of all OAuth2 client registration templates (getClientRegistrationTemplates)  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501

Mail configuration template is set of default smtp settings for mail server that specific provider supports  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_client_registration_templates()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_client_registration_templates'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_client_registration_templates1() -> str:
    """
    Get the list of all OAuth2 client registration templates (getClientRegistrationTemplates)  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501

Client registration template is OAuth2 provider configuration template with default settings for registering new OAuth2 clients  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[OAuth2ClientRegistrationTemplate]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_client_registration_templates1()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_client_registration_templates1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_client_registration_templates_mail() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_client_registration_templates_mail()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_client_registration_templates_mail'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_component_descriptor_by_clazz(component_descriptor_clazz: str) -> str:
    """
    Get Component Descriptor (getComponentDescriptorByClazz)  # noqa: E501

Gets the Component Descriptor object using class name from the path parameters. Each Component Descriptor represents configuration of specific rule node (e.g. 'Save Timeseries' or 'Send Email'.). The Component Descriptors are used by the rule chain Web UI to build the configuration forms for the rule nodes. The Component Descriptors are discovered at runtime by scanning the class path and searching for @RuleNode annotation. Once discovered, the up to date list of descriptors is persisted to the database.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str component_descriptor_clazz: Component Descriptor class name (required)
:return: ComponentDescriptor
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_component_descriptor_by_clazz(component_descriptor_clazz=component_descriptor_clazz)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_component_descriptor_by_clazz'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_component_descriptors_by_type(component_type: str, rule_chain_type: Optional[str] = None) -> str:
    """
    Get Component Descriptors (getComponentDescriptorsByType)  # noqa: E501

Gets the Component Descriptors using rule node type and optional rule chain type request parameters. Each Component Descriptor represents configuration of specific rule node (e.g. 'Save Timeseries' or 'Send Email'.). The Component Descriptors are used by the rule chain Web UI to build the configuration forms for the rule nodes. The Component Descriptors are discovered at runtime by scanning the class path and searching for @RuleNode annotation. Once discovered, the up to date list of descriptors is persisted to the database.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str component_type: Type of the Rule Node (required)
:param str rule_chain_type: Type of the Rule Chain
:return: list[ComponentDescriptor]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_component_descriptors_by_type(component_type=component_type, rule_chain_type=rule_chain_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_component_descriptors_by_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_component_descriptors_by_types(component_types: str, rule_chain_type: Optional[str] = None) -> str:
    """
    Get Component Descriptors (getComponentDescriptorsByTypes)  # noqa: E501

Gets the Component Descriptors using coma separated list of rule node types and optional rule chain type request parameters. Each Component Descriptor represents configuration of specific rule node (e.g. 'Save Timeseries' or 'Send Email'.). The Component Descriptors are used by the rule chain Web UI to build the configuration forms for the rule nodes. The Component Descriptors are discovered at runtime by scanning the class path and searching for @RuleNode annotation. Once discovered, the up to date list of descriptors is persisted to the database.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str component_types: List of types of the Rule Nodes, (ENRICHMENT, FILTER, TRANSFORMATION, ACTION or EXTERNAL) (required)
:param str rule_chain_type: Type of the Rule Chain
:return: list[ComponentDescriptor]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_component_descriptors_by_types(component_types=component_types, rule_chain_type=rule_chain_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_component_descriptors_by_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_converter_by_id(converter_id_json: str) -> str:
    """
    Get Converter (getConverterById)  # noqa: E501

Fetch the Converter object based on the provided Converter Id. The server checks that the converter is owned by the same tenant.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str converter_id: A string value representing the converter id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Converter
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_converter_by_id(converter_id=deserialize_param(converter_id_json, 'ConverterId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_converter_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_converters(page_size: int, page: int, is_edge_template: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Converters (getConverters)  # noqa: E501

Returns a page of converters owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool is_edge_template: Fetch edge template converters
:param str text_search: The case insensitive 'startsWith' filter based on the converter name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataConverter
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_converters(page_size=page_size, page=page, is_edge_template=is_edge_template, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_converters'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_converters_by_ids(converter_ids_json: str) -> str:
    """
    Get Converters By Ids (getConvertersByIds)  # noqa: E501

Requested converters must be owned by tenant which is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str converter_ids: A list of converter ids, separated by comma ',' (required)
:return: list[Converter]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_converters_by_ids(converter_ids=json.loads(converter_ids_json) if converter_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_converters_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_current_login_white_label_params() -> str:
    """
    Get Login White Labeling configuration (getCurrentWhiteLabelParams)  # noqa: E501

Fetch the Login  White Labeling configuration that corresponds to the authority of the user. The API call is designed to load the Login White Labeling configuration for edition. So, the result is NOT merged with the parent level White Labeling configuration. Let's assume there is a custom White Labeling  configured on a system level. And there is no custom White Labeling  items configured on a tenant level. In such a case, the API call will return default object for the tenant administrator.   Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: LoginWhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_current_login_white_label_params()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_current_login_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_current_o_auth2_info() -> str:
    """
    Get current OAuth2 settings (getCurrentOAuth2Info)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: OAuth2Info
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_current_o_auth2_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_current_o_auth2_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_current_white_label_params() -> str:
    """
    Get White Labeling configuration (getCurrentWhiteLabelParams)  # noqa: E501

Fetch the White Labeling configuration that corresponds to the authority of the user. The API call is designed to load the White Labeling configuration for edition. So, the result is NOT merged with the parent level White Labeling configuration. Let's assume there is a custom White Labeling  configured on a system level. And there is no custom White Labeling  items configured on a tenant level. In such a case, the API call will return default object for the tenant administrator.   Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: WhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_current_white_label_params()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_current_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_menu() -> str:
    """
    Get end-user Custom Menu configuration (getCustomMenu)  # noqa: E501

Fetch the Custom Menu object for the end user. The custom menu is configured in the white labeling parameters. If custom menu configuration on the tenant level is present, it overrides the menu configuration of the system level. Similar, if the custom menu configuration on the customer level is present, it overrides the menu configuration of the tenant level.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: CustomMenu
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_menu()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_menu'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_menu_assignee_list(custom_menu_id_json: str) -> str:
    """
    Get Custom Menu assignee list (getCustomMenuAssigneeList)  # noqa: E501

Fetch the list of Entity Info objects that represents users or customers, or empty list if custom menu is not assigned or has NO_ASSIGN/ALL assignee type.  Security check is performed to verify that the user has 'READ' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[EntityInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_menu_assignee_list(custom_menu_id=deserialize_param(custom_menu_id_json, 'CustomMenuId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_menu_assignee_list'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_menu_config(custom_menu_id_json: str) -> str:
    """
    Get Custom Menu configuration by id (getCustomMenuConfig)  # noqa: E501

Fetch the Custom Menu configuration based on the provided Custom Menu Id.   Security check is performed to verify that the user has 'READ' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: CustomMenuConfig
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_menu_config(custom_menu_id=deserialize_param(custom_menu_id_json, 'CustomMenuId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_menu_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_menu_info_by_id(custom_menu_id_json: str) -> str:
    """
    Get Custom Menu Info (getCustomMenuInfoById)  # noqa: E501

Fetch the Custom Menu Info object based on the provided Custom Menu Id.   Security check is performed to verify that the user has 'READ' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: CustomMenuInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_menu_info_by_id(custom_menu_id=deserialize_param(custom_menu_id_json, 'CustomMenuId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_menu_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_menu_infos(page_size: int, page: int, scope: Optional[str] = None, assignee_type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get all custom menus configured at user level (getCustomMenuInfos)  # noqa: E501

Returns a page of custom menu info objects owned by the tenant or the customer of a current user, scope and assigneeType request parameters can be used to filter the result.  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str scope: Custom menu scope.
:param str assignee_type: Custom menu assignee type.
:param str text_search: The case insensitive 'substring' filter based on the custom menu name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomMenuInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_menu_infos(page_size=page_size, page=page, scope=scope, assignee_type=assignee_type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_menu_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_custom_translation(locale_code: str) -> str:
    """
    Get end-user Custom Translation configuration (getCustomTranslation)  # noqa: E501

Fetch the Custom Translation map for the end user. The custom translation is configured in the white labeling parameters. If custom translation translation is defined on the tenant level, it overrides the custom translation of the system level. Similar, if the custom translation is defined on the customer level, it overrides the translation configuration of the tenant level.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: CustomTranslation
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_custom_translation(locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_asset_infos(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, asset_profile_id: Optional[str] = None) -> str:
    """
    Get Customer Asset Infos (getCustomerAssetInfos)  # noqa: E501

Returns a page of asset info objects owned by the specified customer. Asset Info is an extension of the default Asset object that contains information about the owner name.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAssetInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_asset_infos(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, asset_profile_id=asset_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_asset_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_assets(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Assets (getCustomerAssets)  # noqa: E501

Returns a page of assets objects owned by customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Asset type
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAsset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_assets(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_assets'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_by_id(customer_id_json: str) -> str:
    """
    Get Customer (getCustomerById)  # noqa: E501

Get the Customer object based on the provided Customer Id. If the user has the authority of 'Tenant Administrator', the server checks that the customer is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the user belongs to the customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Customer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_by_id(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_customer_infos(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get Customer sub-customers Infos (getCustomerCustomerInfos)  # noqa: E501

Returns a page of customer info objects owned by the specified customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_customer_infos(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_customer_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_dashboards(customer_id_json: str, page_size: int, page: int, mobile: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Dashboards (getCustomerDashboards)  # noqa: E501

Returns a page of dashboard info objects owned by the specified customer. The Dashboard Info object contains lightweight information about the dashboard (e.g. title, image, assigned customers) but does not contain the heavyweight configuration JSON. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the dashboard title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_dashboards(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, mobile=mobile, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_device_infos(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, device_profile_id: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, active: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get Customer Device Infos (getCustomerDeviceInfos)  # noqa: E501

Returns a page of device info objects owned by the specified customer. Device Info is an extension of the default Device object that contains information about the owner name.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param bool active: A boolean value representing the device active flag.
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDeviceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_device_infos(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, device_profile_id=device_profile_id, text_search=text_search, sort_property=sort_property, sort_order=sort_order, active=active, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_device_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_devices(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Devices (getCustomerDevices)  # noqa: E501

Returns a page of devices objects assigned to customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Device type as the name of the device profile
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDevice
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_devices(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_devices'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_edges(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Edges (getCustomerEdges)  # noqa: E501

Returns a page of edges objects assigned to customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: A string value representing the edge type. For example, 'default'
:param str text_search: The case insensitive 'substring' filter based on the edge name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEdge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_edges(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_edges'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_entity_view_infos(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get Customer Entity View Infos (getCustomerEntityViewInfos)  # noqa: E501

Returns a page of entity view info objects owned by the specified customer. Entity Views Info extends the Entity View with owner name. Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str type:   ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityViewInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_entity_view_infos(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_entity_view_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_entity_views(customer_id_json: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Entity Views (getCustomerEntityViews)  # noqa: E501

Returns a page of Entity View objects assigned to customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type:   ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_entity_views(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_entity_views'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_home_dashboard_info() -> str:
    """
    Get Customer Home Dashboard Info (getCustomerHomeDashboardInfo)  # noqa: E501

Returns the home dashboard info object that is configured as 'homeDashboardId' parameter in the 'additionalInfo' of the corresponding customer.   Available for users with 'CUSTOMER_USER' authority.  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: HomeDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_home_dashboard_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_home_dashboard_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_info_by_id(customer_id_json: str) -> str:
    """
    Get Customer info (getCustomerInfoById)  # noqa: E501

Get the Customer info object based on the provided Customer Id. If the user has the authority of 'Tenant Administrator', the server checks that the customer is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the user belongs to the customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: CustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_info_by_id(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_title_by_id(customer_id_json: str) -> str:
    """
    Get Customer Title (getCustomerTitleById)  # noqa: E501

Get the title of the customer. If the user has the authority of 'Tenant Administrator', the server checks that the customer is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the user belongs to the customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_title_by_id(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_title_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_user_infos(customer_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, include_customers: Optional[str] = None) -> str:
    """
    Get Customer user Infos (getCustomerUserInfos)  # noqa: E501

Returns a page of user info objects owned by the specified customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_customers: Include customer or sub-customer entities
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUserInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_user_infos(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, include_customers=include_customers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_user_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customer_users(customer_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customer Users (getCustomerUsers)  # noqa: E501

Returns a page of users owned by customer. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customer_users(customer_id=deserialize_param(customer_id_json, 'CustomerId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customer_users'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customers(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Customers (getCustomers)  # noqa: E501

Returns a page of customers owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customers(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customers'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customers_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get customers by Entity Group Id (getCustomersByEntityGroupId)  # noqa: E501

Returns a page of Customer objects that belongs to specified Entity Group Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customers_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customers_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_customers_by_ids(customer_ids: str) -> str:
    """
    Get customers by Customer Ids (getCustomersByEntityGroupId)  # noqa: E501

Returns a list of Customer objects based on the provided ids. Filters the list based on the user permissions.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_ids: A list of customer ids, separated by comma ',' (required)
:return: list[Customer]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_customers_by_ids(customer_ids=customer_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_customers_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_dashboard_by_id(dashboard_id_json: str, inline_images: Optional[str] = None) -> str:
    """
    Get Dashboard (getDashboardById)  # noqa: E501

Get the dashboard based on 'dashboardId' parameter. The Dashboard object is a heavyweight object that contains information about the dashboard (e.g. title, image, assigned customers) and also configuration JSON (e.g. layouts, widgets, entity aliases).  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str dashboard_id: A string value representing the dashboard id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: Dashboard
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_dashboard_by_id(dashboard_id=deserialize_param(dashboard_id_json, 'DashboardId'), inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_dashboard_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_dashboard_info_by_id(dashboard_id_json: str) -> str:
    """
    Get Dashboard Info (getDashboardInfoById)  # noqa: E501

Get the information about the dashboard based on 'dashboardId' parameter. The Dashboard Info object contains lightweight information about the dashboard (e.g. title, image, assigned customers) but does not contain the heavyweight configuration JSON.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str dashboard_id: A string value representing the dashboard id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_dashboard_info_by_id(dashboard_id=deserialize_param(dashboard_id_json, 'DashboardId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_dashboard_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_dashboards_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get dashboards by Entity Group Id (getDashboardsByEntityGroupId)  # noqa: E501

Returns a page of Dashboard objects that belongs to specified Entity Group Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the dashboard title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_dashboards_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_dashboards_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_dashboards_by_ids(dashboard_ids_json: str) -> str:
    """
    Get dashboards by Dashboard Ids (getDashboardsByIds)  # noqa: E501

Returns a list of DashboardInfo objects based on the provided ids. Filters the list based on the user permissions.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str dashboard_ids: A list of dashboard ids, separated by comma ',' (required)
:return: list[DashboardInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_dashboards_by_ids(dashboard_ids=json.loads(dashboard_ids_json) if dashboard_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_dashboards_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_default_asset_profile_info() -> str:
    """
    Get Default Asset Profile (getDefaultAssetProfileInfo)  # noqa: E501

Fetch the Default Asset Profile Info object. Asset Profile Info is a lightweight object that includes main information about Asset Profile.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: AssetProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_default_asset_profile_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_default_asset_profile_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_default_device_profile_info() -> str:
    """
    Get Default Device Profile (getDefaultDeviceProfileInfo)  # noqa: E501

Fetch the Default Device Profile Info object. Device Profile Info is a lightweight object that includes main information about Device Profile excluding the heavyweight configuration object.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: DeviceProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_default_device_profile_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_default_device_profile_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_default_tenant_profile_info() -> str:
    """
    Get default Tenant Profile Info (getDefaultTenantProfileInfo)  # noqa: E501

Fetch the default Tenant Profile Info object based. Tenant Profile Info is a lightweight object that contains only id and name of the profile.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: EntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_default_tenant_profile_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_default_tenant_profile_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_attributes(device_token: str, client_keys: str, shared_keys: str) -> str:
    """
    Get attributes (getDeviceAttributes)  # noqa: E501

Returns all attributes that belong to device. Use optional 'clientKeys' and/or 'sharedKeys' parameter to return specific attributes.   Example of the result:   ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ```  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str client_keys: Comma separated key names for attribute with client scope (required)
:param str shared_keys: Comma separated key names for attribute with shared scope (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_attributes(device_token=device_token, client_keys=client_keys, shared_keys=shared_keys)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_by_id(device_id_json: str) -> str:
    """
    Get Device (getDeviceById)  # noqa: E501

Fetch the Device object based on the provided Device Id.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Device
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_by_id(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_credentials_by_device_id(device_id_json: str) -> str:
    """
    Get Device Credentials (getDeviceCredentialsByDeviceId)  # noqa: E501

If during device creation there wasn't specified any credentials, platform generates random 'ACCESS_TOKEN' credentials.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ_CREDENTIALS' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeviceCredentials
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_credentials_by_device_id(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_credentials_by_device_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_info_by_id(device_id_json: str) -> str:
    """
    Get Device (getDeviceInfoById)  # noqa: E501

Fetch the Device info object based on the provided Device Id. Device Info is an extension of the default Device object that contains information about the owner name.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeviceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_info_by_id(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profile_by_id(device_profile_id_json: str, inline_images: Optional[str] = None) -> str:
    """
    Get Device Profile (getDeviceProfileById)  # noqa: E501

Fetch the Device Profile object based on the provided Device Profile Id. The server checks that the device profile is owned by the same tenant.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: DeviceProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profile_by_id(device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'), inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profile_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profile_info_by_id(device_profile_id_json: str) -> str:
    """
    Get Device Profile Info (getDeviceProfileInfoById)  # noqa: E501

Fetch the Device Profile Info object based on the provided Device Profile Id. Device Profile Info is a lightweight object that includes main information about Device Profile excluding the heavyweight configuration object.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeviceProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profile_info_by_id(device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profile_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profile_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, transport_type: Optional[str] = None) -> str:
    """
    Get Device Profiles for transport type (getDeviceProfileInfos)  # noqa: E501

Returns a page of devices profile info objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. Device Profile Info is a lightweight object that includes main information about Device Profile excluding the heavyweight configuration object.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the device profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param str transport_type: Type of the transport
:return: PageDataDeviceProfileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profile_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, transport_type=transport_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profile_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profile_names(active_only: str) -> str:
    """
    Get Device Profile names (getDeviceProfileNames)  # noqa: E501

Returns a set of unique device profile names owned by the tenant.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool active_only: Flag indicating whether to retrieve exclusively the names of device profiles that are referenced by tenant's devices.
:return: list[EntityInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profile_names(active_only=active_only)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profile_names'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profiles(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Device Profiles (getDeviceProfiles)  # noqa: E501

Returns a page of devices profile objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the device profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDeviceProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profiles(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profiles'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_profiles_by_ids(device_profile_ids_json: str) -> str:
    """
    Get Device Profiles By Ids (getDeviceProfilesByIds)  # noqa: E501

Requested device profiles must be owned by tenant which is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_ids: A list of device profile ids, separated by comma ',' (required)
:return: list[DeviceProfileInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_profiles_by_ids(device_profile_ids=json.loads(device_profile_ids_json) if device_profile_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_profiles_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_publish_telemetry_commands(device_id_json: str) -> str:
    """
    Get commands to publish device telemetry (getDevicePublishTelemetryCommands)  # noqa: E501

Fetch the list of commands to publish device telemetry based on device profile If the user has the authority of 'Tenant Administrator', the server checks that the device is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the device is assigned to the same customer.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_publish_telemetry_commands(device_id=deserialize_param(device_id_json, 'DeviceId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_publish_telemetry_commands'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_device_types() -> str:
    """
    Get Device Types (getDeviceTypes)  # noqa: E501

Deprecated. See 'getDeviceProfileNames' API from Device Profile Controller instead.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[EntitySubtype]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_device_types()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_device_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_devices_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get devices by Entity Group Id (getDevicesByEntityGroupId)  # noqa: E501

Returns a page of Device objects that belongs to specified Entity Group Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDevice
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_devices_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_devices_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_devices_by_ids(device_ids_json: str) -> str:
    """
    Get Devices By Ids (getDevicesByIds)  # noqa: E501

Requested devices must be owned by tenant or assigned to customer which user is performing the request.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_ids: A list of devices ids, separated by comma ',' (required)
:return: list[Device]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_devices_by_ids(device_ids=json.loads(device_ids_json) if device_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_devices_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_domain_info_by_id(domain_id_json: str) -> str:
    """
    Get Domain info by Id (getDomainInfoById)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: DomainInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_domain_info_by_id(domain_id=deserialize_param(domain_id_json, 'DomainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_domain_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_domain_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Domain infos (getDomainInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object page_size: Maximum amount of entities in a one page (required)
:param object page: Sequence number of page starting from 0 (required)
:param object text_search: Case-insensitive 'substring' filter based on domain's name
:param object sort_property: Property of entity to sort by
:param object sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDomainInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_domain_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_domain_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_downlink_converter(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get downlink converter (getDownlinkConverter)  # noqa: E501

Returns downlink converter body for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_downlink_converter(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_downlink_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_downlink_converter_metadata(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get downlink converter metadata (getDownlinkConverterMetadata)  # noqa: E501

Returns downlink converter metadata for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_downlink_converter_metadata(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_downlink_converter_metadata'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_downlink_payload(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get downlink payload (getDownlinkPayload)  # noqa: E501

Returns payload example for the downlink converter for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_downlink_payload(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_downlink_payload'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_by_id(edge_id_json: str) -> str:
    """
    Get Edge (getEdgeById)  # noqa: E501

Get the Edge object based on the provided Edge Id. If the user has the authority of 'Tenant Administrator', the server checks that the edge is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the edge is assigned to the same customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Edge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_by_id(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_entity_groups(edge_id_json: str, group_type: str, page_size: int, page: int, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edge Entity Groups by entity type (getEdgeEntityGroups)  # noqa: E501

Returns a page of Entity Group Info objects based on the provided Entity Type and assigned to the provided Edge entity. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str group_type: EntityGroup type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_entity_groups(edge_id=deserialize_param(edge_id_json, 'EdgeId'), group_type=group_type, page_size=page_size, page=page, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_entity_groups'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_events(edge_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    Get Edge Events (getEdgeEvents)  # noqa: E501

Returns a page of edge events for the requested edge. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the edge event type name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param int start_time: Timestamp. Edge events with creation time before it won't be queried
:param int end_time: Timestamp. Edge events with creation time after it won't be queried
:return: PageDataEdgeEvent
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_events(edge_id=deserialize_param(edge_id_json, 'EdgeId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_events'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_install_instructions(edge_id_json: str, method: str) -> str:
    """
    Get Edge Install Instructions (getEdgeInstallInstructions)  # noqa: E501

Get an install instructions for provided edge id.If the user has the authority of 'Tenant Administrator', the server checks that the edge is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the edge is assigned to the same customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str method: Installation method ('docker', 'ubuntu' or 'centos') (required)
:return: EdgeInstructions
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_install_instructions(edge_id=deserialize_param(edge_id_json, 'EdgeId'), method=method)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_install_instructions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_integration_infos(edge_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edge Integrations (getEdgeIntegrationInfos)  # noqa: E501

Returns a page of Integrations assigned to the specified edge. The integration object contains information about the Integration, including the heavyweight configuration object. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the integration name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataIntegrationInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_integration_infos(edge_id=deserialize_param(edge_id_json, 'EdgeId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_integration_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_integrations(edge_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edge Integrations (getEdgeIntegrations)  # noqa: E501

Returns a page of Integrations assigned to the specified edge. The integration object contains information about the Integration, including the heavyweight configuration object. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the integration name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataIntegration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_integrations(edge_id=deserialize_param(edge_id_json, 'EdgeId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_integrations'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_rule_chains(edge_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edge Rule Chains (getEdgeRuleChains)  # noqa: E501

Returns a page of Rule Chains assigned to the specified edge. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the rule chain name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataRuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_rule_chains(edge_id=deserialize_param(edge_id_json, 'EdgeId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_scheduler_events(edge_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edge Scheduler Events (getEdgeSchedulerEvents)  # noqa: E501

Returns a page of  Scheduler Events Info objects based on the provided Edge entity. Scheduler Event extends Scheduler Event Info object and adds 'configuration' - a JSON structure of scheduler event configuration. See the 'Model' tab of the Response Class for more details. Scheduler Events allows you to schedule various types of events with flexible schedule configuration. Scheduler fires configured scheduler events according to their schedule. See the 'Model' tab of the Response Class for more details. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the scheduler event name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataSchedulerEventInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_scheduler_events(edge_id=deserialize_param(edge_id_json, 'EdgeId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_scheduler_events'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_types() -> str:
    """
    Get Edge Types (getEdgeTypes)  # noqa: E501

Returns a set of unique edge types based on edges that are either owned by the tenant or assigned to the customer which user is performing the request.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[EntitySubtype]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_types()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edge_upgrade_instructions(edge_version: str, method: str) -> str:
    """
    Get Edge Upgrade Instructions (getEdgeUpgradeInstructions)  # noqa: E501

Get an upgrade instructions for provided edge vesion.If the user has the authority of 'Tenant Administrator', the server checks that the edge is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the edge is assigned to the same customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_version: Edge version (required)
:param str method: Upgrade method ('docker', 'ubuntu' or 'centos') (required)
:return: EdgeInstructions
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edge_upgrade_instructions(edge_version=edge_version, method=method)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edge_upgrade_instructions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edges(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Edges (getEdges)  # noqa: E501

Returns a page of edges owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the edge name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEdge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edges(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edges'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edges_by_ids(edge_ids_json: str) -> str:
    """
    Get Edges By Ids (getEdgesByIds)  # noqa: E501

Requested edges must be owned by tenant or assigned to customer which user is performing the request.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_ids: A list of edges ids, separated by comma ',' (required)
:return: list[Edge]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edges_by_ids(edge_ids=json.loads(edge_ids_json) if edge_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edges_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_edqs_state() -> str:
    """
    getEdqsState  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: EdqsState
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_edqs_state()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_edqs_state'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entities(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Group Entities (getEntities)  # noqa: E501

Returns a page of Short Entity View objects that belongs to specified Entity Group Id. Short Entity View object contains the entity id and number of fields (attributes, telemetry, etc). List of those fields is configurable and defined in the group configuration.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataShortEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entities(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entities'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_data_info(version_id: str, entity_id_json: str) -> str:
    """
    Get entity data info (getEntityDataInfo)  # noqa: E501

Retrieves short info about the remote entity by external id at a concrete version.  Returned entity data info contains following properties: `hasRelations` (whether stored entity data contains relations), `hasAttributes` (contains attributes) and `hasCredentials` (whether stored device data has credentials).  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str version_id: Version id, for example fd82625bdd7d6131cf8027b44ee967012ecaf990. Represents commit hash. (required)
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str external_entity_uuid: A string value representing external entity id (required)
:return: DeferredResultEntityDataInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_data_info(version_id=version_id, entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_data_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_all_by_owner_and_type(owner_type: str, owner_id_json: str, group_type: str) -> str:
    """
    Get special group All by owner and entity type (getEntityGroupsByOwnerAndType)  # noqa: E501

Fetch reserved group 'All' based on the provided Owner Id and Entity Type. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:return: EntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_all_by_owner_and_type(owner_type=owner_type, owner_id=deserialize_param(owner_id_json, 'UserId'), group_type=group_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_all_by_owner_and_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_by_id(entity_group_id_json: str) -> str:
    """
    Get Entity Group Info (getEntityGroupById)  # noqa: E501

Fetch the Entity Group object based on the provided Entity Group Id. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Entity group name is unique in the scope of owner and entity type. For example, you can't create two tenant device groups called 'Water meters'. However, you may create device and asset group with the same name. And also you may create groups with the same name for two different customers of the same tenant.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_by_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_by_owner_and_name_and_type(owner_id_json: str, group_type: str, group_name: str) -> str:
    """
    Get Entity Group by owner, type and name (getEntityGroupByOwnerAndNameAndType)  # noqa: E501

Fetch the Entity Group object based on the provided Entity Group Id. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Entity group name is unique in the scope of owner and entity type. For example, you can't create two tenant device groups called 'Water meters'. However, you may create device and asset group with the same name. And also you may create groups with the same name for two different customers of the same tenant.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:param str group_name: Entity Group name (required)
:return: EntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_by_owner_and_name_and_type(owner_id=deserialize_param(owner_id_json, 'UserId'), group_type=group_type, group_name=group_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_by_owner_and_name_and_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_entity_info_by_id(entity_group_id_json: str) -> str:
    """
    Get Entity Group Entity Info (getEntityGroupEntityInfoById)  # noqa: E501

Fetch the Entity Group Entity Info object based on the provided Entity Group Id. Entity Info is a lightweight object that contains only id and name of the entity group.   Entity group name is unique in the scope of owner and entity type. For example, you can't create two tenant device groups called 'Water meters'. However, you may create device and asset group with the same name. And also you may create groups with the same name for two different customers of the same tenant.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_entity_info_by_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_entity_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_entity_infos_by_ids(entity_group_ids_json: str) -> str:
    """
    Get Entity Group Entity Infos by Ids (getEntityGroupEntityInfosByIds)  # noqa: E501

Fetch the list of Entity Group Entity Info objects based on the provided entity group ids list. Entity Info is a lightweight object that contains only id and name of the entity group.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_ids: A list of group ids, separated by comma ',' (required)
:return: list[EntityInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_entity_infos_by_ids(entity_group_ids=json.loads(entity_group_ids_json) if entity_group_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_entity_infos_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_entity_infos_by_owner_and_type_and_page_link(owner_id: str, owner_type: str, group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Group Entity Infos by owner and entity type and page link (getEntityGroupEntityInfosByOwnerAndTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group Entity Info objects based on the provided Owner Id and Entity Type and Page Link. Entity Info is a lightweight object that contains only id and name of the entity group. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_entity_infos_by_owner_and_type_and_page_link(owner_id=owner_id, owner_type=owner_type, group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_entity_infos_by_owner_and_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_entity_infos_by_type_and_page_link(group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Group Entity Infos by entity type and page link (getEntityGroupEntityInfosByTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group Entity Info objects based on the provided Entity Type and Page Link. Entity Info is a lightweight object that contains only id and name of the entity group. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_shared: Whether to include shared entity groups.
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_entity_infos_by_type_and_page_link(group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_entity_infos_by_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_entity_infos_hierarchy_by_owner_and_type_and_page_link(owner_id: str, owner_type: str, group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Group Entity Infos for all owners starting from specified than ending with owner of current user (getEntityGroupEntityInfosHierarchyByOwnerAndTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group Entity Info objects based on the provided Owner Id and Entity Type and Page Link. Entity Info is a lightweight object that contains only id and name of the entity group. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_entity_infos_hierarchy_by_owner_and_type_and_page_link(owner_id=owner_id, owner_type=owner_type, group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_entity_infos_hierarchy_by_owner_and_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_group_permissions(entity_group_id_json: str) -> str:
    """
    Get group permissions by Entity Group Id (getEntityGroupPermissions)  # noqa: E501

Returns a list of group permission objects that is assigned for the specified Entity Group Id. Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Group Permission Info object extends the Group Permissions with the full information about Role and User and/or Entity Groups.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[GroupPermissionInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_group_permissions(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_group_permissions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_by_ids(entity_group_ids_json: str) -> str:
    """
    Get Entity Groups by Ids (getEntityGroupsByIds)  # noqa: E501

Fetch the list of Entity Group Info objects based on the provided entity group ids list. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_ids: A list of group ids, separated by comma ',' (required)
:return: list[EntityGroupInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_by_ids(entity_group_ids=json.loads(entity_group_ids_json) if entity_group_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_by_owner_and_type(owner_type: str, owner_id_json: str, group_type: str) -> str:
    """
    Get Entity Groups by owner and entity type (getEntityGroupsByOwnerAndType)  # noqa: E501

Fetch the list of Entity Group Info objects based on the provided Owner Id and Entity Type. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:return: list[EntityGroupInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_by_owner_and_type(owner_type=owner_type, owner_id=deserialize_param(owner_id_json, 'UserId'), group_type=group_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_by_owner_and_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_by_owner_and_type_and_page_link(owner_type: str, owner_id_json: str, group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Groups by owner and entity type and page link (getEntityGroupsByOwnerAndTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group objects based on the provided Owner Id and Entity Type and Page Link. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_by_owner_and_type_and_page_link(owner_type=owner_type, owner_id=deserialize_param(owner_id_json, 'UserId'), group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_by_owner_and_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_by_type(group_type: str, include_shared: Optional[str] = None) -> str:
    """
    Get Entity Groups by entity type (getEntityGroupsByType)  # noqa: E501

Fetch the list of Entity Group Info objects based on the provided Entity Type. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:param bool include_shared: Whether to include shared entity groups.
:return: list[EntityGroupInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_by_type(group_type=group_type, include_shared=include_shared)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_by_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_by_type_and_page_link(group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Groups by entity type and page link (getEntityGroupsByTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group Info objects based on the provided Entity Type and Page Link. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_shared: Whether to include shared entity groups.
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_by_type_and_page_link(group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_by_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_for_entity(entity_id_json: str) -> str:
    """
    Get Entity Groups by Entity Id (getEntityGroupsForEntity)  # noqa: E501

Returns a list of groups that contain the specified Entity Id. For example, all device groups that contain specific device. The list always contain at least one element - special group 'All'.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: Entity Group type (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[EntityGroupId]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_for_entity(entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_for_entity'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_groups_hierarchy_by_owner_and_type_and_page_link(owner_id: str, owner_type: str, group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Groups for all owners starting from specified than ending with owner of current user (getEntityGroupsHierarchyByOwnerAndTypeAndPageLink)  # noqa: E501

Returns a page of Entity Group objects based on the provided Owner Id and Entity Type and Page Link. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_groups_hierarchy_by_owner_and_type_and_page_link(owner_id=owner_id, owner_type=owner_type, group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_groups_hierarchy_by_owner_and_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_view_by_id(entity_view_id_json: str) -> str:
    """
    Get entity view (getEntityViewById)  # noqa: E501

Fetch the EntityView object based on the provided entity view id. Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers. See the 'Model' tab for more details.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_view_id: A string value representing the entity view id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_view_by_id(entity_view_id=deserialize_param(entity_view_id_json, 'EntityViewId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_view_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_view_info_by_id(entity_view_id_json: str) -> str:
    """
    Get entity view info (getEntityViewInfoById)  # noqa: E501

Fetch the Entity View info object based on the provided entity view id. Entity Views Info extends the Entity View with owner name. Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers. See the 'Model' tab for more details.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_view_id: A string value representing the entity view id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityViewInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_view_info_by_id(entity_view_id=deserialize_param(entity_view_id_json, 'EntityViewId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_view_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_view_types() -> str:
    """
    Get Entity View Types (getEntityViewTypes)  # noqa: E501

Returns a set of unique entity view types based on entity views that are either owned by the tenant or assigned to the customer which user is performing the request.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[EntitySubtype]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_view_types()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_view_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_views_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get entity views by Entity Group Id (getEntityViewsByEntityGroupId)  # noqa: E501

Returns a page of Entity View objects that belongs to specified Entity View Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_views_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_views_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_entity_views_by_ids(entity_view_ids_json: str) -> str:
    """
    Get Entity Views By Ids (getEntityViewsByIds)  # noqa: E501

Requested entity views must be owned by tenant or assigned to customer which user is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_view_ids: A list of entity view ids, separated by comma ',' (required)
:return: list[EntityView]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_entity_views_by_ids(entity_view_ids=json.loads(entity_view_ids_json) if entity_view_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_entity_views_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_events_get(entity_id_json: str, tenant_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_events_get(entity_id=deserialize_param(entity_id_json, 'EntityId'), tenant_id=deserialize_param(tenant_id_json, 'TenantId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_events_get'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_events_post(tenant_id_json: str, page_size: int, page: int, entity_id_json: str, body: str, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_events_post(tenant_id=deserialize_param(tenant_id_json, 'TenantId'), page_size=page_size, page=page, entity_id=deserialize_param(entity_id_json, 'EntityId'), body=body, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_events_post'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_events_v1_get1(entity_id_json: str, event_type: str, tenant_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_events_v1_get1(entity_id=deserialize_param(entity_id_json, 'EntityId'), event_type=event_type, tenant_id=deserialize_param(tenant_id_json, 'TenantId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_events_v1_get1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_features_info() -> str:
    """
    Get features info (getFeaturesInfo)  # noqa: E501

Get information about enabled/disabled features.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: FeaturesInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_features_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_features_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_firmware(device_token: str, title: str, version: str, size: Optional[str] = None, chunk: Optional[str] = None) -> str:
    """
    Get Device Firmware (getFirmware)  # noqa: E501

Downloads the current firmware package.When the platform initiates firmware update, it informs the device by updating the 'fw_title', 'fw_version', 'fw_checksum' and 'fw_checksum_algorithm' shared attributes.The 'fw_title' and 'fw_version' parameters must be supplied in this request to double-check that the firmware that device is downloading matches the firmware it expects to download. This is important, since the administrator may change the firmware assignment while device is downloading the firmware.   Optional 'chunk' and 'size' parameters may be used to download the firmware in chunks. For example, device may request first 16 KB of firmware using 'chunk'=0 and 'size'=16384. Next 16KB using 'chunk'=1 and 'size'=16384. The last chunk should have less bytes then requested using 'size' parameter.   The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str title: Title of the firmware, corresponds to the value of 'fw_title' attribute. (required)
:param str version: Version of the firmware, corresponds to the value of 'fw_version' attribute. (required)
:param int size: Size of the chunk. Optional. Omit to download the entire file without chunks.
:param int chunk: Index of the chunk. Optional. Omit to download the entire file without chunks.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_firmware(device_token=device_token, title=title, version=version, size=size, chunk=chunk)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_firmware'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_firmware_by_id(group_id_json: str, firmware_type: str) -> str:
    """
    getFirmwareById  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_id: groupId (required)
:param str firmware_type: firmwareType (required)
:return: DeviceGroupOtaPackage
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_firmware_by_id(group_id=deserialize_param(group_id_json, 'EntityGroupId'), firmware_type=firmware_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_firmware_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_full_translation(locale_code: str, if_none_match: Optional[str] = None, accept_encoding: Optional[str] = None) -> str:
    """
    Get end-user all-to-one translation (getFullTranslation)  # noqa: E501

Fetch the end-user translation for specified locale. The result is the merge of user custom translation, system language translation and default locale translation.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:param str if_none_match:
:param str accept_encoding:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_full_translation(locale_code=locale_code, if_none_match=if_none_match, accept_encoding=accept_encoding)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_full_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_group_entity(entity_group_id_json: str, entity_id_json: str) -> str:
    """
    Get Group Entity (getGroupEntity)  # noqa: E501

Fetch the Short Entity View object based on the group and entity id. Short Entity View object contains the entity id and number of fields (attributes, telemetry, etc). List of those fields is configurable and defined in the group configuration.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: ShortEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_group_entity(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_group_entity'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_group_ota_packages(group_id_json: str, type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get group OTA Package Infos (getGroupOtaPackages)  # noqa: E501

Returns a page of OTA Package Info objects owned by tenant, and by entity group. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. OTA Package Info is a lightweight object that includes main information about the OTA Package excluding the heavyweight data.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str type: OTA Package type. (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the ota package title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataOtaPackageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_group_ota_packages(group_id=deserialize_param(group_id_json, 'EntityGroupId'), type=type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_group_ota_packages'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_group_permission_by_id(group_permission_id_json: str) -> str:
    """
    Get Group Permission (getGroupPermissionById)  # noqa: E501

Fetch the Group Permission object based on the provided Group Permission Id. Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_permission_id: A string value representing the group permission id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: GroupPermission
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_group_permission_by_id(group_permission_id=deserialize_param(group_permission_id_json, 'GroupPermissionId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_group_permission_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_group_permission_info_by_id(group_permission_id_json: str, is_user_group: bool) -> str:
    """
    Get Group Permission Info (getGroupPermissionInfoById)  # noqa: E501

Fetch the Group Permission Info object based on the provided Group Permission Id and the flag that controls what additional information to load: User or Entity Group. Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Group Permission Info object extends the Group Permissions with the full information about Role and User and/or Entity Groups.  Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_permission_id: A string value representing the group permission id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool is_user_group: Load additional information about User('true') or Entity Group('false). (required)
:return: GroupPermissionInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_group_permission_info_by_id(group_permission_id=deserialize_param(group_permission_id_json, 'GroupPermissionId'), is_user_group=is_user_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_group_permission_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_help_base_url() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_help_base_url()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_help_base_url'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_highest_alarm_severity(entity_id_json: str, search_status: Optional[str] = None, status: Optional[str] = None, assignee_id: Optional[str] = None) -> str:
    """
    Get Highest Alarm Severity (getHighestAlarmSeverity)  # noqa: E501

Search the alarms by originator ('entityType' and entityId') and optional 'status' or 'searchStatus' filters and returns the highest AlarmSeverity(CRITICAL, MAJOR, MINOR, WARNING or INDETERMINATE). Specifying both parameters 'searchStatus' and 'status' at the same time will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str search_status: A string value representing one of the AlarmSearchStatus enumeration value
:param str status: A string value representing one of the AlarmStatus enumeration value
:param str assignee_id: A string value representing the assignee user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_highest_alarm_severity(entity_id=deserialize_param(entity_id_json, 'EntityId'), search_status=search_status, status=status, assignee_id=assignee_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_highest_alarm_severity'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_home_dashboard() -> str:
    """
    Get Home Dashboard (getHomeDashboard)  # noqa: E501

Returns the home dashboard object that is configured as 'homeDashboardId' parameter in the 'additionalInfo' of the User. If 'homeDashboardId' parameter is not set on the User level and the User has authority 'CUSTOMER_USER', check the same parameter for the corresponding Customer. If 'homeDashboardId' parameter is not set on the User and Customer levels then checks the same parameter for the Tenant that owns the user. The Dashboard object is a heavyweight object that contains information about the dashboard (e.g. title, image, assigned customers) and also configuration JSON (e.g. layouts, widgets, entity aliases).  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: HomeDashboard
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_home_dashboard()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_home_dashboard'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_home_dashboard_info() -> str:
    """
    Get Home Dashboard Info (getHomeDashboardInfo)  # noqa: E501

Returns the home dashboard info object that is configured as 'homeDashboardId' parameter in the 'additionalInfo' of the User. If 'homeDashboardId' parameter is not set on the User level and the User has authority 'CUSTOMER_USER', check the same parameter for the corresponding Customer. If 'homeDashboardId' parameter is not set on the User and Customer levels then checks the same parameter for the Tenant that owns the user.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: HomeDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_home_dashboard_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_home_dashboard_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_image_info(_type: str, key: str) -> str:
    """
    getImageInfo  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_image_info(_type=_type, key=key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_image_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_images(page_size: int, page: int, text_search: Optional[str] = None, include_system_images: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    getImages  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool include_system_images: Use 'true' to include system images. Disabled by default. Ignored for requests by users with system administrator authority.
:param str text_search: The case insensitive 'substring' filter based on the resource title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_images(page_size=page_size, page=page, text_search=text_search, include_system_images=include_system_images, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_images'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integration_by_id(integration_id_json: str) -> str:
    """
    Get Integration (getIntegrationById)  # noqa: E501

Fetch the Integration object based on the provided Integration Id. The server checks that the integration is owned by the same tenant.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_id: A string value representing the integration id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Integration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integration_by_id(integration_id=deserialize_param(integration_id_json, 'IntegrationId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integration_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integration_by_routing_key(routing_key: str) -> str:
    """
    Get Integration by Routing Key (getIntegrationByRoutingKey)  # noqa: E501

Fetch the Integration object based on the provided routing key. The server checks that the integration is owned by the same tenant.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str routing_key: A string value representing the integration routing key. For example, '542047e6-c1b2-112e-a87e-e49247c09d4b' (required)
:return: Integration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integration_by_routing_key(routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integration_by_routing_key'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integration_infos(page_size: int, page: int, is_edge_template: str, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Integration Infos (getIntegrationInfos)  # noqa: E501

Returns a page of integration infos owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool is_edge_template: Fetch edge template integrations
:param str text_search: The case insensitive 'startsWith' filter based on the integration name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataIntegrationInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integration_infos(page_size=page_size, page=page, is_edge_template=is_edge_template, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integration_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integrations(page_size: int, page: int, is_edge_template: str, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Integrations (getIntegrations)  # noqa: E501

Returns a page of integrations owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool is_edge_template: Fetch edge template integrations
:param str text_search: The case insensitive 'startsWith' filter based on the integration name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataIntegration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integrations(page_size=page_size, page=page, is_edge_template=is_edge_template, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integrations'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integrations_by_ids(integration_ids_json: str) -> str:
    """
    Get Integrations By Ids (getIntegrationsByIds)  # noqa: E501

Requested integrations must be owned by tenant which is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_ids: A list of integration ids, separated by comma ',' (required)
:return: list[Integration]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integrations_by_ids(integration_ids=json.loads(integration_ids_json) if integration_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integrations_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_integrations_converters_info() -> str:
    """
    Get Integrations Converters info (getIntegrationsConvertersInfo)  # noqa: E501

Returns a JSON object containing information about existing tenant converters and converters available in library.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: dict(str, IntegrationConvertersInfo)
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_integrations_converters_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_integrations_converters_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_job_by_id(id_json: str) -> str:
    """
    Get job by id (getJobById)  # noqa: E501

Fetches job info by id.  Example of a RUNNING CF_REPROCESSING job response: ```json {   "id": {     "entityType": "JOB",     "id": "475e94e0-2f2d-11f0-8240-91e99922a704"   },   "createdTime": 1747053196590,   "tenantId": {     "entityType": "TENANT",     "id": "46859a00-2f2d-11f0-8240-91e99922a704"   },   "type": "CF_REPROCESSING",   "key": "474e4130-2f2d-11f0-8240-91e99922a704",   "entityId": {     "entityType": "DEVICE_PROFILE",     "id": "9fd41f20-31a1-11f0-933e-27998d6db02e"    },   "status": "RUNNING",   "configuration": {     "type": "CF_REPROCESSING",     "calculatedFieldId": {       "entityType": "CALCULATED_FIELD",       "id": "474e4130-2f2d-11f0-8240-91e99922a704"     },     "startTs": 1747051995760,     "endTs": 1747052895760,     "tasksKey": "c3cdbd42-799e-4d3a-9aad-9310f767aa36",     "toReprocess": null   },   "result": {     "jobType": "CF_REPROCESSING",     "successfulCount": 1,     "failedCount": 0,     "discardedCount": 0,     "totalCount": 2,     "results": [],     "generalError": null,     "startTs": 1747323069445,     "finishTs": 1747323070585,     "cancellationTs": 0   } }  ```  Example of a CF_REPROCESSING job with failures: ```json {   ...,   "status": "FAILED",   ...,   "result": {     "jobType": "CF_REPROCESSING",     "successfulCount": 0,     "failedCount": 2,     "discardedCount": 0,     "totalCount": 2,     "results": [       {         "jobType": "CF_REPROCESSING",         "key": "c3cdbd42-799e-4d3a-9aad-9310f767aa36",         "success": false,         "discarded": false,         "failure": {           "error": "Failed to fetch temperature: Failed to fetch timeseries data",           "entityInfo": {             "id": {               "entityType": "DEVICE",               "id": "9fd41f20-31a1-11f0-933e-27998d6db02e"             },             "name": "Test device 1"           }         }       },       {         "jobType": "CF_REPROCESSING",         "key": "c3cdbd42-799e-4d3a-9aad-9310f767aa36",         "success": false,         "discarded": false,         "failure": {           "error": "Failed to fetch temperature: Failed to fetch timeseries data",           "entityInfo": {             "id": {               "entityType": "DEVICE",               "id": "9ffc4090-31a1-11f0-933e-27998d6db02e"             },             "name": "Test device 2"           }         }       }     ],     "generalError": null,     "startTs": 1747323069445,     "finishTs": 1747323070585,     "cancellationTs": 0   } }  ```  Example of a FAILED job result with general error: ```json {   ...,   "status": "FAILED",   ...,   "result": {     "jobType": "CF_REPROCESSING",     "successfulCount": 1,     "failedCount": 0,     "discardedCount": 0,     "totalCount": null,     "results": [],     "generalError": "Timeout to find devices by profile",     "cancellationTs": 0   } }  ```  Example of a CANCELLED job result: ```json {   ...,   "status": "CANCELLED",   ...,   "result": {     "jobType": "CF_REPROCESSING",     "successfulCount": 15,     "failedCount": 0,     "discardedCount": 85,     "totalCount": 100,     "results": [],     "generalError": null,     "cancellationTs": 1747065908414   } }  ```  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: Job
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_job_by_id(id=deserialize_param(id_json, 'JobId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_job_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_jobs(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, start_time: Optional[str] = None, end_time: Optional[str] = None) -> str:
    """
    Get jobs (getJobs)  # noqa: E501

Returns the page of jobs.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on job's description
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param str types: Comma-separated list of job types to include. If empty - all job types are included.
:param str statuses: Comma-separated list of job statuses to include. If empty - all job statuses are included.
:param str entities: Comma-separated list of entity ids. If empty - jobs for all entities are included.
:param int start_time: To only include jobs created after this timestamp.
:param int end_time: To only include jobs created before this timestamp.
:return: PageDataJob
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_jobs(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, start_time=start_time, end_time=end_time)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_jobs'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_jwt_setting() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_jwt_setting()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_jwt_setting'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_last_calculated_field_reprocessing_job(id_json: str) -> str:
    """
    getLastCalculatedFieldReprocessingJob  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str calculated_field_id: (required)
:return: Job
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_last_calculated_field_reprocessing_job(id=deserialize_param(id_json, 'CalculatedFieldId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_last_calculated_field_reprocessing_job'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_latest_calculated_field_debug_event(calculated_field_id_json: str) -> str:
    """
    Get latest calculated field debug event (getLatestCalculatedFieldDebugEvent)  # noqa: E501

Gets latest calculated field debug event for specified calculated field id. Referencing non-existing calculated field id will cause an error.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object calculated_field_id: (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_latest_calculated_field_debug_event(calculated_field_id=deserialize_param(calculated_field_id_json, 'CalculatedFieldId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_latest_calculated_field_debug_event'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_latest_converter_debug_input(converter_id_json: str, integration: Optional[str] = None) -> str:
    """
    Get latest debug input event (getLatestConverterDebugInput)  # noqa: E501

Returns a JSON object of the latest debug event representing the input message the converter processed.   ## Uplink Converter Debug Input Event Example  ```json {    "inContentType":"JSON",    "inContent":"{\\"temp\\":40}",    "inMetadata":"{\\"Header:sec-ch-ua\\":\\"\\\\\\"Chromium\\\\\\";v=\\\\\\"94\\\\\\", \\\\\\"Google Chrome\\\\\\";v=\\\\\\"94\\\\\\", \\\\\\";Not A Brand\\\\\\";v=\\\\\\"99\\\\\\"\\",\\"Header:user-agent\\":\\"Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/94.0.4606.71 Safari/537.36\\",\\"integrationName\\":\\"Integration\\",\\"Header:cookie\\":\\"GUID=zYSs8hymSwZKv8kHALKY; redirect_to=%2F; JSESSIONID=B0A7C8E481409CE7924E738DB04F62F9\\",\\"Header:sec-ch-ua-platform\\":\\"\\\\\\"Linux\\\\\\"\\",\\"Header:accept\\":\\"*/*\\",\\"Header:origin\\":\\"http://localhost:8080\\",\\"Header:sec-fetch-site\\":\\"same-origin\\",\\"Header:connection\\":\\"keep-alive\\",\\"Header:accept-encoding\\":\\"gzip, deflate, br\\",\\"Header:content-type\\":\\"application/json\\",\\"Header:content-length\\":\\"16\\",\\"Header:sec-fetch-mode\\":\\"cors\\",\\"Header:sec-ch-ua-mobile\\":\\"?0\\",\\"Header:sec-fetch-dest\\":\\"empty\\",\\"Header:host\\":\\"localhost:8080\\",\\"Header:referer\\":\\"http://localhost:8080/swagger-ui.html\\",\\"Header:accept-language\\":\\"en-US,en;q=0.9,ru-RU;q=0.8,ru;q=0.7,uk;q=0.6,und;q=0.5\\"}" } ```   * 'inContentType' - content type of the message received by the integration;   * 'inContent' - message data received;   * 'inMetadata' - integration metadata (e.g. headers).  ## Downlink Converter Debug Input Event Example  ```json {    "inContentType":"JSON",    "inContent":"{\\"temp\\":42,\\"humidity\\":77}",    "inMsgType":"POST_TELEMETRY_REQUEST",    "inMetadata":"{\\"data\\":\\"40\\"}",    "inIntegrationMetadata":"{\\"integrationName\\":\\"Integration\\"}" } ```   * 'inContentType' - content type of the message received by the integration;   * 'inContent' - content of the message pushed from the rule engine;   * 'inMsgType' - type of the message pushed from the rule engine;   * 'inMetadata' - content of the message metadata pushed from the rule engine;   * 'inIntegrationMetadata' - integration metadata.     Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str converter_id: A string value representing the converter id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str converter_type: A string value representing the converter type. One of the following: UPLINK, DOWNLINK
:param str integration_type: A string value representing the integration type. One of the following: APACHE_PULSAR, AWS_IOT, AWS_KINESIS, AWS_SQS, AZURE_EVENT_HUB, AZURE_IOT_HUB, AZURE_SERVICE_BUS, CHIRPSTACK, COAP, CUSTOM, HTTP, IBM_WATSON_IOT, KAFKA, LORIOT, MQTT, OCEANCONNECT, OPC_UA, PUB_SUB, RABBITMQ,  SIGFOX,  TCP,  THINGPARK,  TMOBILE_IOT_CDP,  TPE,  TTI,  TTN,  TUYA,  UDP
:param str integration_name: A string value representing the integration name. For example, 'My New Integration'
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_latest_converter_debug_input(converter_id=deserialize_param(converter_id_json, 'ConverterId'), integration=integration)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_latest_converter_debug_input'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_latest_rule_node_debug_input(rule_node_id_json: str) -> str:
    """
    Get latest input message (getLatestRuleNodeDebugInput)  # noqa: E501

Gets the input message from the debug events for specified Rule Chain Id. Referencing non-existing rule chain Id will cause an error.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_node_id: A string value representing the rule node id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_latest_rule_node_debug_input(rule_node_id=deserialize_param(rule_node_id_json, 'RuleNodeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_latest_rule_node_debug_input'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_latest_timeseries(entity_id_json: str, keys: Optional[str] = None, use_strict_data_types: Optional[str] = None) -> str:
    """
    Get latest time-series value (getLatestTimeseries)  # noqa: E501

Returns all time-series that belong to specified entity. Use optional 'keys' parameter to return specific time-series. The result is a JSON object. The format of the values depends on the 'useStrictDataTypes' parameter. By default, all time-series values are converted to strings:   ```json {   "stringTsKey": [{ "value": "value", "ts": 1609459200000}],   "booleanTsKey": [{ "value": "false", "ts": 1609459200000}],   "doubleTsKey": [{ "value": "42.2", "ts": 1609459200000}],   "longTsKey": [{ "value": "73", "ts": 1609459200000}],   "jsonTsKey": [{ "value": "{\\"someNumber\\": 42,\\"someArray\\": [1,2,3],\\"someNestedObject\\": {\\"key\\": \\"value\\"}}", "ts": 1609459200000}] }  ```   However, it is possible to request the values without conversion ('useStrictDataTypes'=true):   ```json {   "stringTsKey": [{ "value": "value", "ts": 1609459200000}],   "booleanTsKey": [{ "value": false, "ts": 1609459200000}],   "doubleTsKey": [{ "value": 42.2, "ts": 1609459200000}],   "longTsKey": [{ "value": 73, "ts": 1609459200000}],   "jsonTsKey": [{      "value": {       "someNumber": 42,       "someArray": [1,2,3],       "someNestedObject": {"key": "value"}     },      "ts": 1609459200000}] }  ```   Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str keys: A string value representing the comma-separated list of telemetry keys. If keys are not selected, the result will return all latest timeseries. For example, 'temperature,humidity'.
:param bool use_strict_data_types: Enables/disables conversion of telemetry values to strings. Conversion is enabled by default. Set parameter to 'true' in order to disable the conversion.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_latest_timeseries(entity_id=deserialize_param(entity_id_json, 'EntityId'), keys=keys, use_strict_data_types=use_strict_data_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_latest_timeseries'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_license_usage_info() -> str:
    """
    Get license usage info (getLicenseUsageInfo)  # noqa: E501

Get license usage info.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: LicenseUsageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_license_usage_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_license_usage_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_login_mobile_info(pkg_name: str, platform: str) -> str:
    """
    Get mobile app login info (getLoginMobileInfo)  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str pkg_name: Mobile application package name (required)
:param str platform: Platform type (required)
:return: LoginMobileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_login_mobile_info(pkg_name=pkg_name, platform=platform)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_login_mobile_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_login_page_translation(locale_code: str, if_none_match: Optional[str] = None, accept_encoding: Optional[str] = None) -> str:
    """
    Get system translation for login page  # noqa: E501

Fetch the end-user translation for specified locale.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:param str if_none_match:
:param str accept_encoding:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_login_page_translation(locale_code=locale_code, if_none_match=if_none_match, accept_encoding=accept_encoding)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_login_page_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_login_processing_url() -> str:
    """
    Get OAuth2 log in processing URL (getLoginProcessingUrl)  # noqa: E501

Returns the URL enclosed in double quotes. After successful authentication with OAuth2 provider, it makes a redirect to this path so that the platform can do further log in processing. This URL may be configured as 'security.oauth2.loginProcessingUrl' property in yml configuration file, or as 'SECURITY_OAUTH2_LOGIN_PROCESSING_URL' env variable. By default it is '/login/oauth2/code/'  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_login_processing_url()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_login_processing_url'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_login_white_label_params(logo_image_checksum: str, favicon_checksum: str) -> str:
    """
    Get Login White Labeling parameters  # noqa: E501

Returns login white-labeling parameters based on the hostname from request.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: LoginWhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_login_white_label_params(logo_image_checksum=logo_image_checksum, favicon_checksum=favicon_checksum)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_login_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_lwm2m_bootstrap_security_info(is_bootstrap_server: bool) -> str:
    """
    Get Lwm2m Bootstrap SecurityInfo (getLwm2mBootstrapSecurityInfo)  # noqa: E501

Get the Lwm2m Bootstrap SecurityInfo object (of the current server) based on the provided isBootstrapServer parameter. If isBootstrapServer == true, get the parameters of the current Bootstrap Server. If isBootstrapServer == false, get the parameters of the current Lwm2m Server. Used for client settings when starting the client in Bootstrap mode.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool is_bootstrap_server: A Boolean value representing the Server SecurityInfo for future Bootstrap client mode settings. Values: 'true' for Bootstrap Server; 'false' for Lwm2m Server.  (required)
:return: LwM2MServerSecurityConfigDefault
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_lwm2m_bootstrap_security_info(is_bootstrap_server=is_bootstrap_server)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_lwm2m_bootstrap_security_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_lwm2m_list_objects(sort_order: str, sort_property: str, object_ids_json: str) -> str:
    """
    Get LwM2M Objects (getLwm2mListObjects)  # noqa: E501

Returns a page of LwM2M objects parsed from Resources with type 'LWM2M_MODEL' owned by tenant or sysadmin. You can specify parameters to filter the results. LwM2M Object is a object that includes information about the LwM2M model which can be used in transport configuration for the LwM2M device profile.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING) (required)
:param str sort_property: Property of entity to sort by (required)
:param str object_ids: LwM2M Object ids. (required)
:return: list[LwM2mObject]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_lwm2m_list_objects(sort_order=sort_order, sort_property=sort_property, object_ids=json.loads(object_ids_json) if object_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_lwm2m_list_objects'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_lwm2m_list_objects_page(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get LwM2M Objects (getLwm2mListObjectsPage)  # noqa: E501

Returns a page of LwM2M objects parsed from Resources with type 'LWM2M_MODEL' owned by tenant or sysadmin. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. LwM2M Object is a object that includes information about the LwM2M model which can be used in transport configuration for the LwM2M device profile.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the resource title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: list[LwM2mObject]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_lwm2m_list_objects_page(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_lwm2m_list_objects_page'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mail_processing_url() -> str:
    """
    Get OAuth2 log in processing URL (getMailProcessingUrl)  # noqa: E501

Returns the URL enclosed in double quotes. After successful authentication with OAuth2 provider and user consent for requested scope, it makes a redirect to this path so that the platform can do further log in processing and generating access tokens.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mail_processing_url()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mail_processing_url'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_max_datapoints_limit() -> str:
    """
    Get max data points limit (getMaxDatapointsLimit)  # noqa: E501

Get the maximum number of data points that dashboard may request from the server per in a single subscription command. This value impacts the time window behavior. It impacts 'Max values' parameter in case user selects 'None' as 'Data aggregation function'. It also impacts the 'Grouping interval' in case of any other 'Data aggregation function' is selected. The actual value of the limit is configurable in the system configuration file.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_max_datapoints_limit()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_max_datapoints_limit'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_merged_custom_translation(locale_code: str) -> str:
    """
    Get end-user Custom Translation configuration (getMergedCustomTranslation)  # noqa: E501

Fetch end-user Custom Translation for specified locale. The custom translation is configured in the white labeling parameters. If custom translation translation is defined on the tenant level, it overrides the custom translation of the system level. Similar, if the custom translation is defined on the customer level, it overrides the translation configuration of the tenant level.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: CustomTranslation
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_merged_custom_translation(locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_merged_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_merged_mobile_app_settings() -> str:
    """
    Get QR code configuration for home page (getMobileAppQrCodeConfig)  # noqa: E501

The response payload contains ui configuration of qr code  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: QrCodeSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_merged_mobile_app_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_merged_mobile_app_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_app_bundle_info_by_id(mobile_app_bundle_id_json: str) -> str:
    """
    Get mobile app bundle info by id (getMobileAppBundleInfoById)  # noqa: E501

  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: MobileAppBundleInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_app_bundle_info_by_id(mobile_app_bundle_id=deserialize_param(mobile_app_bundle_id_json, 'MobileAppBundleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_app_bundle_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_app_by_id(mobile_app_id_json: str) -> str:
    """
    Get mobile info by id (getMobileAppInfoById)  # noqa: E501

  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: MobileApp
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_app_by_id(mobile_app_id=deserialize_param(mobile_app_id_json, 'MobileAppId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_app_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_app_deep_link() -> str:
    """
    Get the deep link to the associated mobile application (getMobileAppDeepLink)  # noqa: E501

Fetch the url that takes user to linked mobile application   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_app_deep_link()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_app_deep_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_app_info_by_id(mobile_app_id_json: str) -> str:
    """
    Get mobile info by id (getMobileAppInfoById)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: MobileAppInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_app_info_by_id(mobile_app_id=deserialize_param(mobile_app_id_json, 'MobileAppId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_app_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_app_settings() -> str:
    """
    Get Mobile application settings (getMobileAppSettings)  # noqa: E501

The response payload contains configuration for android/iOS applications and platform qr code widget settings.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: MobileAppSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_app_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_app_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_mobile_session(x_mobile_token: str) -> str:
    """
    getMobileSession  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str x_mobile_token: X-Mobile-Token (required)
:return: MobileSessionInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_mobile_session(x_mobile_token=x_mobile_token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_mobile_session'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_request_by_id(id: str) -> str:
    """
    Get notification request by id (getNotificationRequestById)  # noqa: E501

Fetches notification request info by request id.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: NotificationRequestInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_request_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_request_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_request_preview(body_json: str, recipients_preview_size: Optional[str] = None) -> str:
    """
    Get notification request preview (getNotificationRequestPreview)  # noqa: E501

Returns preview for notification request.  `processedTemplates` shows how the notifications for each delivery method will look like for the first recipient of the corresponding notification target.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationRequest body:
:param int recipients_preview_size: Amount of the recipients to show in preview
:return: NotificationRequestPreview
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_request_preview(body=deserialize_param(body_json, 'NotificationRequest'), recipients_preview_size=recipients_preview_size)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_request_preview'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_requests(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get notification requests (getNotificationRequests)  # noqa: E501

Returns the page of notification requests submitted by users of this tenant or sysadmins.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filed based on the used template name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataNotificationRequestInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_requests(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_requests'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_rule_by_id(id: str) -> str:
    """
    Get notification rule by id (getNotificationRuleById)  # noqa: E501

Fetches notification rule info by rule's id. In addition to regular notification rule fields, there are `templateName` and `deliveryMethods` in the response.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: NotificationRuleInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_rule_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_rule_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_rules(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get notification rules (getNotificationRules)  # noqa: E501

Returns the page of notification rules.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on rule's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataNotificationRuleInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_rules(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_rules'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_settings() -> str:
    """
    Get notification settings (getNotificationSettings)  # noqa: E501

Retrieves notification settings for this tenant or sysadmin.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: NotificationSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_target_by_id(id: str) -> str:
    """
    Get notification target by id (getNotificationTargetById)  # noqa: E501

Fetches notification target by id.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: NotificationTarget
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_target_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_target_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_targets(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get notification targets (getNotificationTargets)  # noqa: E501

Returns the page of notification targets owned by sysadmin or tenant.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filed based on the target's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataNotificationTarget
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_targets(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_targets'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_targets_by_ids(ids_json: str) -> str:
    """
    Get notification targets by ids (getNotificationTargetsByIds)  # noqa: E501

Returns the list of notification targets found by provided ids.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ids: Comma-separated list of uuids representing targets ids (required)
:return: list[NotificationTarget]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_targets_by_ids(ids=json.loads(ids_json) if ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_targets_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_targets_by_supported_notification_type(notification_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get notification targets by supported notification type (getNotificationTargetsBySupportedNotificationType)  # noqa: E501

Returns the page of notification targets filtered by notification type that they can be used for.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str notification_type: notificationType (required)
:param int page_size: pageSize (required)
:param int page: page (required)
:param str text_search: textSearch
:param str sort_property: sortProperty
:param str sort_order: sortOrder
:return: PageDataNotificationTarget
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_targets_by_supported_notification_type(notification_type=notification_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_targets_by_supported_notification_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_template_by_id(id: str) -> str:
    """
    Get notification template by id (getNotificationTemplateById)  # noqa: E501

Fetches notification template by id.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: NotificationTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_template_by_id(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_template_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notification_templates(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get notification templates (getNotificationTemplates)  # noqa: E501

Returns the page of notification templates owned by sysadmin or tenant.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on template's name and notification type
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param str notification_types: Comma-separated list of notification types to filter the templates
:return: PageDataNotificationTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notification_templates(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notification_templates'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_notifications(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, delivery_method: Optional[str] = None) -> str:
    """
    Get notifications (getNotifications)  # noqa: E501

Returns the page of notifications for current user.  You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for any authorized user.   **WebSocket API**:  There are 2 types of subscriptions: one for unread notifications count, another for unread notifications themselves.  The URI for opening WS session for notifications: `/api/ws/plugins/notifications`.  Subscription command for unread notifications count: ``` {   "unreadCountSubCmd": {     "cmdId": 1234   } } ``` To subscribe for latest unread notifications: ``` {   "unreadSubCmd": {     "cmdId": 1234,     "limit": 10   } } ``` To unsubscribe from any subscription: ``` {   "unsubCmd": {     "cmdId": 1234   } } ``` To mark certain notifications as read, use following command: ``` {   "markAsReadCmd": {     "cmdId": 1234,     "notifications": [       "6f860330-7fc2-11ed-b855-7dd3b7d2faa9",       "5b6dfee0-8d0d-11ed-b61f-35a57b03dade"     ]   } }  ``` To mark all notifications as read: ``` {   "markAllAsReadCmd": {     "cmdId": 1234   } } ```   Update structure for unread **notifications count subscription**: ``` {   "cmdId": 1234,   "totalUnreadCount": 55 } ``` For **notifications subscription**: - full update of latest unread notifications: ``` {   "cmdId": 1234,   "notifications": [     {       "id": {         "entityType": "NOTIFICATION",         "id": "6f860330-7fc2-11ed-b855-7dd3b7d2faa9"       },       ...     }   ],   "totalUnreadCount": 1 } ``` - when new notification arrives or shown notification is updated: ``` {   "cmdId": 1234,   "update": {     "id": {       "entityType": "NOTIFICATION",       "id": "6f860330-7fc2-11ed-b855-7dd3b7d2faa9"     },     # updated notification info, text, subject etc.     ...   },   "totalUnreadCount": 2 } ``` - when unread notifications count changes: ``` {   "cmdId": 1234,   "totalUnreadCount": 5 } ```  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on notification subject or text
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param bool unread_only: To search for unread notifications only
:param str delivery_method: Delivery method
:return: PageDataNotification
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_notifications(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, delivery_method=delivery_method)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_notifications'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_o_auth2_client_by_id(id_json: str) -> str:
    """
    Get OAuth2 Client by id (getOAuth2ClientById)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: OAuth2Client
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_o_auth2_client_by_id(id=deserialize_param(id_json, 'OAuth2ClientId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_o_auth2_client_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_o_auth2_clients(pkg_name: Optional[str] = None, platform: Optional[str] = None) -> str:
    """
    Get OAuth2 clients (getOAuth2Clients)  # noqa: E501

Get the list of OAuth2 clients to log in with, available for such domain scheme (HTTP or HTTPS) (if x-forwarded-proto request header is present - the scheme is known from it) and domain name and port (port may be known from x-forwarded-port header)  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str pkg_name: Mobile application package name, to find OAuth2 clients where there is configured mobile application with such package name
:param str platform: Platform type to search OAuth2 clients for which the usage with this platform type is allowed in the settings. If platform type is not one of allowable values - it will just be ignored
:return: list[OAuth2ClientInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_o_auth2_clients(pkg_name=pkg_name, platform=platform)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_o_auth2_clients'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ota_package_by_id(ota_package_id_json: str) -> str:
    """
    Get OTA Package (getOtaPackageById)  # noqa: E501

Fetch the OTA Package object based on the provided OTA Package Id. The server checks that the OTA Package is owned by the same tenant. OTA Package is a heavyweight object that includes main information about the OTA Package and also data.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_id: A string value representing the ota package id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: OtaPackage
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_ota_package_by_id(ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ota_package_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ota_package_info_by_id(ota_package_id_json: str) -> str:
    """
    Get OTA Package Info (getOtaPackageInfoById)  # noqa: E501

Fetch the OTA Package Info object based on the provided OTA Package Id. OTA Package Info is a lightweight object that includes main information about the OTA Package excluding the heavyweight data.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_id: A string value representing the ota package id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: OtaPackageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_ota_package_info_by_id(ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ota_package_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ota_packages(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get OTA Package Infos (getOtaPackages)  # noqa: E501

Returns a page of OTA Package Info objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. OTA Package Info is a lightweight object that includes main information about the OTA Package excluding the heavyweight data.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the ota package title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataOtaPackageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_ota_packages(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ota_packages'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_ota_packages_v1(device_profile_id_json: str, type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_ota_packages_v1(device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'), type=type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_ota_packages_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_owner_info(owner_type: str, owner_id: str) -> str:
    """
    Get Owner Info (getOwnerInfo)  # noqa: E501

Fetch the owner info (tenant or customer) presented as Entity Info object based on the provided owner Id.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str owner_type: Tenant or Customer (required)
:param str owner_id: A string value representing the Tenant or Customer id (required)
:return: EntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_owner_info(owner_type=owner_type, owner_id=owner_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_owner_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_owner_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Owner Infos (getOwnerInfos)  # noqa: E501

Provides a rage view of Customers that the current user has READ access to. If the current user is Tenant administrator, the result set also contains the tenant. The call is designed for the UI auto-complete component to show tenant and all possible Customers that the user may select to change the owner of the particular entity or entity group.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_owner_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_owner_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_owners(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Owners (getOwners)  # noqa: E501

Provides a rage view of Customers that the current user has READ access to. If the current user is Tenant administrator, the result set also contains the tenant. The call is designed for the UI auto-complete component to show tenant and all possible Customers that the user may select to change the owner of the particular entity or entity group.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataContactBasedobject
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_owners(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_owners'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_persisted_rpc(rpc_id_json: str) -> str:
    """
    Get persistent RPC request  # noqa: E501

Get information about the status of the RPC call.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rpc_id: A string value representing the rpc id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Rpc
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_persisted_rpc(rpc_id=deserialize_param(rpc_id_json, 'RpcId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_persisted_rpc'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_persisted_rpc_by_device(device_id_json: str, page_size: int, page: int, rpc_status: str, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get persistent RPC requests  # noqa: E501

Allows to query RPC calls for specific device using pagination.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str rpc_status: Status of the RPC
:param str text_search: Not implemented. Leave empty.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_persisted_rpc_by_device(device_id=deserialize_param(device_id_json, 'DeviceId'), page_size=page_size, page=page, rpc_status=rpc_status, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_persisted_rpc_by_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_platform_two_fa_settings() -> str:
    """
    Get platform 2FA settings (getPlatformTwoFaSettings)  # noqa: E501

Get platform settings for 2FA. The settings are described for savePlatformTwoFaSettings API method. If 2FA is not configured, then an empty response will be returned.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: PlatformTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_platform_two_fa_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_platform_two_fa_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_privacy_policy() -> str:
    """
    Get Privacy Policy for Self Registration form (getPrivacyPolicy)  # noqa: E501

Fetch the Privacy Policy based on the domain name from the request. Available for non-authorized users.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_privacy_policy()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_privacy_policy'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_qr_code_settings() -> str:
    """
    Get Mobile application settings (getMobileAppSettings)  # noqa: E501

The response payload contains configuration for android/iOS applications and platform qr code widget settings.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: QrCodeSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_qr_code_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_qr_code_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_queue_by_id(queue_id_json: str) -> str:
    """
    Get Queue (getQueueById)  # noqa: E501

Fetch the Queue object based on the provided Queue Id.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str queue_id: A string value representing the queue id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Queue
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_queue_by_id(queue_id=deserialize_param(queue_id_json, 'QueueId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_queue_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_queue_by_name(queue_name: str) -> str:
    """
    Get Queue (getQueueByName)  # noqa: E501

Fetch the Queue object based on the provided Queue name.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str queue_name: A string value representing the queue id. For example, 'Main' (required)
:return: Queue
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_queue_by_name(queue_name=queue_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_queue_by_name'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_queue_stats_by_id(queue_stats_id_json: str) -> str:
    """
    Get Queue stats entity by id (getQueueStatsById)  # noqa: E501

Fetch the Queue stats object based on the provided Queue stats id.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str queue_stats_id: A string value representing the queue stats id. For example, '687f294c-42b6-435a-983c-b7beff2784f9' (required)
:return: QueueStats
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_queue_stats_by_id(queue_stats_id=deserialize_param(queue_stats_id_json, 'QueueId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_queue_stats_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_queue_stats_by_ids(queue_stats_ids_json: str) -> str:
    """
    Get QueueStats By Ids (getQueueStatsByIds)  # noqa: E501

Fetch the Queue stats objects based on the provided ids.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str queue_stats_ids: A list of queue stats ids, separated by comma ',' (required)
:return: list[QueueStats]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_queue_stats_by_ids(queue_stats_ids=json.loads(queue_stats_ids_json) if queue_stats_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_queue_stats_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_recipients_for_notification_target_config(page_size: int, page: int) -> str:
    """
    Get recipients for notification target config (getRecipientsForNotificationTargetConfig)  # noqa: E501

Returns the page of recipients for such notification target configuration.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param NotificationTarget body:
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_recipients_for_notification_target_config(page_size=page_size, page=page)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_recipients_for_notification_target_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_relation(from_id_json: str, relation_type: str, to_id_json: str, relation_type_group: Optional[str] = None) -> str:
    """
    Get Relation (getRelation)  # noqa: E501

Returns relation object between two specified entities if present. Otherwise throws exception.   If the user has the authority of 'System Administrator', the server checks that 'from' and 'to' entities are owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that 'from' and 'to' entities are owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the 'from' and 'to' entities are assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str from_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str from_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type: A string value representing relation type between entities. For example, 'Contains', 'Manages'. It can be any string value. (required)
:param str to_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str to_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str relation_type_group: A string value representing relation type group. For example, 'COMMON'
:return: EntityRelation
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_relation(from_id=deserialize_param(from_id_json, 'EntityId'), relation_type=relation_type, to_id=deserialize_param(to_id_json, 'EntityId'), relation_type_group=relation_type_group)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_relation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_report_template_by_id(report_template_id_json: str) -> str:
    """
    Get Report Template (getReportTemplateById)  # noqa: E501

Fetch the ReportTemplate object based on the provided report template Id. Report Template extends Report Template Info object and adds 'configuration' - a JSON structure of report template configuration. See the 'Model' tab of the Response Class for more details. Referencing non-existing Report Template Id will cause 'Not Found' error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str report_template_id: A string value representing the report template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: ReportTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_report_template_by_id(report_template_id=deserialize_param(report_template_id_json, 'ReportTemplateId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_report_template_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_report_template_info_by_id(report_template_id_json: str) -> str:
    """
    Get Report Template Info (getReportTemplateInfoById)  # noqa: E501

Fetch the ReportTemplateInfo object based on the provided report template Id. Report Templates allows you to create reports according to the report template configuration. Report service uses report template configuration to generate report. See the 'Model' tab of the Response Class for more details. Referencing non-existing Report Template Id will cause 'Not Found' error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str report_template_id: A string value representing the report template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: ReportTemplateInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_report_template_info_by_id(report_template_id=deserialize_param(report_template_id_json, 'ReportTemplateId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_report_template_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_report_templates_by_ids(report_template_ids_json: str) -> str:
    """
    Get report templates by Report Template Ids (getReportTemplatesByIds)  # noqa: E501

Returns a list of ReportTemplateInfo objects based on the provided ids. Filters the list based on the user permissions.   Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str report_template_ids: A list of report template ids, separated by comma ',' (required)
:return: list
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_report_templates_by_ids(report_template_ids=json.loads(report_template_ids_json) if report_template_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_report_templates_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_repository_settings() -> str:
    """
    Get repository settings (getRepositorySettings)  # noqa: E501

Get the repository settings object.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: RepositorySettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_repository_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_repository_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_repository_settings_info() -> str:
    """
    getRepositorySettingsInfo  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: RepositorySettingsInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_repository_settings_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_repository_settings_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_resource_by_id(resource_id_json: str) -> str:
    """
    Get Resource (getResourceById)  # noqa: E501

Fetch the Resource object based on the provided Resource Id. Resource is a heavyweight object that includes main information about the Resource and also data.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TbResource
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_resource_by_id(resource_id=deserialize_param(resource_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_resource_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_resource_info_by_id(resource_id_json: str) -> str:
    """
    Get Resource Info (getResourceInfoById)  # noqa: E501

Fetch the Resource Info object based on the provided Resource Id. Resource Info is a lightweight object that includes main information about the Resource excluding the heavyweight data.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_id: A string value representing the resource id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_resource_info_by_id(resource_id=deserialize_param(resource_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_resource_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_resources(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Resource Infos (getResources)  # noqa: E501

Returns a page of Resource Info objects owned by tenant or sysadmin. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. Resource Info is a lightweight object that includes main information about the Resource excluding the heavyweight data.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str resource_type: A string value representing the resource type.
:param str text_search: The case insensitive 'substring' filter based on the resource title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_resources(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_resources'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_role_by_id(role_id_json: str) -> str:
    """
    Get Role by Id (getRoleById)  # noqa: E501

Fetch the Role object based on the provided Role Id. Role Contains a set of permissions. Role has two types. Generic Role may be assigned to the user group and will provide permissions for all entities of a certain type. Group Role may be assigned to both user and entity group and will provides permissions only for the entities that belong to specified entity group. The assignment of the Role to the User Group is done using [Group Permission Controller](/swagger-ui.html#/group-permission-controller). Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str role_id: A string value representing the role id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Role
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_role_by_id(role_id=deserialize_param(role_id_json, 'RoleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_role_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_roles(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Roles (getRoles)  # noqa: E501

Returns a page of roles that are available for the current user. Role Contains a set of permissions. Role has two types. Generic Role may be assigned to the user group and will provide permissions for all entities of a certain type. Group Role may be assigned to both user and entity group and will provides permissions only for the entities that belong to specified entity group. The assignment of the Role to the User Group is done using [Group Permission Controller](/swagger-ui.html#/group-permission-controller).You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Type of the role
:param str text_search: The case insensitive 'substring' filter based on the role name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataRole
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_roles(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_roles'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_roles_by_ids(role_ids_json: str) -> str:
    """
    Get Roles By Ids (getRolesByIds)  # noqa: E501

Returns the list of rows based on their ids.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str role_ids: A list of role ids, separated by comma ',' (required)
:return: list[Role]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_roles_by_ids(role_ids=json.loads(role_ids_json) if role_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_roles_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chain_by_id(rule_chain_id_json: str) -> str:
    """
    Get Rule Chain (getRuleChainById)  # noqa: E501

Fetch the Rule Chain object based on the provided Rule Chain Id. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chain_by_id(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chain_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chain_meta_data(rule_chain_id_json: str) -> str:
    """
    Get Rule Chain (getRuleChainById)  # noqa: E501

Fetch the Rule Chain Metadata object based on the provided Rule Chain Id. The metadata object contains information about the rule nodes and their connections.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChainMetaData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chain_meta_data(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chain_meta_data'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chain_output_labels(rule_chain_id_json: str) -> str:
    """
    Get Rule Chain output labels (getRuleChainOutputLabels)  # noqa: E501

Fetch the unique labels for the "output" Rule Nodes that belong to the Rule Chain based on the provided Rule Chain Id. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chain_output_labels(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chain_output_labels'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chain_output_labels_usage(rule_chain_id_json: str) -> str:
    """
    Get output labels usage (getRuleChainOutputLabelsUsage)  # noqa: E501

Fetch the list of rule chains and the relation types (labels) they use to process output of the current rule chain based on the provided Rule Chain Id. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[RuleChainOutputLabelsUsage]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chain_output_labels_usage(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chain_output_labels_usage'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chains(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Rule Chains (getRuleChains)  # noqa: E501

Returns a page of Rule Chains owned by tenant. The rule chain object is lightweight and contains general information about the rule chain. List of rule nodes and their connection is stored in a separate 'metadata' object.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Rule chain type (CORE or EDGE)
:param str text_search: The case insensitive 'substring' filter based on the rule chain name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataRuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chains(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_rule_chains_by_ids(rule_chain_ids_json: str) -> str:
    """
    Get Rule Chains By Ids (getRuleChainsByIds)  # noqa: E501

Requested rule chains must be owned by tenant which is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_ids: A list of rule chain ids, separated by comma ',' (required)
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_rule_chains_by_ids(rule_chain_ids=json.loads(rule_chain_ids_json) if rule_chain_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_rule_chains_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_scheduler_event_by_id(scheduler_event_id_json: str) -> str:
    """
    Get Scheduler Event (getSchedulerEventById)  # noqa: E501

Fetch the SchedulerEvent object based on the provided scheduler event Id. Scheduler Event extends Scheduler Event Info object and adds 'configuration' - a JSON structure of scheduler event configuration. See the 'Model' tab of the Response Class for more details. Referencing non-existing Scheduler Event Id will cause 'Not Found' error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: SchedulerEvent
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_scheduler_event_by_id(scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_scheduler_event_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_scheduler_event_info_by_id(scheduler_event_id_json: str) -> str:
    """
    Get Scheduler Event With Customer Info (getSchedulerEventInfoById)  # noqa: E501

Fetch the SchedulerEventWithCustomerInfo object based on the provided scheduler event Id. Scheduler Event With Customer Info extends Scheduler Event Info object and adds 'customerTitle' - a String value representing the title of the customer which user created a Scheduler Event and 'customerIsPublic' - a boolean parameter that specifies if customer is public. See the 'Model' tab of the Response Class for more details. Referencing non-existing Scheduler Event Id will cause 'Not Found' error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: SchedulerEventWithCustomerInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_scheduler_event_info_by_id(scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_scheduler_event_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_scheduler_events(type: Optional[str] = None) -> str:
    """
    Get Scheduler Events By Type (getSchedulerEvents)  # noqa: E501

Requested scheduler events must be owned by tenant or assigned to customer which user is performing the request. Scheduler Event With Customer Info extends Scheduler Event Info object and adds 'customerTitle' - a String value representing the title of the customer which user created a Scheduler Event and 'customerIsPublic' - a boolean parameter that specifies if customer is public. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: A string value representing the scheduler type. For example, 'generateReport'
:return: list[SchedulerEventWithCustomerInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_scheduler_events(type=type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_scheduler_events'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_scheduler_events_by_ids(scheduler_event_ids_json: str) -> str:
    """
    Get Scheduler Events By Ids (getSchedulerEventsByIds)  # noqa: E501

Requested scheduler events must be owned by tenant or assigned to customer which user is performing the request. Scheduler Events allows you to schedule various types of events with flexible schedule configuration. Scheduler fires configured scheduler events according to their schedule. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str scheduler_event_ids: A list of scheduler event ids, separated by comma ',' (required)
:return: list[SchedulerEventInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_scheduler_events_by_ids(scheduler_event_ids=json.loads(scheduler_event_ids_json) if scheduler_event_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_scheduler_events_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_secret_info_by_id(secret_id_json: str) -> str:
    """
    Get Secret info by Id (getSecretInfoById)  # noqa: E501

  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: SecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_secret_info_by_id(secret_id=deserialize_param(secret_id_json, 'SecretId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_secret_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_secret_info_by_name(name: str) -> str:
    """
    Get Secret info by name (getSecretInfoByName)  # noqa: E501

  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str name: (required)
:return: SecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_secret_info_by_name(name=name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_secret_info_by_name'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_secret_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Secret infos (getSecretInfos)  # noqa: E501

Returns a page of secret infos owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the secret name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataSecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_secret_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_secret_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_secret_names() -> str:
    """
    Get Tenant Secret names (getSecretNames)  # noqa: E501

Returns a page of secret names owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_secret_names()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_secret_names'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_security_settings() -> str:
    """
    Get the Security Settings object  # noqa: E501

Get the Security Settings object that contains password policy, etc.  Available for users with 'SYS_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: SecuritySettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_security_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_security_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_self_registration_params() -> str:
    """
    Get Self Registration parameters (getSelfRegistrationParams)  # noqa: E501

Fetch the Self Registration parameters object for the tenant of the current user.   Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: SelfRegistrationParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_self_registration_params()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_server_time() -> str:
    """
    Get server time (getServerTime)  # noqa: E501

Get the server time (milliseconds since January 1, 1970 UTC). Used to adjust view of the dashboards according to the difference between browser and server time.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_server_time()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_server_time'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_shared_entity_group_entity_infos_by_type_and_page_link(group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Shared Entity Group Entity Infos by entity type and page link (getSharedEntityGroupEntityInfosByTypeAndPageLink)  # noqa: E501

Returns a page of Shared Entity Group Entity Info objects based on the provided Entity Type and Page Link. Entity Info is a lightweight object that contains only id and name of the entity group. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_shared_entity_group_entity_infos_by_type_and_page_link(group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_shared_entity_group_entity_infos_by_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_shared_entity_groups_by_type(group_type: str) -> str:
    """
    Get Shared Entity Groups by entity type (getSharedEntityGroupsByType)  # noqa: E501

Fetch the list of Shared Entity Group Info objects based on the provided Entity Type. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:return: list[EntityGroupInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_shared_entity_groups_by_type(group_type=group_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_shared_entity_groups_by_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_shared_entity_groups_by_type_and_page_link(group_type: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Shared Entity Groups by entity type and page link (getSharedEntityGroupsByTypeAndPageLink)  # noqa: E501

Returns a page of Shared Entity Group Info objects based on the provided Entity Type and Page Link. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.Entity Group Info extends Entity Group object and adds 'ownerIds' - a list of owner ids.You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str group_type: Entity Group type (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'startsWith' filter based on the entity group name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_shared_entity_groups_by_type_and_page_link(group_type=group_type, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_shared_entity_groups_by_type_and_page_link'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_short_customer_info_by_id(customer_id_json: str) -> str:
    """
    Get short Customer info (getShortCustomerInfoById)  # noqa: E501

Get the short customer object that contains only the title and 'isPublic' flag. If the user has the authority of 'Tenant Administrator', the server checks that the customer is owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the user belongs to the customer.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_short_customer_info_by_id(customer_id=deserialize_param(customer_id_json, 'CustomerId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_short_customer_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_sign_up_self_registration_params(pkg_name: Optional[str] = None) -> str:
    """
    Get Self Registration form parameters without authentication (getSignUpSelfRegistrationParams)  # noqa: E501

Fetch the Self Registration parameters based on the domain name from the request. Available for non-authorized users. Contains the information to customize the sign-up form.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str pkg_name: pkgName
:return: SignUpSelfRegistrationParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_sign_up_self_registration_params(pkg_name=pkg_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_sign_up_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_software(device_token: str, title: str, version: str, size: Optional[str] = None, chunk: Optional[str] = None) -> str:
    """
    Get Device Software (getSoftware)  # noqa: E501

Downloads the current software package.When the platform initiates software update, it informs the device by updating the 'sw_title', 'sw_version', 'sw_checksum' and 'sw_checksum_algorithm' shared attributes.The 'sw_title' and 'sw_version' parameters must be supplied in this request to double-check that the software that device is downloading matches the software it expects to download. This is important, since the administrator may change the software assignment while device is downloading the software.   Optional 'chunk' and 'size' parameters may be used to download the software in chunks. For example, device may request first 16 KB of software using 'chunk'=0 and 'size'=16384. Next 16KB using 'chunk'=1 and 'size'=16384. The last chunk should have less bytes then requested using 'size' parameter.   The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str title: Title of the software, corresponds to the value of 'sw_title' attribute. (required)
:param str version: Version of the software, corresponds to the value of 'sw_version' attribute. (required)
:param int size: Size of the chunk. Optional. Omit to download the entire file without using  chunks.
:param int chunk: Index of the chunk. Optional. Omit to download the entire file without using chunks.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_software(device_token=device_token, title=title, version=version, size=size, chunk=chunk)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_software'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_solution_template_details(solution_template_id: str) -> str:
    """
    Get Solution template details (getSolutionTemplateDetails)  # noqa: E501

Get a solution template details based on the provided id   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str solution_template_id: A string value representing the solution template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TenantSolutionTemplateDetails
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_solution_template_details(solution_template_id=solution_template_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_solution_template_details'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_solution_template_infos() -> str:
    """
    Get Solution templates (getSolutionTemplateInfos)  # noqa: E501

Get a list of solution template descriptors   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[TenantSolutionTemplateInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_solution_template_infos()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_solution_template_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_solution_template_instructions(solution_template_id: str) -> str:
    """
    Get Solution Template Instructions (getSolutionTemplateInstructions)  # noqa: E501

Get a solution template instructions based on the provided id   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str solution_template_id: A string value representing the solution template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TenantSolutionTemplateInstructions
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_solution_template_instructions(solution_template_id=solution_template_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_solution_template_instructions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_system_info() -> str:
    """
    Get system info (getSystemInfo)  # noqa: E501

Get main information about system.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: SystemInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_system_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_system_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_system_or_tenant_resources_by_ids(resource_ids_json: str) -> str:
    """
    Get Resource Infos by ids (getSystemOrTenantResourcesByIds)  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str resource_ids: A list of resource ids, separated by comma ',' (required)
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_system_or_tenant_resources_by_ids(resource_ids=json.loads(resource_ids_json) if resource_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_system_or_tenant_resources_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_admins(tenant_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Users (getTenantAdmins)  # noqa: E501

Returns a page of users owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_admins(tenant_id=deserialize_param(tenant_id_json, 'TenantId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_admins'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_asset(asset_name: str) -> str:
    """
    Get Tenant Asset (getTenantAsset)  # noqa: E501

Requested asset must be owned by tenant that the user belongs to. Asset name is an unique property of asset. So it can be used to identify the asset.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_name: A string value representing the Asset name. (required)
:return: Asset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_asset(asset_name=asset_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_asset'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_assets(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Assets (getTenantAssets)  # noqa: E501

Returns a page of assets owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Asset type
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAsset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_assets(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_assets'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_by_id(tenant_id_json: str) -> str:
    """
    Get Tenant (getTenantById)  # noqa: E501

Fetch the Tenant object based on the provided Tenant Id.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Tenant
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_by_id(tenant_id=deserialize_param(tenant_id_json, 'TenantId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_customer(customer_title: str) -> str:
    """
    Get Tenant Customer by Customer title (getTenantCustomer)  # noqa: E501

Get the Customer using Customer Title.   Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str customer_title: A string value representing the Customer title. (required)
:return: Customer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_customer(customer_title=customer_title)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_customer'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_dashboards(page_size: int, page: int, mobile: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Dashboards (getTenantDashboards)  # noqa: E501

Returns a page of dashboard info objects owned by the tenant of a current user. The Dashboard Info object contains lightweight information about the dashboard (e.g. title, image, assigned customers) but does not contain the heavyweight configuration JSON. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool mobile: Exclude dashboards that are hidden for mobile
:param str text_search: The case insensitive 'substring' filter based on the dashboard title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_dashboards(page_size=page_size, page=page, mobile=mobile, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_dashboards_v1(tenant_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_tenant_dashboards_v1(tenant_id=deserialize_param(tenant_id_json, 'TenantId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_dashboards_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_device(device_name: str) -> str:
    """
    Get Tenant Device (getTenantDevice)  # noqa: E501

Requested device must be owned by tenant that the user belongs to. Device name is an unique property of device. So it can be used to identify the device.  Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_name: A string value representing the Device name. (required)
:return: Device
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_device(device_name=device_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_devices(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Devices (getTenantDevices)  # noqa: E501

Returns a page of devices owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Device type as the name of the device profile
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDevice
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_devices(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_devices'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_domain_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Domain infos (getTenantDomainInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on domain's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDomainInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_domain_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_domain_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_edge(edge_name: str) -> str:
    """
    Get Tenant Edge (getTenantEdge)  # noqa: E501

Requested edge must be owned by tenant or customer that the user belongs to. Edge name is an unique property of edge. So it can be used to identify the edge.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_name: Unique name of the edge (required)
:return: Edge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_edge(edge_name=edge_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_edges(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Edges (getTenantEdges)  # noqa: E501

Returns a page of edges owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: A string value representing the edge type. For example, 'default'
:param str text_search: The case insensitive 'substring' filter based on the edge name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEdge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_edges(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_edges'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_entity_view(entity_view_name: str) -> str:
    """
    Get Entity View by name (getTenantEntityView)  # noqa: E501

Fetch the Entity View object based on the tenant id and entity view name.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_view_name: Entity View name (required)
:return: EntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_entity_view(entity_view_name=entity_view_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_entity_view'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_entity_views(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Entity Views (getTenantEntityViews)  # noqa: E501

Returns a page of entity views owned by tenant. Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type:   ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_entity_views(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_entity_views'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_home_dashboard_info() -> str:
    """
    Get Tenant Home Dashboard Info (getTenantHomeDashboardInfo)  # noqa: E501

Returns the home dashboard info object that is configured as 'homeDashboardId' parameter in the 'additionalInfo' of the corresponding tenant.   Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: HomeDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_home_dashboard_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_home_dashboard_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_info_by_id(tenant_id_json: str) -> str:
    """
    Get Tenant Info (getTenantInfoById)  # noqa: E501

Fetch the Tenant Info object based on the provided Tenant Id. The Tenant Info object extends regular Tenant object and includes Tenant Profile name.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_id: A string value representing the tenant id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TenantInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_info_by_id(tenant_id=deserialize_param(tenant_id_json, 'TenantId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenants Info (getTenants)  # noqa: E501

Returns a page of tenant info objects registered in the platform. The Tenant Info object extends regular Tenant object and includes Tenant Profile name. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the tenant name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTenantInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_mobile_app_bundle_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get mobile app bundle infos (getTenantMobileAppBundleInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on app's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataMobileAppBundleInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_mobile_app_bundle_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_mobile_app_bundle_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_mobile_app_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get mobile app infos (getTenantMobileAppInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: Case-insensitive 'substring' filter based on app's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataMobileAppInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_mobile_app_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_mobile_app_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_mobile_apps(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get mobile app infos (getTenantMobileAppInfos)  # noqa: E501

  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str platform_type: Platform type: ANDROID or IOS
:param str text_search: Case-insensitive 'substring' filter based on app's name
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataMobileApp
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_mobile_apps(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_mobile_apps'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profile_by_id(tenant_profile_id_json: str) -> str:
    """
    Get Tenant Profile (getTenantProfileById)  # noqa: E501

Fetch the Tenant Profile object based on the provided Tenant Profile Id.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_profile_id: A string value representing the tenant profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TenantProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profile_by_id(tenant_profile_id=deserialize_param(tenant_profile_id_json, 'TenantProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profile_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profile_data() -> str:
    """
    getTenantProfileData  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: TenantProfileData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profile_data()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profile_data'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profile_data_by_id(tenant_profile_id_json: str) -> str:
    """
    getTenantProfileDataById  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_profile_id: tenantProfileId (required)
:return: TenantProfileData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profile_data_by_id(tenant_profile_id=deserialize_param(tenant_profile_id_json, 'TenantProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profile_data_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profile_info_by_id(tenant_profile_id_json: str) -> str:
    """
    Get Tenant Profile Info (getTenantProfileInfoById)  # noqa: E501

Fetch the Tenant Profile Info object based on the provided Tenant Profile Id. Tenant Profile Info is a lightweight object that contains only id and name of the profile.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_profile_id: A string value representing the tenant profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profile_info_by_id(tenant_profile_id=deserialize_param(tenant_profile_id_json, 'TenantProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profile_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profile_infos(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Profiles Info (getTenantProfileInfos)  # noqa: E501

Returns a page of tenant profile info objects registered in the platform. Tenant Profile Info is a lightweight object that contains only id and name of the profile. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the tenant profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profile_infos(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profile_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profiles(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenant Profiles (getTenantProfiles)  # noqa: E501

Returns a page of tenant profiles registered in the platform. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the tenant profile name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTenantProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profiles(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profiles'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_profiles_by_ids(ids_json: str) -> str:
    """
    getTenantProfilesByIds  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ids: ids (required)
:return: list[TenantProfile]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_profiles_by_ids(ids=json.loads(ids_json) if ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_profiles_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_queue_stats(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Queue Stats entities (getTenantQueueStats)  # noqa: E501

Returns a page of queue stats objects that are designed to collect queue statistics for every service. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the queue name or service id.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataQueueStats
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_queue_stats(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_queue_stats'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_queues_by_service_type(service_type: str, page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Queues (getTenantQueuesByServiceType)  # noqa: E501

Returns a page of queues registered in the platform. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str service_type: Service type (implemented only for the TB-RULE-ENGINE) (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the queue name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataQueue
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_queues_by_service_type(service_type=service_type, page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_queues_by_service_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_resources(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get All Resource Infos (getAllResources)  # noqa: E501

Returns a page of Resource Info objects owned by tenant. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. Resource Info is a lightweight object that includes main information about the Resource excluding the heavyweight data.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the resource title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_resources(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_resources'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_subscription_usage() -> str:
    """
    getTenantSubscriptionUsage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: SubscriptionUsage
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_subscription_usage()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_subscription_usage'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenant_usage_info() -> str:
    """
    getTenantUsageInfo  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: UsageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenant_usage_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenant_usage_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenants(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Tenants (getTenants)  # noqa: E501

Returns a page of tenants registered in the platform. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the tenant name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataTenant
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenants(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenants'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_tenants_by_ids(tenant_ids_json: str) -> str:
    """
    getTenantsByIds  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_ids: tenantIds (required)
:return: list[Tenant]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_tenants_by_ids(tenant_ids=json.loads(tenant_ids_json) if tenant_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_tenants_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_terms_of_use() -> str:
    """
    Get Terms of Use for Self Registration form (getTermsOfUse)  # noqa: E501

Fetch the Terms of Use based on the domain name from the request. Available for non-authorized users.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_terms_of_use()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_terms_of_use'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_timeseries(entity_id_json: str, keys: str, start_ts: int, end_ts: int, interval_type: Optional[str] = None, interval: Optional[str] = None, time_zone: Optional[str] = None, limit: Optional[str] = None, agg: Optional[str] = None, order_by: Optional[str] = None, use_strict_data_types: Optional[str] = None) -> str:
    """
    Get time-series data (getTimeseries)  # noqa: E501

Returns a range of time-series values for specified entity. Returns not aggregated data by default. Use aggregation function ('agg') and aggregation interval ('interval') to enable aggregation of the results on the database / server side. The aggregation is generally more efficient then fetching all records.   ```json {   "temperature": [     {       "value": 36.7,       "ts": 1609459200000     },     {       "value": 36.6,       "ts": 1609459201000     }   ] } ```  Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str keys: A string value representing the comma-separated list of telemetry keys. (required)
:param int start_ts: A long value representing the start timestamp of the time range in milliseconds, UTC. (required)
:param int end_ts: A long value representing the end timestamp of the time range in milliseconds, UTC. (required)
:param str interval_type: A string value representing the type fo the interval.
:param int interval: A long value representing the aggregation interval range in milliseconds.
:param str time_zone: A string value representing the timezone that will be used to calculate exact timestamps for 'WEEK', 'WEEK_ISO', 'MONTH' and 'QUARTER' interval types.
:param int limit: An integer value that represents a max number of timeseries data points to fetch. This parameter is used only in the case if 'agg' parameter is set to 'NONE'.
:param str agg: A string value representing the aggregation function. If the interval is not specified, 'agg' parameter will use 'NONE' value.
:param str order_by: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param bool use_strict_data_types: Enables/disables conversion of telemetry values to strings. Conversion is enabled by default. Set parameter to 'true' in order to disable the conversion.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_timeseries(entity_id=deserialize_param(entity_id_json, 'EntityId'), keys=keys, start_ts=start_ts, end_ts=end_ts, interval_type=interval_type, interval=interval, time_zone=time_zone, limit=limit, agg=agg, order_by=order_by, use_strict_data_types=use_strict_data_types)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_timeseries'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_timeseries_keys(device_profile_id: Optional[str] = None) -> str:
    """
    Get time-series keys (getTimeseriesKeys)  # noqa: E501

Get a set of unique time-series keys used by devices that belong to specified profile. If profile is not set returns a list of unique keys among all profiles. The call is used for auto-complete in the UI forms. The implementation limits the number of devices that participate in search to 100 as a trade of between accurate results and time-consuming queries.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_timeseries_keys(device_profile_id=device_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_timeseries_keys'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_timeseries_keys_v1(entity_id_json: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_timeseries_keys_v1(entity_id=deserialize_param(entity_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_timeseries_keys_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_translation_for_basic_edit(locale_code: str) -> str:
    """
    Get end-user multi-translation for basic edit (getTranslationForBasicEdit)  # noqa: E501

Fetch the translation info map where value is info object containing key translation, origin translation, translation of parent level, translation status.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: (required)
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_translation_for_basic_edit(locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_translation_for_basic_edit'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_translation_infos() -> str:
    """
    Get Translation info (getTranslationInfos)  # noqa: E501

Fetch the list of customized locales and corresponding details such as language display name, country display name and translation progress percentage.   Response example:   ```json [   {     "localeCode": "uk_UA",     "language": "Ukrainian (українська)",     "country": "Україна",     "progress": 32   },   {     "localeCode": "es_ES",     "language": "Spanish (español)",     "country": "España",     "progress": 79   }] ```  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[TranslationInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_translation_infos()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_translation_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_trendz_settings() -> str:
    """
    Get Trendz Settings (getTrendzSettings)  # noqa: E501

Retrieves Trendz settings for this tenant.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: TrendzSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_trendz_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_trendz_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_unread_notifications_count(delivery_method: Optional[str] = None) -> str:
    """
    Get unread notifications count (getUnreadNotificationsCount)  # noqa: E501

Returns unread notifications count for chosen delivery method.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str delivery_method: Delivery method
:return: int
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_unread_notifications_count(delivery_method=delivery_method)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_unread_notifications_count'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_uplink_converter(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get uplink converter (getUplinkConverter)  # noqa: E501

Returns uplink converter body for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_uplink_converter(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_uplink_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_uplink_converter_metadata(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get uplink converter metadata (getUplinkConverterMetadata)  # noqa: E501

Returns uplink converter metadata for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_uplink_converter_metadata(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_uplink_converter_metadata'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_uplink_payload(integration_type: str, vendor_name: str, model: str) -> str:
    """
    Get uplink payload (getUplinkPayload)  # noqa: E501

Returns payload example for the uplink converter for the vendor, integration type and model  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str model: (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_uplink_payload(integration_type=integration_type, vendor_name=vendor_name, model=model)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_uplink_payload'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user() -> str:
    """
    Get current User (getUser)  # noqa: E501

Get the information about the User which credentials are used to perform this REST API call.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: User
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_assets(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, asset_profile_id: Optional[str] = None) -> str:
    """
    Get Assets (getUserAssets)  # noqa: E501

Returns a page of assets objects available for the current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. Asset Info is an extension of the default Asset object that contains information about the owner name.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Asset type
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:param str text_search: The case insensitive 'substring' filter based on the asset name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataAsset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_assets(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order, asset_profile_id=asset_profile_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_assets'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_by_id(user_id_json: str) -> str:
    """
    Get User (getUserById)  # noqa: E501

Fetch the User object based on the provided User Id. If the user has the authority of 'SYS_ADMIN', the server does not perform additional checks. If the user has the authority of 'TENANT_ADMIN', the server checks that the requested user is owned by the same tenant. If the user has the authority of 'CUSTOMER_USER', the server checks that the requested user is owned by the same customer.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: User
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_by_id(user_id=deserialize_param(user_id_json, 'UserId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_customers(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Customers (getUserCustomers)  # noqa: E501

Returns a page of customers available for the user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the customer title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataCustomer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_customers(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_customers'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_dashboards(page_size: int, page: int, mobile: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, operation: Optional[str] = None, user_id: Optional[str] = None) -> str:
    """
    Get Dashboards (getUserDashboards)  # noqa: E501

Returns a page of Dashboard Info objects available for specified or current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details. The Dashboard Info object contains lightweight information about the dashboard (e.g. title, image, assigned customers) but does not contain the heavyweight configuration JSON.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param bool mobile: Exclude dashboards that are hidden for mobile
:param str text_search: The case insensitive 'substring' filter based on the dashboard title.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param str operation: Filter by allowed operations for the current user
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: PageDataDashboardInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_dashboards(page_size=page_size, page=page, mobile=mobile, text_search=text_search, sort_property=sort_property, sort_order=sort_order, operation=operation, user_id=user_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_dashboards_info() -> str:
    """
    Get information about last visited and starred dashboards (getLastVisitedDashboards)  # noqa: E501

Fetch the list of last visited and starred dashboards. Both lists are limited to 10 items.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: UserDashboardsInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_dashboards_info()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_dashboards_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_devices(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Devices (getUserDevices)  # noqa: E501

Returns a page of devices that are available for the current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: Device type as the name of the device profile
:param str text_search: The case insensitive 'substring' filter based on the device name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataDevice
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_devices(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_devices'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_edges(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Edges (getUserEdges)  # noqa: E501

Returns a page of edges available for current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type: A string value representing the edge type. For example, 'default'
:param str text_search: The case insensitive 'substring' filter based on the edge name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEdge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_edges(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_edges'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_entity_views(page_size: int, page: int, type: Optional[str] = None, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Entity Views (getUserEntityViews)  # noqa: E501

Returns a page of entity views that are available for the current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str type:   ## Entity View Filter  Allows to filter entity views based on their type and the **'starts with'** expression over their name. For example, this entity filter selects all 'Concrete Mixer' entity views which name starts with 'CAT':  ```json {   "type": "entityViewType",   "entityViewType": "Concrete Mixer",   "entityViewNameFilter": "CAT" } ```
:param str text_search: The case insensitive 'substring' filter based on the entity view name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataEntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_entity_views(page_size=page_size, page=page, type=type, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_entity_views'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_group_permissions(user_group_id_json: str) -> str:
    """
    Get group permissions by User Group Id (getUserGroupPermissions)  # noqa: E501

Returns a list of group permission objects that belongs to specified User Group Id. Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Group Permission Info object extends the Group Permissions with the full information about Role and User and/or Entity Groups.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: list[GroupPermissionInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_group_permissions(user_group_id=deserialize_param(user_group_id_json, 'EntityId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_group_permissions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_info_by_id(user_id_json: str) -> str:
    """
    Get User info (getUserInfoById)  # noqa: E501

Fetch the User info object based on the provided User Id. If the user has the authority of 'SYS_ADMIN', the server does not perform additional checks. If the user has the authority of 'TENANT_ADMIN', the server checks that the requested user is owned by the same tenant. If the user has the authority of 'CUSTOMER_USER', the server checks that the requested user is owned by the same customer.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: UserInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_info_by_id(user_id=deserialize_param(user_id_json, 'UserId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_mobile_info(pkg_name: str, platform: str) -> str:
    """
    Get user mobile app basic info (getUserMobileInfo)  # noqa: E501

  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str pkg_name: Mobile application package name (required)
:param str platform: Platform type (required)
:return: UserMobileInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_mobile_info(pkg_name=pkg_name, platform=platform)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_mobile_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_notification_settings() -> str:
    """
    getUserNotificationSettings  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: UserNotificationSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_notification_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_notification_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_password_policy() -> str:
    """
    Get the current User password policy (getUserPasswordPolicy)  # noqa: E501

API call to get the password policy for the password validation form(s).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: UserPasswordPolicy
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_password_policy()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_password_policy'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_settings() -> str:
    """
    Get user settings (getUserSettings)  # noqa: E501

Fetch the User settings based on authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_settings()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_token_by_mobile_secret(secret: str) -> str:
    """
    Get User Token (getUserTokenByMobileSecret)  # noqa: E501

Returns the token of the User based on the provided secret key.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str secret: A string value representing short-lived secret key (required)
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_token_by_mobile_secret(secret=secret)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_token_by_mobile_secret'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_user_users(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get Users (getUsers)  # noqa: E501

Returns a page of user objects available for the current user. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_user_users(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_user_users'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_users_by_entity_group_id(entity_group_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get users by Entity Group Id (getUsersByEntityGroupId)  # noqa: E501

Returns a page of user objects that belongs to specified Entity Group Id. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.    Security check is performed to verify that the user has 'READ' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUser
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_users_by_entity_group_id(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_users_by_entity_group_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_users_by_ids(user_ids_json: str) -> str:
    """
    Get Users By Ids (getUsersByIds)  # noqa: E501

Requested users must be owned by tenant or assigned to customer which user is performing the request.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_ids: A list of user ids, separated by comma ',' (required)
:return: list[User]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_users_by_ids(user_ids=json.loads(user_ids_json) if user_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_users_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_users_for_assign(alarm_id_json: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    Get usersForAssign (getUsersForAssign)  # noqa: E501

Returns page of user data objects that can be assigned to provided alarmId. Search is been executed by email, firstName and lastName fields. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the user email.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: PageDataUserEmailInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_users_for_assign(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'), page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_users_for_assign'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_vendor_models(integration_type: str, vendor_name: str, converter_type: str) -> str:
    """
    Get vendor models (getVendorModels)  # noqa: E501

Returns a list of models for the vendor, integration type and converter type  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:param str vendor_name: (required)
:param str converter_type:
:return: list[Model]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_vendor_models(integration_type=integration_type, vendor_name=vendor_name, converter_type=converter_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_vendor_models'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_vendors(integration_type: str) -> str:
    """
    Get vendors (getVendors)  # noqa: E501

Returns a list of vendors for the integration type  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str integration_type: (required)
:return: list[Vendor]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_vendors(integration_type=integration_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_vendors'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_version_create_request_status(request_id: str) -> str:
    """
    Get version create request status (getVersionCreateRequestStatus)  # noqa: E501

Returns the status of previously made version create request.   This status contains following properties: - `done` - whether request processing is finished; - `version` - created version info: timestamp, version id (commit hash), commit name and commit author; - `added` - count of items that were created in the remote repo; - `modified` - modified items count; - `removed` - removed items count; - `error` - error message, if an error occurred while handling the request.  An example of successful status: ```json {   "done": true,   "added": 10,   "modified": 2,   "removed": 5,   "version": {     "timestamp": 1655198528000,     "id":"8a834dd389ed80e0759ba8ee338b3f1fd160a114",     "name": "My devices v2.0",     "author": "John Doe"   },   "error": null } ```  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str request_id: A string value representing the version control request id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: VersionCreationResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_version_create_request_status(request_id=request_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_version_create_request_status'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_version_load_request_status(request_id: str) -> str:
    """
    Get version load request status (getVersionLoadRequestStatus)  # noqa: E501

Returns the status of previously made version load request. The structure contains following parameters: - `done` - if the request was successfully processed; - `result` - a list of load results for each entity type:      - `created` - created entities count;      - `updated` - updated entities count;      - `deleted` - removed entities count. - `error` - if an error occurred during processing, error info:      - `type` - error type;      - `source` - an external id of remote entity;      - `target` - if failed to find referenced entity by external id - this external id;      - `message` - error message.  An example of successfully processed request status: ```json {   "done": true,   "result": [     {       "entityType": "DEVICE",       "created": 10,       "updated": 5,       "deleted": 5     },      {       "entityType": "ASSET",       "created": 4,       "updated": 0,       "deleted": 8     }   ] } ```  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str request_id: A string value representing the version control request id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: VersionLoadResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_version_load_request_status(request_id=request_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_version_load_request_status'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_web_self_registration_params() -> str:
    """
    Get Self Registration parameters (getSelfRegistrationParams)  # noqa: E501

Fetch the Self Registration parameters object for the tenant of the current user.   Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[str]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_web_self_registration_params()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_web_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_white_label_params(logo_image_checksum: str, favicon_checksum: str) -> str:
    """
    Get White Labeling parameters  # noqa: E501

Returns white-labeling parameters for the current user.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: WhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_white_label_params(logo_image_checksum=logo_image_checksum, favicon_checksum=favicon_checksum)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widget_type(fqn: str) -> str:
    """
    Get Widget Type (getWidgetType)  # noqa: E501

Get the Widget Type by FQN. Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str fqn: Widget Type fqn (required)
:return: WidgetType
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widget_type(fqn=fqn)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widget_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widget_type_by_bundle_alias_and_type_alias(is_system: bool, bundle_alias: str, alias: str) -> str:
    """
    Get Widget Type (getWidgetTypeByBundleAliasAndTypeAlias) (Deprecated)  # noqa: E501

Get the Widget Type based on the provided parameters. Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param bool is_system: System or Tenant (required)
:param str bundle_alias: Widget Bundle alias (required)
:param str alias: Widget Type alias (required)
:return: WidgetType
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widget_type_by_bundle_alias_and_type_alias(is_system=is_system, bundle_alias=bundle_alias, alias=alias)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widget_type_by_bundle_alias_and_type_alias'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widget_type_by_id(widget_type_id_json: str, inline_images: Optional[str] = None) -> str:
    """
    Get Widget Type Details (getWidgetTypeById)  # noqa: E501

Get the Widget Type Details based on the provided Widget Type Id. Widget Type Details extend Widget Type and add image and description properties. Those properties are useful to edit the Widget Type but they are not required for Dashboard rendering.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widget_type_id: A string value representing the widget type id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: WidgetTypeDetails
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widget_type_by_id(widget_type_id=deserialize_param(widget_type_id_json, 'WidgetTypeId'), inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widget_type_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widget_type_info_by_id(widget_type_id_json: str) -> str:
    """
    Get Widget Type Info (getWidgetTypeInfoById)  # noqa: E501

Get the Widget Type Info based on the provided Widget Type Id. Widget Type Details extend Widget Type and add image and description properties. Those properties are useful to edit the Widget Type but they are not required for Dashboard rendering.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widget_type_id: A string value representing the widget type id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: WidgetTypeInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widget_type_info_by_id(widget_type_id=deserialize_param(widget_type_id_json, 'WidgetTypeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widget_type_info_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widget_types(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, tenant_only: Optional[str] = None, full_search: Optional[str] = None, deprecated_filter: Optional[str] = None, widget_type_list_json: str = None) -> str:
    """
    Get Widget Types (getWidgetTypes)  # noqa: E501

Returns a page of Widget Type objects available for current user. Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the widget type name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:param bool tenant_only: Optional boolean parameter indicating whether only tenant widget types should be returned
:param bool full_search: Optional boolean parameter indicating whether search widgets by description not only by name
:param str deprecated_filter: Optional string parameter indicating whether to include deprecated widgets
:param str widget_type_list: A list of string values separated by comma ',' representing one of the widget type value
:return: PageDataWidgetTypeInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widget_types(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, tenant_only=tenant_only, full_search=full_search, deprecated_filter=deprecated_filter, widget_type_list=json.loads(widget_type_list_json) if widget_type_list_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widget_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widgets_bundle_by_id(widgets_bundle_id_json: str, inline_images: Optional[str] = None) -> str:
    """
    Get Widget Bundle (getWidgetsBundleById)  # noqa: E501

Get the Widget Bundle based on the provided Widget Bundle Id. Widget Bundle represents a group(bundle) of widgets. Widgets are grouped into bundle by type or use case.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: A string value representing the widget bundle id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool inline_images: Inline images as a data URL (Base64)
:return: WidgetsBundle
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widgets_bundle_by_id(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'), inline_images=inline_images)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widgets_bundle_by_id'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widgets_bundles() -> str:
    """
    Get all Widget Bundles (getWidgetsBundles)  # noqa: E501

Returns an array of Widget Bundle objects that are available for current user.Widget Bundle represents a group(bundle) of widgets. Widgets are grouped into bundle by type or use case.    Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: list[WidgetsBundle]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.get_widgets_bundles()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widgets_bundles'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widgets_bundles_by_ids(widget_bundle_ids_json: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_widgets_bundles_by_ids(widget_bundle_ids=json.loads(widget_bundle_ids_json) if widget_bundle_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widgets_bundles_by_ids'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def get_widgets_bundles_v1(page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None, tenant_only: Optional[str] = None, full_search: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.get_widgets_bundles_v1(page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order, tenant_only=tenant_only, full_search=full_search)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'get_widgets_bundles_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_one_way_device_rpc_request(device_id_json: str, body: Optional[str] = None) -> str:
    """
    Send one-way RPC request (handleOneWayDeviceRPCRequest)  # noqa: E501

Deprecated. See 'Rpc V 2 Controller' instead.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.handle_one_way_device_rpc_request(device_id=deserialize_param(device_id_json, 'DeviceId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_one_way_device_rpc_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_one_way_device_rpc_request_v1(device_id_json: str, body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.handle_one_way_device_rpc_request_v1(device_id=deserialize_param(device_id_json, 'DeviceId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_one_way_device_rpc_request_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_rule_engine_request(entity_id_json: str, timeout: int, body: Optional[str] = None) -> str:
    """
    Push entity message to the rule engine (handleRuleEngineRequest)  # noqa: E501

Creates the Message with type 'REST_API_REQUEST' and payload taken from the request body. Uses specified Entity Id as the Rule Engine message originator. This method allows you to extend the regular platform API with the power of Rule Engine. You may use default and custom rule nodes to handle the message. The generated message contains two important metadata fields:   * **'serviceId'** to identify the platform server that received the request;  * **'requestUUID'** to identify the request and route possible response from the Rule Engine;  Use **'rest call reply'** rule node to push the reply from rule engine back as a REST API call response. The default timeout of the request processing is 10 seconds.   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object body: (required)
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.handle_rule_engine_request(entity_id=deserialize_param(entity_id_json, 'EntityId'), timeout=timeout, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_rule_engine_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_rule_engine_request_v1(entity_id_json: str, body: Optional[str] = None, queue_name: Optional[str] = None, timeout: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.handle_rule_engine_request_v1(entity_id=deserialize_param(entity_id_json, 'EntityId'), body=body, queue_name=queue_name, timeout=timeout)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_rule_engine_request_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_rule_engine_request_v2(entity_id_json: str, body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.handle_rule_engine_request_v2(entity_id=deserialize_param(entity_id_json, 'EntityId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_rule_engine_request_v2'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_two_way_device_rpc_request(device_id_json: str, body: Optional[str] = None) -> str:
    """
    Send two-way RPC request (handleTwoWayDeviceRPCRequest)  # noqa: E501

Deprecated. See 'Rpc V 2 Controller' instead.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.handle_two_way_device_rpc_request(device_id=deserialize_param(device_id_json, 'DeviceId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_two_way_device_rpc_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def handle_two_way_device_rpc_request_v1(device_id_json: str, body: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.handle_two_way_device_rpc_request_v1(device_id=deserialize_param(device_id_json, 'DeviceId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'handle_two_way_device_rpc_request_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def http_check_status_get(routing_key: str, request_params: str, request_headers: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.http_check_status_get(routing_key=routing_key, request_params=request_params, request_headers=request_headers)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'http_check_status_get'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def http_process_request_v1_post1(routing_key: str, suffix: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.http_process_request_v1_post1(routing_key=routing_key, suffix=suffix)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'http_process_request_v1_post1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def http_process_request_v2_post2(routing_key: str, suffix: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.http_process_request_v2_post2(routing_key=routing_key, suffix=suffix)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'http_process_request_v2_post2'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def import_group_dashboards(entity_group_id_json: str, body_json: str = None, overwrite: Optional[str] = None) -> str:
    """
    Import Dashboards (importGroupDashboards)  # noqa: E501

Import the dashboards to specified group.The Dashboard object is a heavyweight object that contains information about the dashboard (e.g. title, image, assigned customers) and also configuration JSON (e.g. layouts, widgets, entity aliases).  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[Dashboard] body:
:param bool overwrite: Overwrite dashboards with the same name
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.import_group_dashboards(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), body=json.loads(body_json) if body_json else None, overwrite=overwrite)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'import_group_dashboards'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def import_image(body_json: str) -> str:
    """
    importImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ImageExportData body:
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.import_image(body=deserialize_param(body_json, 'ImageExportData'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'import_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def import_rule_chains(body: str, overwrite: Optional[str] = None) -> str:
    """
    Import Rule Chains  # noqa: E501

Imports all tenant rule chains as one JSON.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param RuleChainData body:
:param bool overwrite: Enables overwrite for existing rule chains with the same name.
:return: list[RuleChainImportResult]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.import_rule_chains(body=body, overwrite=overwrite)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'import_rule_chains'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def install_solution_template(solution_template_id: str) -> str:
    """
    Install Solution Template (installSolutionTemplate)  # noqa: E501

Install solution template based on the provided id   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str solution_template_id: A string value representing the solution template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: SolutionInstallResponse
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.install_solution_template(solution_template_id=solution_template_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'install_solution_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def isDaemon() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.isDaemon()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'isDaemon'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_alive() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.is_alive()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_alive'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_customer_white_labeling_allowed() -> str:
    """
    Check Customer White Labeling Allowed  # noqa: E501

Check if the White Labeling is enabled for the customers of the current tenant  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_customer_white_labeling_allowed()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_customer_white_labeling_allowed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_edge_upgrade_available(edge_id_json: str) -> str:
    """
    Is edge upgrade enabled (isEdgeUpgradeAvailable)  # noqa: E501

Returns 'true' if upgrade available for connected edge, 'false' - otherwise.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_edge_upgrade_available(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_edge_upgrade_available'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_edges_support_enabled() -> str:
    """
    Is edges support enabled (isEdgesSupportEnabled)  # noqa: E501

Returns 'true' if edges support enabled on server, 'false' - otherwise.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_edges_support_enabled()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_edges_support_enabled'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_edqs_api_enabled() -> str:
    """
    isEdqsApiEnabled  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: object
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_edqs_api_enabled()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_edqs_api_enabled'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_tbel_enabled() -> str:
    """
    Is TBEL script executor enabled  # noqa: E501

Returns 'True' if the TBEL script execution is enabled  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_tbel_enabled()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_tbel_enabled'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_user_token_access_enabled() -> str:
    """
    Check Token Access Enabled (isUserTokenAccessEnabled)  # noqa: E501

Checks that the system is configured to allow administrators to impersonate themself as other users. If the user who performs the request has the authority of 'SYS_ADMIN', it is possible to login as any tenant administrator. If the user who performs the request has the authority of 'TENANT_ADMIN', it is possible to login as any customer user.   Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_user_token_access_enabled()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_user_token_access_enabled'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def is_white_labeling_allowed() -> str:
    """
    Check White Labeling Allowed  # noqa: E501

Check if the White Labeling is enabled for the current user owner (tenant or customer)  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.is_white_labeling_allowed()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'is_white_labeling_allowed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def join(timeout: Optional[str] = None) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.join(timeout=timeout)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'join'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_all_entities_at_version(version_id: str) -> str:
    """
    List all entities at version (listAllEntitiesAtVersion)  # noqa: E501

Returns a list of all remote entities available in a specific version. Response type is the same as for listAllEntitiesAtVersion API method.  Returned entities order will be the same as in the repository.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str version_id: Version id, for example fd82625bdd7d6131cf8027b44ee967012ecaf990. Represents commit hash. (required)
:return: DeferredResultListVersionedEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_all_entities_at_version(version_id=version_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_all_entities_at_version'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_branches() -> str:
    """
    List branches (listBranches)  # noqa: E501

Lists branches available in the remote repository.   Response example:  ```json [   {     "name": "master",     "default": true   },   {     "name": "dev",     "default": false   },   {     "name": "dev-2",     "default": false   } ] ```  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: DeferredResultListBranchInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_branches()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_branches'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_entities_at_version(entity_type: str, version_id: str) -> str:
    """
    List entities at version (listEntitiesAtVersion)  # noqa: E501

Returns a list of remote entities of a specific entity type that are available at a concrete version.  Each entity item in the result has `externalId` property. Entities order will be the same as in the repository.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str version_id: Version id, for example fd82625bdd7d6131cf8027b44ee967012ecaf990. Represents commit hash. (required)
:return: DeferredResultListVersionedEntityInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_entities_at_version(entity_type=entity_type, version_id=version_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_entities_at_version'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_entity_type_versions(entity_type: str, branch: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    List entity type versions (listEntityTypeVersions)  # noqa: E501

Returns list of versions of an entity type in a branch. This is a collected list of versions that were created for entities of this type in a remote branch.  If specified branch does not exist - empty page data will be returned. The response structure is the same as for `listEntityVersions` API method.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str branch: The name of the working branch, for example 'master' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the entity version name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: DeferredResultPageDataEntityVersion
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_entity_type_versions(entity_type=entity_type, branch=branch, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_entity_type_versions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_entity_versions(entity_type: str, external_entity_uuid: str, branch: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    List entity versions (listEntityVersions)  # noqa: E501

Returns list of versions for a specific entity in a concrete branch.  You need to specify external id of an entity to list versions for. This is `externalId` property of an entity, or otherwise if not set - simply id of this entity.  If specified branch does not exist - empty page data will be returned.   Each version info item has timestamp, id, name and author. Version id can then be used to restore the version. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Response example:  ```json {   "data": [     {       "timestamp": 1655198593000,       "id": "fd82625bdd7d6131cf8027b44ee967012ecaf990",       "name": "Devices and assets - v2.0",       "author": "John Doe <johndoe@gmail.com>"     },     {       "timestamp": 1655198528000,       "id": "682adcffa9c8a2f863af6f00c4850323acbd4219",       "name": "Update my device",       "author": "John Doe <johndoe@gmail.com>"     },     {       "timestamp": 1655198280000,       "id": "d2a6087c2b30e18cc55e7cdda345a8d0dfb959a4",       "name": "Devices and assets - v1.0",       "author": "John Doe <johndoe@gmail.com>"     }   ],   "totalPages": 1,   "totalElements": 3,   "hasNext": false } ```  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str external_entity_uuid: A string value representing external entity id. This is `externalId` property of an entity, or otherwise if not set - simply id of this entity. (required)
:param str branch: The name of the working branch, for example 'master' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the entity version name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: DeferredResultPageDataEntityVersion
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_entity_versions(entity_type=entity_type, external_entity_uuid=external_entity_uuid, branch=branch, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_entity_versions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_slack_conversations(type: str, token: Optional[str] = None) -> str:
    """
    List Slack conversations (listSlackConversations)  # noqa: E501

List available Slack conversations by type.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: type (required)
:param str token: Slack bot token. If absent - system Slack settings will be used
:return: list[SlackConversation]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_slack_conversations(type=type, token=token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_slack_conversations'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def list_versions(branch: str, page_size: int, page: int, text_search: Optional[str] = None, sort_property: Optional[str] = None, sort_order: Optional[str] = None) -> str:
    """
    List all versions (listVersions)  # noqa: E501

Lists all available versions in a branch for all entity types.  If specified branch does not exist - empty page data will be returned. The response format is the same as for `listEntityVersions` API method.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str branch: The name of the working branch, for example 'master' (required)
:param int page_size: Maximum amount of entities in a one page (required)
:param int page: Sequence number of page starting from 0 (required)
:param str text_search: The case insensitive 'substring' filter based on the entity version name.
:param str sort_property: Property of entity to sort by
:param str sort_order: Sort order. ASC (ASCENDING) or DESC (DESCENDING)
:return: DeferredResultPageDataEntityVersion
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.list_versions(branch=branch, page_size=page_size, page=page, text_search=text_search, sort_property=sort_property, sort_order=sort_order)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'list_versions'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def load_entities_version(body: Optional[str] = None) -> str:
    """
    Load entities version (loadEntitiesVersion)  # noqa: E501

Loads specific version of remote entities (or single entity) by request. Supported entity types: CUSTOMER, ASSET, RULE_CHAIN, DASHBOARD, DEVICE_PROFILE, DEVICE, ENTITY_VIEW, WIDGETS_BUNDLE.  There are multiple types of request. Each of them requires branch name (`branch`) and version id (`versionId`). Request of type `SINGLE_ENTITY` is needed to restore a concrete version of a specific entity. It contains id of a remote entity (`externalEntityId`) and additional configuration (`config`): - `loadRelations` - to update relations list (in case `saveRelations` option was enabled during version creation); - `loadAttributes` - to load entity attributes (if `saveAttributes` config option was enabled); - `loadCredentials` - to update device credentials (if `saveCredentials` option was enabled during version creation).  An example of such request: ```json {   "type": "SINGLE_ENTITY",      "branch": "dev",   "versionId": "b3c28d722d328324c7c15b0b30047b0c40011cf7",      "externalEntityId": {     "entityType": "DEVICE",     "id": "b7944123-d4f4-11ec-847b-0f432358ab48"   },   "config": {     "loadRelations": false,     "loadAttributes": true,     "loadCredentials": true   } } ```  Another request type (`ENTITY_TYPE`) is needed to load specific version of the whole entity types. It contains a structure with entity types to load and configs for each entity type (`entityTypes`). For each specified entity type, the method will load all remote entities of this type that are present at the version. A config for each entity type contains the same options as in `SINGLE_ENTITY` request type, and additionally contains following options: - `removeOtherEntities` - to remove local entities that are not present on the remote - basically to    overwrite local entity type with the remote one; - `findExistingEntityByName` - when you are loading some remote entities that are not yet present at this tenant,    try to find existing entity by name and update it rather than create new.  Here is an example of the request to completely restore version of the whole device entity type: ```json {   "type": "ENTITY_TYPE",    "branch": "dev",   "versionId": "b3c28d722d328324c7c15b0b30047b0c40011cf7",    "entityTypes": {     "DEVICE": {       "removeOtherEntities": true,       "findExistingEntityByName": false,       "loadRelations": true,       "loadAttributes": true,       "loadCredentials": true     }   } } ```  The response will contain generated request UUID that is to be used to check the status of operation via `getVersionLoadRequestStatus`.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param VersionLoadRequest body:
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.load_entities_version(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'load_entities_version'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def load_user_group_permission_infos(body_json: str = None) -> str:
    """
    Load User Group Permissions (loadUserGroupPermissionInfos)  # noqa: E501

Enrich a list of group permission objects with the information about Role, User and Entity Groups. Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Group Permission Info object extends the Group Permissions with the full information about Role and User and/or Entity Groups.    Security check is performed to verify that the user has 'READ' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param list[GroupPermission] body:
:return: list[GroupPermissionInfo]
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.load_user_group_permission_infos(body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'load_user_group_permission_infos'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def make_entity_group_private(entity_group_id_json: str) -> str:
    """
    Make Entity Group Private (makeEntityGroupPrivate)  # noqa: E501

Make the entity group not available for non authorized users. Every group is private by default. This call is useful to hide the group that was previously made public.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.make_entity_group_private(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'make_entity_group_private'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def make_entity_group_public(entity_group_id_json: str) -> str:
    """
    Make Entity Group Publicly available (makeEntityGroupPublic)  # noqa: E501

Make the entity group available for non authorized users. Useful for public dashboards that will be embedded into the public websites.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.make_entity_group_public(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'make_entity_group_public'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def mark_all_notifications_as_read() -> str:
    """
    Mark all notifications as read (markAllNotificationsAsRead)  # noqa: E501

Marks all unread notifications as read.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str delivery_method: Delivery method
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.mark_all_notifications_as_read()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'mark_all_notifications_as_read'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def mark_notification_as_read(id: str) -> str:
    """
    Mark notification as read (markNotificationAsRead)  # noqa: E501

Marks notification as read by its id.  Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: id (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.mark_notification_as_read(id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'mark_notification_as_read'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def mobile_app_update_oauth2_clients(body_json: str, id: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.mobile_app_update_oauth2_clients(body=json.loads(body_json) if body_json else None, id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'mobile_app_update_oauth2_clients'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def mobile_login(pkg_name: str) -> str:
    """
    Mobile Login redirect (mobileLogin)  # noqa: E501

This method generates redirect to the special link that is handled by mobile application. Useful for email verification flow on mobile app.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str pkg_name: Mobile app package name. Used to identify the application and build the redirect link. (required)
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.mobile_login(pkg_name=pkg_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'mobile_login'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def notify_tenant_plan_changed(body: str) -> str:
    """
    notifyTenantPlanChanged  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: tenantPlanChangedRequest (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.notify_tenant_plan_changed(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'notify_tenant_plan_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def notify_tenant_state_changed(body: str) -> str:
    """
    notifyTenantStateChanged  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: tenantStateChangedRequest (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.notify_tenant_state_changed(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'notify_tenant_state_changed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def patch_custom_translation(body_json: str, locale_code: str) -> str:
    """
    Update Custom Translation for specified translation keys only (patchCustomTranslation)  # noqa: E501

The API call is designed to update the custom translation for specified key only.    Request example:   ```json {"notification.active":"active"} ```  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object body: (required)
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.patch_custom_translation(body=deserialize_param(body_json, 'CustomTranslation'), locale_code=locale_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'patch_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def post_device_attributes(device_token: str, body: Optional[str] = None) -> str:
    """
    Post attributes (postDeviceAttributes)  # noqa: E501

Post client attribute updates on behalf of device.   Example of the request:   ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ```  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.post_device_attributes(device_token=device_token, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'post_device_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def post_rpc_request(device_token: str, body: Optional[str] = None) -> str:
    """
    Send the RPC command (postRpcRequest)  # noqa: E501

Send the RPC request to server. The request payload is a JSON document that contains 'method' and 'params'. For example:  ```json {"method": "sumOnServer", "params":{"a":2, "b":2}} ```  The response contains arbitrary JSON with the RPC reply. For example:   ```json {"result": 4} ```  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.post_rpc_request(device_token=device_token, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'post_rpc_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def post_telemetry(device_token: str, body: Optional[str] = None) -> str:
    """
    Post time-series data (postTelemetry)  # noqa: E501

Post time-series data on behalf of device.   Example of the request: The request payload is a JSON document with three possible formats:  Simple format without timestamp. In such a case, current server time will be used:     ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ```     Single JSON object with timestamp:     ```json {"ts":1634712287000,"values":{"temperature":26, "humidity":87}} ```     JSON array with timestamps:     ```json [ {"ts":1634712287000,"values":{"temperature":26, "humidity":87}},  {"ts":1634712588000,"values":{"temperature":25, "humidity":88}} ] ```  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.post_telemetry(device_token=device_token, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'post_telemetry'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def preview_white_label_params(body: Optional[str] = None) -> str:
    """
    Preview Login White Labeling configuration (saveWhiteLabelParams)  # noqa: E501

Merge the White Labeling configuration with the parent configuration and return the result.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param WhiteLabelingParams body:
:return: WhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.preview_white_label_params(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'preview_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def privacy_policy_accepted() -> str:
    """
    Check privacy policy (privacyPolicyAccepted)  # noqa: E501

Checks that current user accepted the privacy policy.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.privacy_policy_accepted()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'privacy_policy_accepted'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def process_asset_bulk_import(body: Optional[str] = None) -> str:
    """
    Import the bulk of assets (processAssetsBulkImport)  # noqa: E501

There's an ability to import the bulk of assets using the only .csv file.   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param BulkImportRequest body:
:return: BulkImportResultAsset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.process_asset_bulk_import(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'process_asset_bulk_import'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def process_devices_bulk_import(body: Optional[str] = None) -> str:
    """
    Import the bulk of devices (processDevicesBulkImport)  # noqa: E501

There's an ability to import the bulk of devices using the only .csv file. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param BulkImportRequest body:
:return: BulkImportResultDevice
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.process_devices_bulk_import(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'process_devices_bulk_import'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def process_edges_bulk_import(body: Optional[str] = None) -> str:
    """
    Import the bulk of edges (processEdgesBulkImport)  # noqa: E501

There's an ability to import the bulk of edges using the only .csv file.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param BulkImportRequest body:
:return: BulkImportResultEdge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.process_edges_bulk_import(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'process_edges_bulk_import'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def process_system_edqs_request(body_json: str) -> str:
    """
    processSystemEdqsRequest  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ToCoreEdqsRequest body: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.process_system_edqs_request(body=deserialize_param(body_json, 'ToCoreEdqsRequest'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'process_system_edqs_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def provision_device(body: Optional[str] = None) -> str:
    """
    Provision new device (provisionDevice)  # noqa: E501

Exchange the provision request to the device credentials. See more info about provisioning in the corresponding 'Device provisioning' platform documentation.Requires valid JSON request with the following format:   ```json {   "deviceName": "NEW_DEVICE_NAME",   "provisionDeviceKey": "u7piawkboq8v32dmcmpp",   "provisionDeviceSecret": "jpmwdn8ptlswmf4m29bw" } ```  Where 'deviceName' is the name of enw or existing device which depends on the provisioning strategy. The 'provisionDeviceKey' and 'provisionDeviceSecret' matches info configured in one of the existing device profiles. The result of the successful call is the JSON object that contains new credentials:  ```json {   "credentialsType":"ACCESS_TOKEN",   "credentialsValue":"DEVICE_ACCESS_TOKEN",   "status":"SUCCESS" } ```    # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.provision_device(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'provision_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def put_user_settings(body_json: str) -> str:
    """
    Update user settings (saveUserSettings)  # noqa: E501

Update user settings for authorized user. Only specified json elements will be updated.Example: you have such settings: {A:5, B:{C:10, D:20}}. Updating it with {B:{C:10, D:30}} will result in{A:5, B:{C:10, D:30}}. The same could be achieved by putting {B.D:30}  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.put_user_settings(body=deserialize_param(body_json, 'JsonNode'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'put_user_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def re_claim_device(device_name: str) -> str:
    """
    Reclaim device (reClaimDevice)  # noqa: E501

Reclaiming means the device will be unassigned from the customer and the device will be available for claiming again.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'CLAIM_DEVICES' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_name: Unique name of the device which is going to be reclaimed (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.re_claim_device(device_name=device_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 're_claim_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def refresh() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.refresh()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'refresh'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def remove_entities_from_entity_group(entity_group_id_json: str, body_json: str = None) -> str:
    """
    Remove entities from the group (removeEntitiesFromEntityGroup)  # noqa: E501

Removes entities from the specified entity group. Entity group allows you to group multiple entities of the same entity type (Device, Asset, Customer, User, Dashboard, etc). Entity Group always have an owner - particular Tenant or Customer. Each entity may belong to multiple groups simultaneously.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'REMOVE_FROM_GROUP' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.remove_entities_from_entity_group(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'remove_entities_from_entity_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def remove_mobile_session(x_mobile_token: str) -> str:
    """
    removeMobileSession  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str x_mobile_token: X-Mobile-Token (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.remove_mobile_session(x_mobile_token=x_mobile_token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'remove_mobile_session'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def reply_to_command(device_token: str, request_id: int, body: Optional[str] = None) -> str:
    """
    Reply to RPC commands (replyToCommand)  # noqa: E501

Replies to server originated RPC command identified by 'requestId' parameter. The response is arbitrary JSON.  The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param int request_id: RPC request id from the incoming RPC request (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.reply_to_command(device_token=device_token, request_id=request_id, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'reply_to_command'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def report_user_dashboard_action(dashboard_id_json: str, action: str) -> str:
    """
    Report action of User over the dashboard (reportUserDashboardAction)  # noqa: E501

Report action of User over the dashboard.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str dashboard_id: A string value representing the dashboard id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str action: Dashboard action, one of: "visit", "star" or "unstar". (required)
:return: UserDashboardsInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.report_user_dashboard_action(dashboard_id=deserialize_param(dashboard_id_json, 'DashboardId'), action=action)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'report_user_dashboard_action'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def repository_settings_exists() -> str:
    """
    Check repository settings exists (repositorySettingsExists)  # noqa: E501

Check whether the repository settings exists.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.repository_settings_exists()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'repository_settings_exists'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def reprocess_calculated_field(id_json: str, start_ts: int, end_ts: int) -> str:
    """
    Reprocess Calculated Field (reprocessCalculatedField)  # noqa: E501

Reprocesses the calculated field.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str calculated_field_id: (required)
:param int start_ts: (required)
:param int end_ts: (required)
:return: Job
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.reprocess_calculated_field(id=deserialize_param(id_json, 'CalculatedFieldId'), start_ts=start_ts, end_ts=end_ts)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'reprocess_calculated_field'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def reprocess_job(id_json: str) -> str:
    """
    Reprocess job (reprocessJob)  # noqa: E501

Reprocesses the job. Failures are located at job.result.results list. Platform iterates over this list and submits new tasks for them. Doesn't create new job entity but updates the existing one. Successfully reprocessed job will look the same as completed one.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.reprocess_job(id=deserialize_param(id_json, 'JobId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'reprocess_job'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def request_reset_password_by_email(body: Optional[str] = None) -> str:
    """
    Request reset password email (requestResetPasswordByEmail)  # noqa: E501

Request to send the reset password email if the user with specified email address is present in the database. Always return '200 OK' status for security purposes.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ResetPasswordEmailRequest body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.request_reset_password_by_email(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'request_reset_password_by_email'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def request_two_fa_verification_code(provider_type: str) -> str:
    """
    Request 2FA verification code (requestTwoFaVerificationCode)  # noqa: E501

Request 2FA verification code.  To make a request to this endpoint, you need an access token with the scope of PRE_VERIFICATION_TOKEN, which is issued on username/password auth if 2FA is enabled.  The API method is rate limited (using rate limit config from TwoFactorAuthSettings). Will return a Bad Request error if provider is not configured for usage, and Too Many Requests error if rate limits are exceeded.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str provider_type: providerType (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.request_two_fa_verification_code(provider_type=provider_type)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'request_two_fa_verification_code'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def resend_email_activation(email: str, pkg_name: Optional[str] = None) -> str:
    """
    Resend Activation Email (resendEmailActivation)  # noqa: E501

Request to resend the activation email for the user. Checks that user was not activated yet.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str email: Email of the user. (required)
:param str pkg_name: Optional package name of the mobile application.
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.resend_email_activation(email=email, pkg_name=pkg_name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'resend_email_activation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def reset_password(body: Optional[str] = None) -> str:
    """
    Reset password (resetPassword)  # noqa: E501

Checks the password reset token and updates the password. If token is valid, returns the object that contains [JWT](https://jwt.io/) access and refresh tokens. If token is not valid, returns '404 Bad Request'.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ResetPasswordRequest body:
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.reset_password(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'reset_password'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def rpc_v2_get_persisted_rpc(rpc_id_json: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.rpc_v2_get_persisted_rpc(rpc_id=deserialize_param(rpc_id_json, 'RpcId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'rpc_v2_get_persisted_rpc'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def run() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.run()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'run'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_admin_settings(body: Optional[str] = None) -> str:
    """
    Get the Administration Settings object using key (getAdminSettings)  # noqa: E501

Creates or Updates the Administration Settings. Platform generates random Administration Settings Id during settings creation. The Administration Settings Id will be present in the response. Specify the Administration Settings Id when you would like to update the Administration Settings. Referencing non-existing Administration Settings Id will cause an error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'WRITE' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AdminSettings body:
:return: AdminSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_admin_settings(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_admin_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_ai_model(body_json: str) -> str:
    """
    Create or update AI model (saveAiModel)  # noqa: E501

Creates or updates an AI model record.  • **Create:** Omit the `id` to create a new record. The platform assigns a UUID to the new record and returns it in the `id` field of the response.  • **Update:** Include an existing `id` to modify that record. If no matching record exists, the API responds with **404 Not Found**.  Tenant ID for the AI model will be taken from the authenticated user making the request, regardless of any value provided in the request body.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AiModel body: (required)
:return: AiModel
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_ai_model(body=deserialize_param(body_json, 'AiModel'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_ai_model'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_alarm(body_json: str) -> str:
    """
    Create or Update Alarm (saveAlarm)  # noqa: E501

Creates or Updates the Alarm. When creating alarm, platform generates Alarm Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Alarm id will be present in the response. Specify existing Alarm id to update the alarm. Referencing non-existing Alarm Id will cause 'Not Found' error.   Platform also deduplicate the alarms based on the entity id of originator and alarm 'type'. For example, if the user or system component create the alarm with the type 'HighTemperature' for device 'Device A' the new active alarm is created. If the user tries to create 'HighTemperature' alarm for the same device again, the previous alarm will be updated (the 'end_ts' will be set to current timestamp). If the user clears the alarm (see 'Clear Alarm(clearAlarm)'), than new alarm with the same type and same device may be created. Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Alarm entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Alarm body:
:return: Alarm
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_alarm(body=deserialize_param(body_json, 'Alarm'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_alarm_comment(alarm_id_json: str, body: Optional[str] = None) -> str:
    """
    Create or update Alarm Comment   # noqa: E501

Creates or Updates the Alarm Comment. When creating comment, platform generates Alarm Comment Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Alarm Comment id will be present in the response. Specify existing Alarm Comment id to update the alarm. Referencing non-existing Alarm Comment Id will cause 'Not Found' error.    To create new Alarm comment entity it is enough to specify 'comment' json element with 'text' node, for example: {"comment": { "text": "my comment"}}.    If comment type is not specified the default value 'OTHER' will be saved. If 'alarmId' or 'userId' specified in body it will be ignored.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param AlarmComment body:
:return: AlarmComment
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_alarm_comment(alarm_id=deserialize_param(alarm_id_json, 'AlarmId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_alarm_comment'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_asset(entity_group_id_json: str, body: Optional[str] = None, entity_group_ids: Optional[str] = None) -> str:
    """
    Create Or Update Asset (saveAsset)  # noqa: E501

Creates or Updates the Asset. When creating asset, platform generates Asset Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Asset id will be present in the response. Specify existing Asset id to update the asset. Referencing non-existing Asset Id will cause 'Not Found' error. Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Asset entity.    Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Asset body:
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'. If specified, the entity will be added to the corresponding entity group.
:param str entity_group_ids: A list of string values, separated by comma ',' representing the Entity Group Ids. For example, '784f394c-42b6-435a-983c-b7beff2784f9','a84f394c-42b6-435a-083c-b7beff2784f9'. If specified, the entity will be added to the corresponding entity groups.
:return: Asset
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_asset(body=body, entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), entity_group_ids=entity_group_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_asset'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_asset_profile(body_json: str) -> str:
    """
    Create Or Update Asset Profile (saveAssetProfile)  # noqa: E501

Create or update the Asset Profile. When creating asset profile, platform generates asset profile id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created asset profile id will be present in the response. Specify existing asset profile id to update the asset profile. Referencing non-existing asset profile Id will cause 'Not Found' error.   Asset profile name is unique in the scope of tenant. Only one 'default' asset profile may exist in scope of tenant. Remove 'id', 'tenantId' from the request body example (below) to create new Asset Profile entity.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AssetProfile body:
:return: AssetProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_asset_profile(body=deserialize_param(body_json, 'AssetProfile'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_asset_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_auto_commit_settings(body: Optional[str] = None) -> str:
    """
    Creates or Updates the auto commit settings (saveAutoCommitSettings)  # noqa: E501

Creates or Updates the auto commit settings object.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param dict(str, AutoVersionCreateConfig) body:
:return: AutoCommitSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_auto_commit_settings(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_auto_commit_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_calculated_field(body_json: str) -> str:
    """
    Create Or Update Calculated Field (saveCalculatedField)  # noqa: E501

Creates or Updates the Calculated Field. When creating calculated field, platform generates Calculated Field Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Calculated Field Id will be present in the response. Specify existing Calculated Field Id to update the calculated field. Referencing non-existing Calculated Field Id will cause 'Not Found' error. Remove 'id', 'tenantId' from the request body example (below) to create new Calculated Field entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param CalculatedField body: A JSON value representing the calculated field. (required)
:return: CalculatedField
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_calculated_field(body=deserialize_param(body_json, 'CalculatedField'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_calculated_field'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_client_registration_template(body: Optional[str] = None) -> str:
    """
    Create or update OAuth2 client registration template (saveClientRegistrationTemplate)  Available for users with 'SYS_ADMIN' authority.  # noqa: E501

Client registration template is OAuth2 provider configuration template with default settings for registering new OAuth2 clients  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param OAuth2ClientRegistrationTemplate body:
:return: OAuth2ClientRegistrationTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_client_registration_template(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_client_registration_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_converter(body: Optional[str] = None) -> str:
    """
    Create Or Update Converter (saveConverter)  # noqa: E501

Create or update the Converter. When creating converter, platform generates Converter Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created converter id will be present in the response. Specify existing Converter id to update the converter. Referencing non-existing converter Id will cause 'Not Found' error. Converter name is unique in the scope of tenant.   # Converter Configuration  Converter configuration (**'configuration'** field) is the JSON object that should contain one of two possible fields: **'decoder'** or **'encoder'**. The former is used when the converter has UPLINK type, the latter is used - when DOWNLINK type. It can contain both 'decoder' and 'encoder' fields, when the correct one is specified for the appropriate converter type, another one can be set to 'null'. See the examples of each one below.   ## Uplink Converter Configuration  ***Default converter may be different, depending on integration type***.  ```json {    "decoder":"// Decode an uplink message from a buffer\\n// payload - array of bytes\\n// metadata - key/value object\\n\\n/** Decoder **/\\n\\n// decode payload to string\\nvar payloadStr = decodeToString(payload);\\n\\n// decode payload to JSON\\n// var data = decodeToJson(payload);\\n\\nvar deviceName = 'Device A';\\nvar deviceType = 'thermostat';\\nvar customerName = 'customer';\\nvar groupName = 'thermostat devices';\\nvar manufacturer = 'Example corporation';\\n// use assetName and assetType instead of deviceName and deviceType\\n// to automatically create assets instead of devices.\\n// var assetName = 'Asset A';\\n// var assetType = 'building';\\n\\n// Result object with device/asset attributes/telemetry data\\nvar result = {\\n// Use deviceName and deviceType or assetName and assetType, but not both.\\n   deviceName: deviceName,\\n   deviceType: deviceType,\\n// assetName: assetName,\\n// assetType: assetType,\\n   customerName: customerName,\\n   groupName: groupName,\\n   contentAwareAttributeKeys: ['manufacturer'],\\n   attributes: {\\n       model: 'Model A',\\n       serialNumber: 'SN111',\\n       integrationName: metadata['integrationName'],\\n       manufacturer: manufacturer\\n   },\\n   telemetry: {\\n       temperature: 42,\\n       humidity: 80,\\n       rawData: payloadStr\\n   }\\n};\\n\\n/** Helper functions **/\\n\\nfunction decodeToString(payload) {\\n   return String.fromCharCode.apply(String, payload);\\n}\\n\\nfunction decodeToJson(payload) {\\n   // covert payload to string.\\n   var str = decodeToString(payload);\\n\\n   // parse string to JSON\\n   var data = JSON.parse(str);\\n   return data;\\n}\\n\\nreturn result;",    "encoder":null } ```  Decoder field in the more readable form:  ```text // Decode an uplink message from a buffer // payload - array of bytes // metadata - key/value object  /** Decoder **/  // decode payload to string var payloadStr = decodeToString(payload);  // decode payload to JSON // var data = decodeToJson(payload);  var deviceName = 'Device A'; var deviceType = 'thermostat'; var customerName = 'customer'; var groupName = 'thermostat devices'; var manufacturer = 'Example corporation'; // use assetName and assetType instead of deviceName and deviceType // to automatically create assets instead of devices. // var assetName = 'Asset A'; // var assetType = 'building';  // Result object with device/asset attributes/telemetry data var result = { // Use deviceName and deviceType or assetName and assetType, but not both.    deviceName: deviceName,    deviceType: deviceType, // assetName: assetName, // assetType: assetType,    customerName: customerName,    groupName: groupName,    attributes: {        model: 'Model A',        serialNumber: 'SN111',        integrationName: metadata['integrationName']        manufacturer: manufacturer,    },    telemetry: {        temperature: 42,        humidity: 80,        rawData: payloadStr    } };  /** Helper functions **/  function decodeToString(payload) {    return String.fromCharCode.apply(String, payload); }  function decodeToJson(payload) {    // covert payload to string.    var str = decodeToString(payload);     // parse string to JSON    var data = JSON.parse(str);    return data; }  return result; ```  ## Downlink Converter Configuration  ```json {    "decoder":null,    "encoder":"// Encode downlink data from incoming Rule Engine message\\n\\n// msg - JSON message payload downlink message json\\n// msgType - type of message, for ex. 'ATTRIBUTES_UPDATED', 'POST_TELEMETRY_REQUEST', etc.\\n// metadata - list of key-value pairs with additional data about the message\\n// integrationMetadata - list of key-value pairs with additional data defined in Integration executing this converter\\n\\n/** Encoder **/\\n\\nvar data = {};\\n\\n// Process data from incoming message and metadata\\n\\ndata.tempFreq = msg.temperatureUploadFrequency;\\ndata.humFreq = msg.humidityUploadFrequency;\\n\\ndata.devSerialNumber = metadata['ss_serialNumber'];\\n\\n// Result object with encoded downlink payload\\nvar result = {\\n\\n    // downlink data content type: JSON, TEXT or BINARY (base64 format)\\n    contentType: \\"JSON\\",\\n\\n    // downlink data\\n    data: JSON.stringify(data),\\n\\n    // Optional metadata object presented in key/value format\\n    metadata: {\\n            topic: metadata['deviceType']+'/'+metadata['deviceName']+'/upload'\\n    }\\n\\n};\\n\\nreturn result;" } ```  Encoder field in the more readable form:  ```text // Encode downlink data from incoming Rule Engine message  // msg - JSON message payload downlink message json // msgType - type of message, for ex. 'ATTRIBUTES_UPDATED', 'POST_TELEMETRY_REQUEST', etc. // metadata - list of key-value pairs with additional data about the message // integrationMetadata - list of key-value pairs with additional data defined in Integration executing this converter  /** Encoder **/  var data = {};  // Process data from incoming message and metadata  data.tempFreq = msg.temperatureUploadFrequency; data.humFreq = msg.humidityUploadFrequency;  data.devSerialNumber = metadata['ss_serialNumber'];  // Result object with encoded downlink payload var result = {      // downlink data content type: JSON, TEXT or BINARY (base64 format)     contentType: "JSON",      // downlink data     data: JSON.stringify(data),      // Optional metadata object presented in key/value format     metadata: {             topic: metadata['deviceType']+'/'+metadata['deviceName']+'/upload'     }  };  return result; ```  Remove 'id', 'tenantId' from the request body example (below) to create new converter entity.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Converter body:
:return: Converter
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_converter(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_custom_translation(locale_code: str, body_json: str) -> str:
    """
    Create Or Update Custom Translation (saveCustomTranslation)  # noqa: E501

Creates or Updates the Custom Translation map.   Request example:   ```json {"translationMap":{"es_ES":"{\\"home\\":\\"MyHome\\"}"}} ```  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param CustomTranslation body:
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:return: CustomTranslation
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_custom_translation(locale_code=locale_code, body=deserialize_param(body_json, 'CustomTranslation'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_customer(body: Optional[str] = None, entity_group_id: Optional[str] = None, entity_group_ids_json: str = None) -> str:
    """
    Create or update Customer (saveCustomer)  # noqa: E501

Creates or Updates the Customer. When creating customer, platform generates Customer Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Customer Id will be present in the response. Specify existing Customer Id to update the Customer. Referencing non-existing Customer Id will cause 'Not Found' error.Remove 'id', 'tenantId' from the request body example (below) to create new Customer entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Customer body:
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'. If specified, the entity will be added to the corresponding entity group.
:param str entity_group_ids: A list of string values, separated by comma ',' representing the Entity Group Ids. For example, '784f394c-42b6-435a-983c-b7beff2784f9','a84f394c-42b6-435a-083c-b7beff2784f9'. If specified, the entity will be added to the corresponding entity groups.
:return: Customer
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_customer(body=body, entity_group_id=entity_group_id, entity_group_ids=json.loads(entity_group_ids_json) if entity_group_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_customer'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_dashboard(body: Optional[str] = None) -> str:
    """
    Create Or Update Dashboard (saveDashboard)  # noqa: E501

Create or update the Dashboard. When creating dashboard, platform generates Dashboard Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Dashboard id will be present in the response. Specify existing Dashboard id to update the dashboard. Referencing non-existing dashboard Id will cause 'Not Found' error. Only users with 'TENANT_ADMIN') authority may create the dashboards.Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Dashboard entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Dashboard body:
:param str entity_group_id: entityGroupId
:param str entity_group_ids: entityGroupIds
:return: Dashboard
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_dashboard(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_dashboard'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_device(body: str, access_token: Optional[str] = None) -> str:
    """
    Create Or Update Device (saveDevice)  # noqa: E501

Create or update the Device. When creating device, platform generates Device Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). Device credentials are also generated if not provided in the 'accessToken' request parameter. The newly created device id will be present in the response. Specify existing Device id to update the device. Referencing non-existing device Id will cause 'Not Found' error.  Device name is unique in the scope of tenant. Use unique identifiers like MAC or IMEI for the device names and non-unique 'label' field for user-friendly visualization purposes.Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Device entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Device body:
:param str access_token: Optional value of the device credentials to be used during device creation. If omitted, access token will be auto-generated.
:param str entity_group_id: entityGroupId
:param str entity_group_ids: entityGroupIds
:return: Device
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_device(body=body, access_token=access_token)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_device'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_device_attributes(device_id_json: str, scope: str, body_json: str = None) -> str:
    """
    Save device attributes (saveDeviceAttributes)  # noqa: E501

Creates or updates the device attributes based on device id and specified attribute scope. The request payload is a JSON object with key-value format of attributes to create or update. For example:  ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ```   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_id: A string value representing the device id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param JsonNode body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_device_attributes(device_id=deserialize_param(device_id_json, 'DeviceId'), scope=scope, body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_device_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_device_group_ota_package(body: Optional[str] = None) -> str:
    """
    saveDeviceGroupOtaPackage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param DeviceGroupOtaPackage body:
:return: DeviceGroupOtaPackage
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_device_group_ota_package(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_device_group_ota_package'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_device_profile(body: Optional[str] = None) -> str:
    """
    Create Or Update Device Profile (saveDeviceProfile)  # noqa: E501

Create or update the Device Profile. When creating device profile, platform generates device profile id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created device profile id will be present in the response. Specify existing device profile id to update the device profile. Referencing non-existing device profile Id will cause 'Not Found' error.   Device profile name is unique in the scope of tenant. Only one 'default' device profile may exist in scope of tenant.  # Device profile data definition  Device profile data object contains alarm rules configuration, device provision strategy and transport type configuration for device connectivity. Let's review some examples. First one is the default device profile data configuration and second one - the custom one.   ```json {    "alarms":[    ],    "configuration":{       "type":"DEFAULT"    },    "provisionConfiguration":{       "type":"DISABLED",       "provisionDeviceSecret":null    },    "transportConfiguration":{       "type":"DEFAULT"    } } ```  ```json {    "alarms":[       {          "id":"2492b935-1226-59e9-8615-17d8978a4f93",          "alarmType":"Temperature Alarm",          "clearRule":{             "schedule":null,             "condition":{                "spec":{                   "type":"SIMPLE"                },                "condition":[                   {                      "key":{                         "key":"temperature",                         "type":"TIME_SERIES"                      },                      "value":null,                      "predicate":{                         "type":"NUMERIC",                         "value":{                            "userValue":null,                            "defaultValue":30.0,                            "dynamicValue":null                         },                         "operation":"LESS"                      },                      "valueType":"NUMERIC"                   }                ]             },             "dashboardId":null,             "alarmDetails":null          },          "propagate":false,          "createRules":{             "MAJOR":{                "schedule":{                   "type":"SPECIFIC_TIME",                   "endsOn":64800000,                   "startsOn":43200000,                   "timezone":"Europe/Kiev",                   "daysOfWeek":[                      1,                      3,                      5                   ]                },                "condition":{                   "spec":{                      "type":"DURATION",                      "unit":"MINUTES",                      "predicate":{                         "userValue":null,                         "defaultValue":30,                         "dynamicValue":null                      }                   },                   "condition":[                      {                         "key":{                            "key":"temperature",                            "type":"TIME_SERIES"                         },                         "value":null,                         "predicate":{                            "type":"COMPLEX",                            "operation":"OR",                            "predicates":[                               {                                  "type":"NUMERIC",                                  "value":{                                     "userValue":null,                                     "defaultValue":50.0,                                     "dynamicValue":null                                  },                                  "operation":"LESS_OR_EQUAL"                               },                               {                                  "type":"NUMERIC",                                  "value":{                                     "userValue":null,                                     "defaultValue":30.0,                                     "dynamicValue":null                                  },                                  "operation":"GREATER"                               }                            ]                         },                         "valueType":"NUMERIC"                      }                   ]                },                "dashboardId":null,                "alarmDetails":null             },             "WARNING":{                "schedule":{                   "type":"CUSTOM",                   "items":[                      {                         "endsOn":0,                         "enabled":false,                         "startsOn":0,                         "dayOfWeek":1                      },                      {                         "endsOn":64800000,                         "enabled":true,                         "startsOn":43200000,                         "dayOfWeek":2                      },                      {                         "endsOn":0,                         "enabled":false,                         "startsOn":0,                         "dayOfWeek":3                      },                      {                         "endsOn":57600000,                         "enabled":true,                         "startsOn":36000000,                         "dayOfWeek":4                      },                      {                         "endsOn":0,                         "enabled":false,                         "startsOn":0,                         "dayOfWeek":5                      },                      {                         "endsOn":0,                         "enabled":false,                         "startsOn":0,                         "dayOfWeek":6                      },                      {                         "endsOn":0,                         "enabled":false,                         "startsOn":0,                         "dayOfWeek":7                      }                   ],                   "timezone":"Europe/Kiev"                },                "condition":{                   "spec":{                      "type":"REPEATING",                      "predicate":{                         "userValue":null,                         "defaultValue":5,                         "dynamicValue":null                      }                   },                   "condition":[                      {                         "key":{                            "key":"tempConstant",                            "type":"CONSTANT"                         },                         "value":30,                         "predicate":{                            "type":"NUMERIC",                            "value":{                               "userValue":null,                               "defaultValue":0.0,                               "dynamicValue":{                                  "inherit":false,                                  "sourceType":"CURRENT_DEVICE",                                  "sourceAttribute":"tempThreshold"                               }                            },                            "operation":"EQUAL"                         },                         "valueType":"NUMERIC"                      }                   ]                },                "dashboardId":null,                "alarmDetails":null             },             "CRITICAL":{                "schedule":null,                "condition":{                   "spec":{                      "type":"SIMPLE"                   },                   "condition":[                      {                         "key":{                            "key":"temperature",                            "type":"TIME_SERIES"                         },                         "value":null,                         "predicate":{                            "type":"NUMERIC",                            "value":{                               "userValue":null,                               "defaultValue":50.0,                               "dynamicValue":null                            },                            "operation":"GREATER"                         },                         "valueType":"NUMERIC"                      }                   ]                },                "dashboardId":null,                "alarmDetails":null             }          },          "propagateRelationTypes":null       }    ],    "configuration":{       "type":"DEFAULT"    },    "provisionConfiguration":{       "type":"ALLOW_CREATE_NEW_DEVICES",       "provisionDeviceSecret":"vaxb9hzqdbz3oqukvomg"    },    "transportConfiguration":{       "type":"MQTT",       "deviceTelemetryTopic":"v1/devices/me/telemetry",       "deviceAttributesTopic":"v1/devices/me/attributes",       "transportPayloadTypeConfiguration":{          "transportPayloadType":"PROTOBUF",          "deviceTelemetryProtoSchema":"syntax =\\"proto3\\";\\npackage telemetry;\\n\\nmessage SensorDataReading {\\n\\n  optional double temperature = 1;\\n  optional double humidity = 2;\\n  InnerObject innerObject = 3;\\n\\n  message InnerObject {\\n    optional string key1 = 1;\\n    optional bool key2 = 2;\\n    optional double key3 = 3;\\n    optional int32 key4 = 4;\\n    optional string key5 = 5;\\n  }\\n}",          "deviceAttributesProtoSchema":"syntax =\\"proto3\\";\\npackage attributes;\\n\\nmessage SensorConfiguration {\\n  optional string firmwareVersion = 1;\\n  optional string serialNumber = 2;\\n}",          "deviceRpcRequestProtoSchema":"syntax =\\"proto3\\";\\npackage rpc;\\n\\nmessage RpcRequestMsg {\\n  optional string method = 1;\\n  optional int32 requestId = 2;\\n  optional string params = 3;\\n}",          "deviceRpcResponseProtoSchema":"syntax =\\"proto3\\";\\npackage rpc;\\n\\nmessage RpcResponseMsg {\\n  optional string payload = 1;\\n}"       }    } } ```  Let's review some specific objects examples related to the device profile configuration:  # Alarm Schedule  Alarm Schedule JSON object represents the time interval during which the alarm rule is active. Note,   ```json "schedule": null ```  means alarm rule is active all the time. **'daysOfWeek'** field represents Monday as 1, Tuesday as 2 and so on. **'startsOn'** and **'endsOn'** fields represent hours in millis (e.g. 64800000 = 18:00 or 6pm). **'enabled'** flag specifies if item in a custom rule is active for specific day of the week:  ## Specific Time Schedule  ```json {    "schedule":{       "type":"SPECIFIC_TIME",       "endsOn":64800000,       "startsOn":43200000,       "timezone":"Europe/Kiev",       "daysOfWeek":[          1,          3,          5       ]    } } ```  ## Custom Schedule  ```json {    "schedule":{       "type":"CUSTOM",       "items":[          {             "endsOn":0,             "enabled":false,             "startsOn":0,             "dayOfWeek":1          },          {             "endsOn":64800000,             "enabled":true,             "startsOn":43200000,             "dayOfWeek":2          },          {             "endsOn":0,             "enabled":false,             "startsOn":0,             "dayOfWeek":3          },          {             "endsOn":57600000,             "enabled":true,             "startsOn":36000000,             "dayOfWeek":4          },          {             "endsOn":0,             "enabled":false,             "startsOn":0,             "dayOfWeek":5          },          {             "endsOn":0,             "enabled":false,             "startsOn":0,             "dayOfWeek":6          },          {             "endsOn":0,             "enabled":false,             "startsOn":0,             "dayOfWeek":7          }       ],       "timezone":"Europe/Kiev"    } } ```  # Alarm condition type (**'spec'**)  Alarm condition type can be either simple, duration, or repeating. For example, 5 times in a row or during 5 minutes.  Note, **'userValue'** field is not used and reserved for future usage, **'dynamicValue'** is used for condition appliance by using the value of the **'sourceAttribute'** or else **'defaultValue'** is used (if **'sourceAttribute'** is absent).  **'sourceType'** of the **'sourceAttribute'** can be:   * 'CURRENT_DEVICE';  * 'CURRENT_CUSTOMER';  * 'CURRENT_TENANT'.  **'sourceAttribute'** can be inherited from the owner if **'inherit'** is set to true (for CURRENT_DEVICE and CURRENT_CUSTOMER).  ## Repeating alarm condition  ```json {    "spec":{       "type":"REPEATING",       "predicate":{          "userValue":null,          "defaultValue":5,          "dynamicValue":{             "inherit":true,             "sourceType":"CURRENT_DEVICE",             "sourceAttribute":"tempAttr"          }       }    } } ```  ## Duration alarm condition  ```json {    "spec":{       "type":"DURATION",       "unit":"MINUTES",       "predicate":{          "userValue":null,          "defaultValue":30,          "dynamicValue":null       }    } } ```  **'unit'** can be:   * 'SECONDS';  * 'MINUTES';  * 'HOURS';  * 'DAYS'.  # Key Filters  Key filter objects are created under the **'condition'** array. They allow you to define complex logical expressions over entity field, attribute, latest time-series value or constant. The filter is defined using 'key', 'valueType', 'value' (refers to the value of the 'CONSTANT' alarm filter key type) and 'predicate' objects. Let's review each object:  ## Alarm Filter Key  Filter Key defines either entity field, attribute, telemetry or constant. It is a JSON object that consists the key name and type. The following filter key types are supported:  * 'ATTRIBUTE' - used for attributes values;  * 'TIME_SERIES' - used for time-series values;  * 'ENTITY_FIELD' - used for accessing entity fields like 'name', 'label', etc. The list of available fields depends on the entity type;  * 'CONSTANT' - constant value specified.  Let's review the example:  ```json {   "type": "TIME_SERIES",   "key": "temperature" } ```  ## Value Type and Operations  Provides a hint about the data type of the entity field that is defined in the filter key. The value type impacts the list of possible operations that you may use in the corresponding predicate. For example, you may use 'STARTS_WITH' or 'END_WITH', but you can't use 'GREATER_OR_EQUAL' for string values.The following filter value types and corresponding predicate operations are supported:    * 'STRING' - used to filter any 'String' or 'JSON' values. Operations: EQUAL, NOT_EQUAL, STARTS_WITH, ENDS_WITH, CONTAINS, NOT_CONTAINS;   * 'NUMERIC' - used for 'Long' and 'Double' values. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;   * 'BOOLEAN' - used for boolean values. Operations: EQUAL, NOT_EQUAL;  * 'DATE_TIME' - similar to numeric, transforms value to milliseconds since epoch. Operations: EQUAL, NOT_EQUAL, GREATER, LESS, GREATER_OR_EQUAL, LESS_OR_EQUAL;      ## Filter Predicate  Filter Predicate defines the logical expression to evaluate. The list of available operations depends on the filter value type, see above. Platform supports 4 predicate types: 'STRING', 'NUMERIC', 'BOOLEAN' and 'COMPLEX'. The last one allows to combine multiple operations over one filter key.  Simple predicate example to check 'value < 100':   ```json {   "operation": "LESS",   "value": {     "userValue": null,     "defaultValue": 100,     "dynamicValue": null   },   "type": "NUMERIC" } ```  Complex predicate example, to check 'value < 10 or value > 20':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "userValue": null,         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "operation": "GREATER",       "value": {         "userValue": null,         "defaultValue": 20,         "dynamicValue": null       },       "type": "NUMERIC"     }   ] } ```  More complex predicate example, to check 'value < 10 or (value > 50 && value < 60)':   ```json {   "type": "COMPLEX",   "operation": "OR",   "predicates": [     {       "operation": "LESS",       "value": {         "userValue": null,         "defaultValue": 10,         "dynamicValue": null       },       "type": "NUMERIC"     },     {       "type": "COMPLEX",       "operation": "AND",       "predicates": [         {           "operation": "GREATER",           "value": {             "userValue": null,             "defaultValue": 50,             "dynamicValue": null           },           "type": "NUMERIC"         },         {           "operation": "LESS",           "value": {             "userValue": null,             "defaultValue": 60,             "dynamicValue": null           },           "type": "NUMERIC"         }       ]     }   ] } ```  You may also want to replace hardcoded values (for example, temperature > 20) with the more dynamic expression (for example, temperature > value of the tenant attribute with key 'temperatureThreshold'). It is possible to use 'dynamicValue' to define attribute of the tenant, customer or device. See example below:  ```json {   "operation": "GREATER",   "value": {     "userValue": null,     "defaultValue": 0,     "dynamicValue": {       "inherit": false,       "sourceType": "CURRENT_TENANT",       "sourceAttribute": "temperatureThreshold"     }   },   "type": "NUMERIC" } ```  Note that you may use 'CURRENT_DEVICE', 'CURRENT_CUSTOMER' and 'CURRENT_TENANT' as a 'sourceType'. The 'defaultValue' is used when the attribute with such a name is not defined for the chosen source. The 'sourceAttribute' can be inherited from the owner of the specified 'sourceType' if 'inherit' is set to true.  # Provision Configuration  There are 3 types of device provision configuration for the device profile:   * 'DISABLED';  * 'ALLOW_CREATE_NEW_DEVICES';  * 'CHECK_PRE_PROVISIONED_DEVICES'.  Please refer to the [docs](https://thingsboard.io/docs/user-guide/device-provisioning/) for more details.  # Transport Configuration  5 transport configuration types are available:  * 'DEFAULT';  * 'MQTT';  * 'LWM2M';  * 'COAP';  * 'SNMP'.  Default type supports basic MQTT, HTTP, CoAP and LwM2M transports. Please refer to the [docs](https://thingsboard.io/docs/user-guide/device-profiles/#transport-configuration) for more details about other types.  See another example of COAP transport configuration below:  ```json {    "type":"COAP",    "clientSettings":{       "edrxCycle":null,       "powerMode":"DRX",       "psmActivityTimer":null,       "pagingTransmissionWindow":null    },    "coapDeviceTypeConfiguration":{       "coapDeviceType":"DEFAULT",       "transportPayloadTypeConfiguration":{          "transportPayloadType":"JSON"       }    } } ```Remove 'id', 'tenantId' from the request body example (below) to create new Device Profile entity.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param DeviceProfile body:
:return: DeviceProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_device_profile(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_device_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_device_with_credentials(body: Optional[str] = None, entity_group_id: Optional[str] = None, entity_group_ids: Optional[str] = None) -> str:
    """
    Save device with credentials (Deprecated)  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object body: (required)
:param str entity_group_id:
:return: Device
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_device_with_credentials(body=body, entity_group_id=entity_group_id, entity_group_ids=entity_group_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_device_with_credentials'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_domain(body_json: str, oauth2_client_ids: Optional[str] = None) -> str:
    """
    Save or Update Domain (saveDomain)  # noqa: E501

Create or update the Domain. When creating domain, platform generates Domain Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Domain Id will be present in the response. Specify existing Domain Id to update the domain. Referencing non-existing Domain Id will cause 'Not Found' error.  Domain name is unique for entire platform setup.    Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Domain body: (required)
:param list[object] oauth2_client_ids: A list of oauth2 client registration ids, separated by comma ','
:return: Domain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_domain(body=deserialize_param(body_json, 'Domain'), oauth2_client_ids=oauth2_client_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_domain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_edge(body: Optional[str] = None) -> str:
    """
    Create Or Update Edge (saveEdge)  # noqa: E501

Create or update the Edge. When creating edge, platform generates Edge Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created edge id will be present in the response. Specify existing Edge id to update the edge. Referencing non-existing Edge Id will cause 'Not Found' error.  Edge name is unique in the scope of tenant. Use unique identifiers like MAC or IMEI for the edge names and non-unique 'label' field for user-friendly visualization purposes.Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Edge entity.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Edge body:
:param str entity_group_id: entityGroupId
:param str entity_group_ids: entityGroupIds
:return: Edge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_edge(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entities_version(body: Optional[str] = None) -> str:
    """
    Save entities version (saveEntitiesVersion)  # noqa: E501

Creates a new version of entities (or a single entity) by request. Supported entity types: CUSTOMER, ASSET, RULE_CHAIN, DASHBOARD, DEVICE_PROFILE, DEVICE, ENTITY_VIEW, WIDGETS_BUNDLE.  There are two available types of request: `SINGLE_ENTITY` and `COMPLEX`. Each of them contains version name (`versionName`) and name of a branch (`branch`) to create version (commit) in. If specified branch does not exists in a remote repo, then new empty branch will be created. Request of the `SINGLE_ENTITY` type has id of an entity (`entityId`) and additional configuration (`config`) which has following options:  - `saveRelations` - whether to add inbound and outbound relations of type COMMON to created entity version; - `saveAttributes` - to save attributes of server scope (and also shared scope for devices); - `saveCredentials` - when saving a version of a device, to add its credentials to the version.  An example of a `SINGLE_ENTITY` version create request: ```json {   "type": "SINGLE_ENTITY",    "versionName": "Version 1.0",   "branch": "dev",    "entityId": {     "entityType": "DEVICE",     "id": "b79448e0-d4f4-11ec-847b-0f432358ab48"   },   "config": {     "saveRelations": true,     "saveAttributes": true,     "saveCredentials": false   } } ```  Second request type (`COMPLEX`), additionally to `branch` and `versionName`, contains following properties: - `entityTypes` - a structure with entity types to export and configuration for each entity type;    this configuration has all the options available for `SINGLE_ENTITY` and additionally has these ones:       - `allEntities` and `entityIds` - if you want to save the version of all entities of the entity type         then set `allEntities` param to true, otherwise set it to false and specify the list of specific entities (`entityIds`);      - `syncStrategy` - synchronization strategy to use for this entity type: when set to `OVERWRITE`         then the list of remote entities of this type will be overwritten by newly added entities. If set to         `MERGE` - existing remote entities of this entity type will not be removed, new entities will just         be added on top (or existing remote entities will be updated). - `syncStrategy` - default synchronization strategy to use when it is not specified for an entity type.  Example for this type of request: ```json {   "type": "COMPLEX",    "versionName": "Devices and profiles: release 2",   "branch": "master",    "syncStrategy": "OVERWRITE",   "entityTypes": {     "DEVICE": {       "syncStrategy": null,       "allEntities": true,       "saveRelations": true,       "saveAttributes": true,       "saveCredentials": true     },     "DEVICE_PROFILE": {       "syncStrategy": "MERGE",       "allEntities": false,       "entityIds": [         "b79448e0-d4f4-11ec-847b-0f432358ab48"       ],       "saveRelations": true     }   } } ```  Response wil contain generated request UUID, that can be then used to retrieve status of operation via `getVersionCreateRequestStatus`.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param VersionCreateRequest body:
:return: DeferredResultuuid
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entities_version(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entities_version'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_attributes_v1(entity_id_json: str, scope: str, body_json: str = None) -> str:
    """
    Save entity attributes (saveEntityAttributesV1)  # noqa: E501

Creates or updates the entity attributes based on Entity Id and the specified attribute scope.  List of possible attribute scopes depends on the entity type:    * SERVER_SCOPE - supported for all entity types;  * SHARED_SCOPE - supported for devices.  The request payload is a JSON object with key-value format of attributes to create or update. For example:  ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ``` Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param JsonNode body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_attributes_v1(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_attributes_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_attributes_v2(entity_id_json: str, scope: str, body_json: str = None) -> str:
    """
    Save entity attributes (saveEntityAttributesV2)  # noqa: E501

Creates or updates the entity attributes based on Entity Id and the specified attribute scope.  List of possible attribute scopes depends on the entity type:    * SERVER_SCOPE - supported for all entity types;  * SHARED_SCOPE - supported for devices.  The request payload is a JSON object with key-value format of attributes to create or update. For example:  ```json {  "stringKey":"value1",   "booleanKey":true,   "doubleKey":42.0,   "longKey":73,   "jsonKey": {     "someNumber": 42,     "someArray": [1,2,3],     "someNestedObject": {"key": "value"}  } } ``` Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: A string value representing the attributes scope. For example, 'SERVER_SCOPE'. (required)
:param JsonNode body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_attributes_v2(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_attributes_v2'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_group(body: Optional[str] = None) -> str:
    """
    Create Or Update Entity Group (saveEntityGroup)  # noqa: E501

Create or update the Entity Group. When creating Entity Group, platform generates Entity Group Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Entity Group Id will be present in the response. Specify existing Entity Group Id to update the group. Referencing non-existing Entity Group Id will cause 'Not Found' error.Remove 'id', 'tenantId' and optionally 'ownerId' from the request body example (below) to create new Entity Group entity.   Entity group name is unique in the scope of owner and entity type. For example, you can't create two tenant device groups called 'Water meters'. However, you may create device and asset group with the same name. And also you may create groups with the same name for two different customers of the same tenant.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityGroup body:
:return: EntityGroupInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_group(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_telemetry(entity_id_json: str, scope: str, body: Optional[str] = None) -> str:
    """
    Save or update time-series data (saveEntityTelemetry)  # noqa: E501

Creates or updates the entity time-series data based on the Entity Id and request payload.The request payload is a JSON document with three possible formats:  Simple format without timestamp. In such a case, current server time will be used:   ```json {"temperature": 26} ```   Single JSON object with timestamp:   ```json {"ts":1634712287000,"values":{"temperature":26, "humidity":87}} ```   JSON array with timestamps:   ```json [{"ts":1634712287000,"values":{"temperature":26, "humidity":87}}, {"ts":1634712588000,"values":{"temperature":25, "humidity":88}}] ```   The scope parameter is not used in the API call implementation but should be specified whatever value because it is used as a path variable. Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: Value is deprecated, reserved for backward compatibility and not used in the API call implementation. Specify any scope for compatibility (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_telemetry(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_telemetry'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_telemetry_with_ttl(entity_id_json: str, scope: str, ttl: int, body: Optional[str] = None) -> str:
    """
    Save or update time-series data with TTL (saveEntityTelemetryWithTTL)  # noqa: E501

Creates or updates the entity time-series data based on the Entity Id and request payload.The request payload is a JSON document with three possible formats:  Simple format without timestamp. In such a case, current server time will be used:   ```json {"temperature": 26} ```   Single JSON object with timestamp:   ```json {"ts":1634712287000,"values":{"temperature":26, "humidity":87}} ```   JSON array with timestamps:   ```json [{"ts":1634712287000,"values":{"temperature":26, "humidity":87}}, {"ts":1634712588000,"values":{"temperature":25, "humidity":88}}] ```   The scope parameter is not used in the API call implementation but should be specified whatever value because it is used as a path variable.   The ttl parameter takes affect only in case of Cassandra DB.Referencing a non-existing entity Id or invalid entity type will cause an error.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_type: A string value representing the entity type. For example, 'DEVICE' (required)
:param str entity_id: A string value representing the entity id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scope: Value is deprecated, reserved for backward compatibility and not used in the API call implementation. Specify any scope for compatibility (required)
:param str ttl: A long value representing TTL (Time to Live) parameter. (required)
:param str body:
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_telemetry_with_ttl(entity_id=deserialize_param(entity_id_json, 'EntityId'), scope=scope, ttl=ttl, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_telemetry_with_ttl'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_entity_view(body: Optional[str] = None, entity_group_id: Optional[str] = None, entity_group_ids_json: str = None) -> str:
    """
    Save or update entity view (saveEntityView)  # noqa: E501

Entity Views limit the degree of exposure of the Device or Asset telemetry and attributes to the Customers. Every Entity View references exactly one entity (device or asset) and defines telemetry and attribute keys that will be visible to the assigned Customer. As a Tenant Administrator you are able to create multiple EVs per Device or Asset and assign them to different Customers. See the 'Model' tab for more details.Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Entity View entity.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityView body:
:param str entity_group_id: entityGroupId
:param str entity_group_ids: entityGroupIds
:return: EntityView
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_entity_view(body=body, entity_group_id=entity_group_id, entity_group_ids=json.loads(entity_group_ids_json) if entity_group_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_entity_view'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_group_permission(body: Optional[str] = None) -> str:
    """
    Create Or Update Group Permission (saveGroupPermission)  # noqa: E501

Creates or Updates the Group Permission. When creating group permission, platform generates Group Permission Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Group Permission id will be present in the response. Specify existing Group Permission id to update the permission. Referencing non-existing Group Permission Id will cause 'Not Found' error.  Group permission entity represents list of allowed operations for certain User Group to perform against certain Entity Group. Basically, this entity wires three other entities:    * Role that defines set of allowed operations;  * User Group that defines set of users who may perform the operations;   * Entity Group that defines set of entities which will be accessible to users;   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param GroupPermission body:
:return: GroupPermission
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_group_permission(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_group_permission'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_integration(body: Optional[str] = None) -> str:
    """
    Create Or Update Integration (saveIntegration)  # noqa: E501

Create or update the Integration. When creating integration, platform generates Integration Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created integration id will be present in the response. Specify existing Integration id to update the integration. Referencing non-existing integration Id will cause 'Not Found' error. Integration configuration is validated for each type of the integration before it can be created.   # Integration Configuration  Integration configuration (**'configuration'** field) is the JSON object representing the special configuration per integration type with the connectivity fields and other important parameters dependent on the specific integration type. Let's review the configuration object for the MQTT Integration type below.   ```json {    "clientConfiguration":{       "host":"broker.hivemq.com",       "port":1883,       "cleanSession":false,       "ssl":false,       "connectTimeoutSec":10,       "clientId":"",       "maxBytesInMessage":32368,       "credentials":{          "type":"anonymous"       }    },    "downlinkTopicPattern":"${topic}",    "topicFilters":[       {          "filter":"tb/mqtt-integration-tutorial/sensors/+/temperature",          "qos":0       }    ],    "metadata":{    } } ```  Remove 'id', 'tenantId' from the request body example (below) to create new Integration entity.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Integration body:
:return: Integration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_integration(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_integration'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_jwt_settings(body: Optional[str] = None) -> str:
    """
    Update JWT Settings (saveJwtSettings)  # noqa: E501

Updates the JWT Settings object that contains JWT token policy, etc. The tokenSigningKey field is a Base64 encoded string.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JwtSettings body:
:return: JwtPair
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_jwt_settings(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_jwt_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_login_white_label_params(body: Optional[str] = None) -> str:
    """
    Create Or Update Login White Labeling configuration (saveWhiteLabelParams)  # noqa: E501

Creates or Updates the White Labeling configuration.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param LoginWhiteLabelingParams body:
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: LoginWhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_login_white_label_params(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_login_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_mobile_app(body_json: str, oauth2_client_ids: Optional[str] = None) -> str:
    """
    Save Or update Mobile app (saveMobileApp)  # noqa: E501

Create or update the Mobile app. When creating mobile app, platform generates Mobile App Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Mobile App Id will be present in the response. Specify existing Mobile App Id to update the mobile app. Referencing non-existing Mobile App Id will cause 'Not Found' error.  Mobile app package name is unique for entire platform setup.    Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param MobileApp body: (required)
:param list[object] oauth2_client_ids: A list of entity oauth2 client ids, separated by comma ','
:return: MobileApp
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_mobile_app(body=deserialize_param(body_json, 'MobileApp'), oauth2_client_ids=oauth2_client_ids)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_mobile_app'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_mobile_app_bundle(body_json: str, oauth2_client_ids_json: str = None) -> str:
    """
    Save Or update Mobile app bundle (saveMobileAppBundle)  # noqa: E501

Create or update the Mobile app bundle that represents tha pair of ANDROID and IOS app and mobile settings like oauth2 clients, self-registration and layout configuration.When creating mobile app bundle, platform generates Mobile App Bundle Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Mobile App Bundle Id will be present in the response. Referencing non-existing Mobile App Bundle Id will cause 'Not Found' error.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param MobileAppBundle body: (required)
:param str oauth2_client_ids: A list of oauth2 client ids, separated by comma ','
:return: MobileAppBundle
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_mobile_app_bundle(body=deserialize_param(body_json, 'MobileAppBundle'), oauth2_client_ids=json.loads(oauth2_client_ids_json) if oauth2_client_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_mobile_app_bundle'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_mobile_app_settings(body_json: str) -> str:
    """
    Create Or Update the Mobile application settings (saveMobileAppSettings)  # noqa: E501

The request payload contains configuration for android/iOS applications and platform qr code widget settings.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param MobileAppSettings body: (required)
:return: MobileAppSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_mobile_app_settings(body=deserialize_param(body_json, 'MobileAppSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_mobile_app_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_mobile_session(x_mobile_token: str, body_json: str) -> str:
    """
    saveMobileSession  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str x_mobile_token: X-Mobile-Token (required)
:param MobileSessionInfo body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_mobile_session(x_mobile_token=x_mobile_token, body=deserialize_param(body_json, 'MobileSessionInfo'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_mobile_session'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_notification_rule(body_json: str) -> str:
    """
    Save notification rule (saveNotificationRule)  # noqa: E501

Creates or updates notification rule.   Mandatory properties are `name`, `templateId` (of a template with `notificationType` matching to rule's `triggerType`), `triggerType`, `triggerConfig` and `recipientConfig`. Additionally, you may specify rule `description` inside of `additionalConfig`.  Trigger type of the rule cannot be changed. Available trigger types for tenant: `ENTITY_ACTION`, `ALARM`, `ALARM_COMMENT`, `ALARM_ASSIGNMENT`, `DEVICE_ACTIVITY`, `RULE_ENGINE_COMPONENT_LIFECYCLE_EVENT`. For sysadmin, there are following trigger types available: `ENTITIES_LIMIT`, `API_USAGE_LIMIT`, `NEW_PLATFORM_VERSION`.  Here is an example of notification rule to send notification when a device, asset or customer is created or deleted: ```json {   "name": "Entity action",   "templateId": {     "entityType": "NOTIFICATION_TEMPLATE",     "id": "32117320-d785-11ed-a06c-21dd57dd88ca"   },   "triggerType": "ENTITY_ACTION",   "triggerConfig": {     "entityTypes": [       "CUSTOMER",       "DEVICE",       "ASSET"     ],     "created": true,     "updated": false,     "deleted": true,     "triggerType": "ENTITY_ACTION"   },   "recipientsConfig": {     "targets": [       "320f2930-d785-11ed-a06c-21dd57dd88ca"     ],     "triggerType": "ENTITY_ACTION"   },   "additionalConfig": {     "description": "Send notification to tenant admins or customer users when a device, asset or customer is created"   },   "templateName": "Entity action notification",   "deliveryMethods": [     "WEB"   ] } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationRule body:
:return: NotificationRule
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_notification_rule(body=deserialize_param(body_json, 'NotificationRule'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_notification_rule'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_notification_settings(body_json: str) -> str:
    """
    Save notification settings (saveNotificationSettings)  # noqa: E501

Saves notification settings for this tenant or sysadmin. `deliveryMethodsConfigs` of the settings must be specified.  Here is an example of the notification settings with Slack configuration: ```json {   "deliveryMethodsConfigs": {     "SLACK": {       "method": "SLACK",       "botToken": "xoxb-...."     }   } } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationSettings body:
:return: NotificationSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_notification_settings(body=deserialize_param(body_json, 'NotificationSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_notification_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_notification_target(body_json: str) -> str:
    """
    Save notification target (saveNotificationTarget)  # noqa: E501

Creates or updates notification target.  Available `configuration` types are `PLATFORM_USERS` and `SLACK`. For `PLATFORM_USERS` the `usersFilter` must be specified. For tenant, there are following users filter types available: `USER_LIST`, `CUSTOMER_USERS`, `TENANT_ADMINISTRATORS`, `ALL_USERS`, `ORIGINATOR_ENTITY_OWNER_USERS`, `AFFECTED_USER`. For sysadmin: `TENANT_ADMINISTRATORS`, `AFFECTED_TENANT_ADMINISTRATORS`, `SYSTEM_ADMINISTRATORS`, `ALL_USERS`.  Here is an example of tenant-level notification target to send notification to customer's users: ```json {   "name": "Users of Customer A",   "configuration": {     "type": "PLATFORM_USERS",     "usersFilter": {       "type": "CUSTOMER_USERS",       "customerId": "32499a20-d785-11ed-a06c-21dd57dd88ca"     },     "description": "Users of Customer A"   } } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationTarget body:
:return: NotificationTarget
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_notification_target(body=deserialize_param(body_json, 'NotificationTarget'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_notification_target'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_notification_template(body_json: str) -> str:
    """
    Save notification template (saveNotificationTemplate)  # noqa: E501

Creates or updates notification template.  Here is an example of template to send notification via Web, SMS and Slack: ```json {   "name": "Greetings",   "notificationType": "GENERAL",   "configuration": {     "deliveryMethodsTemplates": {       "WEB": {         "enabled": true,         "subject": "Greetings",         "body": "Hi there, ${recipientTitle}",         "additionalConfig": {           "icon": {             "enabled": true,             "icon": "back_hand",             "color": "#757575"           },           "actionButtonConfig": {             "enabled": false           }         },         "method": "WEB"       },       "SMS": {         "enabled": true,         "body": "Hi there, ${recipientTitle}",         "method": "SMS"       },       "SLACK": {         "enabled": true,         "body": "Hi there, @${recipientTitle}",         "method": "SLACK"       }     }   } } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param NotificationTemplate body:
:return: NotificationTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_notification_template(body=deserialize_param(body_json, 'NotificationTemplate'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_notification_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_o_auth2_client(body: Optional[str] = None) -> str:
    """
    Save OAuth2 Client (saveOAuth2Client)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param OAuth2Client body: (required)
:return: OAuth2Client
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_o_auth2_client(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_o_auth2_client'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_o_auth2_info(body: Optional[str] = None) -> str:
    """
    Save OAuth2 settings (saveOAuth2Info)  # noqa: E501

  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param OAuth2Info body:
:return: OAuth2Info
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_o_auth2_info(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_o_auth2_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_ota_package_data(ota_package_id_json: str, checksum: Optional[str] = None, checksum_algorithm: Optional[str] = None, file: Optional[str] = None) -> str:
    """
    Save OTA Package data (saveOtaPackageData)  # noqa: E501

Update the OTA Package. Adds the date to the existing OTA Package Info  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str ota_package_id: A string value representing the ota package id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str checksum:
:param str checksum_algorithm:
:param str file:
:return: OtaPackageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_ota_package_data(ota_package_id=deserialize_param(ota_package_id_json, 'OtaPackageId'), checksum=checksum, checksum_algorithm=checksum_algorithm, file=file)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_ota_package_data'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_ota_package_info(body: Optional[str] = None) -> str:
    """
    Create Or Update OTA Package Info (saveOtaPackageInfo)  # noqa: E501

Create or update the OTA Package Info. When creating OTA Package Info, platform generates OTA Package id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created OTA Package id will be present in the response. Specify existing OTA Package id to update the OTA Package Info. Referencing non-existing OTA Package Id will cause 'Not Found' error.   OTA Package combination of the title with the version is unique in the scope of tenant.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param SaveOtaPackageInfoRequest body:
:return: OtaPackageInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_ota_package_info(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_ota_package_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_platform_two_fa_settings(body_json: str) -> str:
    """
    Save platform 2FA settings (savePlatformTwoFaSettings)  # noqa: E501

Save 2FA settings for platform. The settings have following properties: - `providers` - the list of 2FA providers' configs. Users will only be allowed to use 2FA providers from this list.   - `minVerificationCodeSendPeriod` - minimal period in seconds to wait after verification code send request to send next request.  - `verificationCodeCheckRateLimit` - rate limit configuration for verification code checking. The format is standard: 'amountOfRequests:periodInSeconds'. The value of '1:60' would limit verification code checking requests to one per minute. - `maxVerificationFailuresBeforeUserLockout` - maximum number of verification failures before a user gets disabled. - `totalAllowedTimeForVerification` - total amount of time in seconds allotted for verification. Basically, this property sets a lifetime for pre-verification token. If not set, default value of 30 minutes is used.   TOTP 2FA provider config has following settings: - `issuerName` - issuer name that will be displayed in an authenticator app near a username. Must not be blank.  For SMS 2FA provider: - `smsVerificationMessageTemplate` - verification message template.  Available template variables are ${code} and ${userEmail}. It must not be blank and must contain verification code variable. - `verificationCodeLifetime` - verification code lifetime in seconds. Required to be positive.  For EMAIL provider type: - `verificationCodeLifetime` - the same as for SMS.  Example of the settings: ``` {   "providers": [     {       "providerType": "TOTP",       "issuerName": "TB"     },     {       "providerType": "EMAIL",       "verificationCodeLifetime": 60     },     {       "providerType": "SMS",       "verificationCodeLifetime": 60,       "smsVerificationMessageTemplate": "Here is your verification code: ${code}"     }   ],   "minVerificationCodeSendPeriod": 60,   "verificationCodeCheckRateLimit": "4.1.0",   "maxVerificationFailuresBeforeUserLockout": 10,   "totalAllowedTimeForVerification": 600 } ```  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param PlatformTwoFaSettings body:
:return: PlatformTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_platform_two_fa_settings(body=deserialize_param(body_json, 'PlatformTwoFaSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_platform_two_fa_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_qr_code_settings(body_json: str) -> str:
    """
    Create Or Update the Mobile application settings (saveMobileAppSettings)  # noqa: E501

The request payload contains configuration for android/iOS applications and platform qr code widget settings.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param QrCodeSettings body: (required)
:return: QrCodeSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_qr_code_settings(body=deserialize_param(body_json, 'QrCodeSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_qr_code_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_queue(service_type: str, body: Optional[str] = None) -> str:
    """
    Create Or Update Queue (saveQueue)  # noqa: E501

Create or update the Queue. When creating queue, platform generates Queue Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). Specify existing Queue id to update the queue. Referencing non-existing Queue Id will cause 'Not Found' error.  Queue name is unique in the scope of sysadmin. Remove 'id', 'tenantId' from the request body example (below) to create new Queue entity.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str service_type: Service type (implemented only for the TB-RULE-ENGINE) (required)
:param Queue body:
:return: Queue
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_queue(service_type=service_type, body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_queue'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_relation(body: Optional[str] = None) -> str:
    """
    Create Relation (saveRelation)  # noqa: E501

Creates or updates a relation between two entities in the platform. Relations unique key is a combination of from/to entity id and relation type group and relation type.   If the user has the authority of 'System Administrator', the server checks that 'from' and 'to' entities are owned by the sysadmin. If the user has the authority of 'Tenant Administrator', the server checks that 'from' and 'to' entities are owned by the same tenant. If the user has the authority of 'Customer User', the server checks that the 'from' and 'to' entities are assigned to the same customer.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param EntityRelation body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_relation(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_relation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_report_template(body_json: str) -> str:
    """
    Save Report Template (saveReportTemplate)  # noqa: E501

Creates or Updates report template. Report Template extends Report Template Info object and adds 'configuration' - a JSON structure of report template configuration. See the 'Model' tab of the Response Class for more details. When creating report template, platform generates report template Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created report template id will be present in the response. Specify existing report template id to update the report template. Referencing non-existing report template Id will cause 'Not Found' error. Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Report Template entity.   Available for users with 'TENANT_ADMIN' authority.   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param ReportTemplate body: (required)
:return: ReportTemplate
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_report_template(body=deserialize_param(body_json, 'ReportTemplate'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_report_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_repository_settings(body: Optional[str] = None) -> str:
    """
    Creates or Updates the repository settings (saveRepositorySettings)  # noqa: E501

Creates or Updates the repository settings object.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param RepositorySettings body:
:return: DeferredResultRepositorySettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_repository_settings(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_repository_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_resource(body: Optional[str] = None) -> str:
    """
    Create Or Update Resource (saveResource)  # noqa: E501

Create or update the Resource. When creating the Resource, platform generates Resource id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Resource id will be present in the response. Specify existing Resource id to update the Resource. Referencing non-existing Resource Id will cause 'Not Found' error.   Resource combination of the title with the key is unique in the scope of tenant. Remove 'id', 'tenantId' from the request body example (below) to create new Resource entity.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TbResource body:
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_resource(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_resource'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_role(body: Optional[str] = None) -> str:
    """
    Create Or Update Role (saveRole)  # noqa: E501

Creates or Updates the Role. When creating Role, platform generates Role Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Role id will be present in the response. Specify existing Role id to update the permission. Referencing non-existing Group Permission Id will cause 'Not Found' error.  Role Contains a set of permissions. Role has two types. Generic Role may be assigned to the user group and will provide permissions for all entities of a certain type. Group Role may be assigned to both user and entity group and will provides permissions only for the entities that belong to specified entity group. The assignment of the Role to the User Group is done using [Group Permission Controller](/swagger-ui.html#/group-permission-controller).  Example of Generic Role with read-only permissions for any resource and all permissions for the 'DEVICE' and 'PROFILE' resources is listed below:   ```json {   "name": "Read-Only User",   "type": "GENERIC",   "permissions": {     "ALL": [       "READ",       "RPC_CALL",       "READ_CREDENTIALS",       "READ_ATTRIBUTES",       "READ_TELEMETRY"     ],     "DEVICE": [       "ALL"     ]     "PROFILE": [       "ALL"     ]   },   "additionalInfo": {     "description": "Read-only permissions for everything, Write permissions for devices and own profile."   } } ```  Example of Group Role with read-only permissions. Note that the group role has no association with the resources. The type of the resource is taken from the entity group that this role is assigned to:   ```json {   "name": "Entity Group Read-only User",   "type": "GROUP",   "permissions": [     "READ",     "RPC_CALL",     "READ_CREDENTIALS",     "READ_ATTRIBUTES",     "READ_TELEMETRY"   ],   "additionalInfo": {     "description": "Read-only permissions."   } } ```   Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Role body:
:return: Role
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_role(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_role'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_rule_chain(body: str) -> str:
    """
    Create Default Rule Chain  # noqa: E501

Create rule chain from template, based on the specified name in the request. Creates the rule chain based on the template that is used to create root rule chain.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param DefaultRuleChainCreateRequest body:
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_rule_chain(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_rule_chain_meta_data(body: Optional[str] = None, update_related: Optional[str] = None) -> str:
    """
    Update Rule Chain Metadata  # noqa: E501

Updates the rule chain metadata. The metadata object contains information about the rule nodes and their connections.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param RuleChainMetaData body:
:param bool update_related: Update related rule nodes.
:return: RuleChainMetaData
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_rule_chain_meta_data(body=body, update_related=update_related)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_rule_chain_meta_data'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_rule_chain_v1(body: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.save_rule_chain_v1(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_rule_chain_v1'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_scheduler_event(body: Optional[str] = None) -> str:
    """
    Save Scheduler Event (saveSchedulerEvent)  # noqa: E501

Creates or Updates scheduler event. Scheduler Event extends Scheduler Event Info object and adds 'configuration' - a JSON structure of scheduler event configuration. See the 'Model' tab of the Response Class for more details. When creating scheduler event, platform generates scheduler event Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created scheduler event id will be present in the response. Specify existing scheduler event id to update the scheduler event. Referencing non-existing scheduler event Id will cause 'Not Found' error. Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new Scheduler Event entity.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param SchedulerEvent body:
:return: SchedulerEvent
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_scheduler_event(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_scheduler_event'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_secret(body_json: str) -> str:
    """
    Save or Update Secret (saveSecret)  # noqa: E501

Create or update the Secret. When creating secret, platform generates Secret Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Secret Id will be present in the response. Specify existing Secret Id to update the secret. Secret name is not updatable, only value could be changed. Referencing non-existing Secret Id will cause 'Not Found' error.  Secret name is unique in the scope of tenant.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Secret body: (required)
:return: SecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_secret(body=deserialize_param(body_json, 'Secret'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_secret'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_security_settings(body: Optional[str] = None) -> str:
    """
    Update Security Settings (saveSecuritySettings)  # noqa: E501

Updates the Security Settings object that contains password policy, etc.  Available for users with 'SYS_ADMIN' authority.  Security check is performed to verify that the user has 'WRITE' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param SecuritySettings body:
:return: SecuritySettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_security_settings(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_security_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_self_registration_params(body: Optional[str] = None) -> str:
    """
    Create Or Update Self Registration parameters (saveSelfRegistrationParams)  # noqa: E501

Creates or Updates the Self Registration parameters. When creating, platform generates Admin Settings Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Admin Settings Id will be present in the response. Specify existing Admin Settings Id to update the Self Registration parameters. Referencing non-existing Admin Settings Id will cause 'Not Found' error.  Self Registration allows users to signup for using the platform and automatically create a Customer account for them. You may configure default dashboard and user roles that will be assigned for this Customer. This allows you to build out-of-the-box solutions for customers. Ability to white-label the login and main pages helps to brand the platform.  Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param SelfRegistrationParams body:
:return: SelfRegistrationParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_self_registration_params(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_tenant(body: Optional[str] = None) -> str:
    """
    Create Or update Tenant (saveTenant)  # noqa: E501

Create or update the Tenant. When creating tenant, platform generates Tenant Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). Default Rule Chain and Device profile are also generated for the new tenants automatically. The newly created Tenant Id will be present in the response. Specify existing Tenant Id id to update the Tenant. Referencing non-existing Tenant Id will cause 'Not Found' error.Remove 'id', 'tenantId' from the request body example (below) to create new Tenant entity.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param Tenant body:
:return: Tenant
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_tenant(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_tenant'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_tenant_profile(body: Optional[str] = None) -> str:
    """
    Create Or update Tenant Profile (saveTenantProfile)  # noqa: E501

Create or update the Tenant Profile. When creating tenant profile, platform generates Tenant Profile Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Tenant Profile Id will be present in the response. Specify existing Tenant Profile Id id to update the Tenant Profile. Referencing non-existing Tenant Profile Id will cause 'Not Found' error.   Update of the tenant profile configuration will cause immediate recalculation of API limits for all affected Tenants.   The **'profileData'** object is the part of Tenant Profile that defines API limits and Rate limits.   You have an ability to define maximum number of devices ('maxDevice'), assets ('maxAssets') and other entities. You may also define maximum number of messages to be processed per month ('maxTransportMessages', 'maxREExecutions', etc). The '*RateLimit' defines the rate limits using simple syntax. For example, '1000:1,20000:60' means up to 1000 events per second but no more than 20000 event per minute. Let's review the example of tenant profile data below:   ```json {   "name": "Default",   "description": "Default tenant profile",   "isolatedTbRuleEngine": false,   "profileData": {     "configuration": {       "type": "DEFAULT",       "maxDevices": 0,       "maxAssets": 0,       "maxCustomers": 0,       "maxUsers": 0,       "maxDashboards": 0,       "maxRuleChains": 0,       "maxResourcesInBytes": 0,       "maxOtaPackagesInBytes": 0,       "maxResourceSize": 0,       "transportTenantMsgRateLimit": "1000:1,20000:60",       "transportTenantTelemetryMsgRateLimit": "1000:1,20000:60",       "transportTenantTelemetryDataPointsRateLimit": "1000:1,20000:60",       "transportDeviceMsgRateLimit": "20:1,600:60",       "transportDeviceTelemetryMsgRateLimit": "20:1,600:60",       "transportDeviceTelemetryDataPointsRateLimit": "20:1,600:60",       "maxTransportMessages": 10000000,       "maxTransportDataPoints": 10000000,       "maxREExecutions": 4000000,       "maxJSExecutions": 5000000,       "maxDPStorageDays": 0,       "maxRuleNodeExecutionsPerMessage": 50,       "maxEmails": 0,       "maxSms": 0,       "maxCreatedAlarms": 1000,       "defaultStorageTtlDays": 0,       "alarmsTtlDays": 0,       "rpcTtlDays": 0,       "queueStatsTtlDays": 0,       "ruleEngineExceptionsTtlDays": 0,       "warnThreshold": 0     }   },   "default": true } ```Remove 'id', from the request body example (below) to create new Tenant Profile entity.  Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TenantProfile body:
:return: TenantProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_tenant_profile(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_tenant_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_trendz_settings(body_json: str) -> str:
    """
    Save Trendz settings (saveTrendzSettings)  # noqa: E501

Saves Trendz settings for this tenant.   Here is an example of the Trendz settings: ```json {   "enabled": true,   "baseUrl": "https://some.domain.com:18888/also_necessary_prefix" } ```  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TrendzSettings body: (required)
:return: TrendzSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_trendz_settings(body=deserialize_param(body_json, 'TrendzSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_trendz_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_user(body: Optional[str] = None, send_activation_mail: Optional[str] = None, entity_group_id: Optional[str] = None, entity_group_ids_json: str = None) -> str:
    """
    Save Or update User (saveUser)  # noqa: E501

Create or update the User. When creating user, platform generates User Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created User Id will be present in the response. Specify existing User Id to update the device. Referencing non-existing User Id will cause 'Not Found' error.  Device email is unique for entire platform setup.  Remove 'id', 'tenantId' and optionally 'customerId' from the request body example (below) to create new User entity. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param User body:
:param bool send_activation_mail: Send activation email (or use activation link)
:param str entity_group_id: entityGroupId
:param str entity_group_ids: entityGroupIds
:return: User
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_user(body=body, send_activation_mail=send_activation_mail, entity_group_id=entity_group_id, entity_group_ids=json.loads(entity_group_ids_json) if entity_group_ids_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_user'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_user_notification_settings(body_json: str) -> str:
    """
    saveUserNotificationSettings  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param UserNotificationSettings body:
:return: UserNotificationSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_user_notification_settings(body=deserialize_param(body_json, 'UserNotificationSettings'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_user_notification_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_user_settings(body_json: str) -> str:
    """
    Save user settings (saveUserSettings)  # noqa: E501

Save user settings represented in json format for authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_user_settings(body=deserialize_param(body_json, 'JsonNode'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_user_settings'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_web_self_registration_params(body: str) -> str:
    """
    Create Or Update Self Registration parameters (saveSelfRegistrationParams)  # noqa: E501

Creates or Updates the Self Registration parameters. When creating, platform generates Admin Settings Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Admin Settings Id will be present in the response. Specify existing Admin Settings Id to update the Self Registration parameters. Referencing non-existing Admin Settings Id will cause 'Not Found' error.  Self Registration allows users to signup for using the platform and automatically create a Customer account for them. You may configure default dashboard and user roles that will be assigned for this Customer. This allows you to build out-of-the-box solutions for customers. Ability to white-label the login and main pages helps to brand the platform.  Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object body: (required)
:return: WebSelfRegistrationParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_web_self_registration_params(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_web_self_registration_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_white_label_params(body: Optional[str] = None) -> str:
    """
    Create Or Update White Labeling configuration (saveWhiteLabelParams)  # noqa: E501

Creates or Updates the White Labeling configuration.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param WhiteLabelingParams body:
:param str customer_id: A string value representing the customer id. For example, '784f394c-42b6-435a-983c-b7beff2784f9'
:return: WhiteLabelingParams
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_white_label_params(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_white_label_params'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_widget_type(body: Optional[str] = None, update_existing_by_fqn: Optional[str] = None) -> str:
    """
    Create Or Update Widget Type (saveWidgetType)  # noqa: E501

Create or update the Widget Type. Widget Type represents the template for widget creation. Widget Type and Widget are similar to class and object in OOP theory. When creating the Widget Type, platform generates Widget Type Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Widget Type Id will be present in the response. Specify existing Widget Type id to update the Widget Type. Referencing non-existing Widget Type Id will cause 'Not Found' error.  Widget Type fqn is unique in the scope of System or Tenant. Special Tenant Id '13814000-1dd2-11b2-8080-808080808080' is automatically used if the create request is sent by user with 'SYS_ADMIN' authority.Remove 'id', 'tenantId' rom the request body example (below) to create new Widget Type entity.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param WidgetTypeDetails body:
:param bool update_existing_by_fqn: Optional boolean parameter indicating whether to update existing widget type by FQN if present instead of creating new one
:return: WidgetTypeDetails
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_widget_type(body=body, update_existing_by_fqn=update_existing_by_fqn)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_widget_type'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def save_widgets_bundle(body: Optional[str] = None) -> str:
    """
    Create Or Update Widget Bundle (saveWidgetsBundle)  # noqa: E501

Create or update the Widget Bundle. Widget Bundle represents a group(bundle) of widgets. Widgets are grouped into bundle by type or use case.  When creating the bundle, platform generates Widget Bundle Id as [time-based UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier#Version_1_(date-time_and_MAC_address)). The newly created Widget Bundle Id will be present in the response. Specify existing Widget Bundle id to update the Widget Bundle. Referencing non-existing Widget Bundle Id will cause 'Not Found' error.  Widget Bundle alias is unique in the scope of tenant. Special Tenant Id '13814000-1dd2-11b2-8080-808080808080' is automatically used if the create bundle request is sent by user with 'SYS_ADMIN' authority.Remove 'id', 'tenantId' from the request body example (below) to create new Widgets Bundle entity.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param WidgetsBundle body:
:return: WidgetsBundle
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.save_widgets_bundle(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'save_widgets_bundle'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_activation_email(email: str) -> str:
    """
    Send or re-send the activation email  # noqa: E501

Force send the activation email to the user. Useful to resend the email if user has accidentally deleted it.   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str email: Email of the user (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_activation_email(email=email)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_activation_email'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_chat_request(body_json: str) -> str:
    """
    Send request to AI chat model (sendChatRequest)  # noqa: E501

Submits a single prompt - made up of an optional system message and a required user message - to the specified AI chat model and returns either the generated answer or an error envelope.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TbChatRequest body: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_chat_request(body=deserialize_param(body_json, 'TbChatRequest'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_chat_request'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_password_was_reset_email(body: str) -> str:
    """
    sendPasswordWasResetEmail  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: sendPassowordWasResetEmailRequest (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_password_was_reset_email(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_password_was_reset_email'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_reset_password_email(body: str) -> str:
    """
    sendResetPasswordEmail  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: sendResetPasswordEmailRequest (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_reset_password_email(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_reset_password_email'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_test_mail(body: Optional[str] = None) -> str:
    """
    Send test email (sendTestMail)  # noqa: E501

Attempts to send test email using Mail Settings provided as a parameter. Email is sent to the address specified in the profile of user who is performing the requestYou may change the 'To' email in the user profile of the System/Tenant Administrator.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param AdminSettings body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_test_mail(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_test_mail'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def send_test_sms(body: Optional[str] = None) -> str:
    """
    Send test sms (sendTestMail)  # noqa: E501

Attempts to send test sms to the System Administrator User using SMS Settings and phone number provided as a parameters of the request.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'READ' permission for the 'ADMIN_SETTINGS' (for 'SYS_ADMIN' authority) or 'WHITE_LABELING' (for 'TENANT_ADMIN' authority) resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TestSmsRequest body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.send_test_sms(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'send_test_sms'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def setDaemon(daemonic: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.setDaemon(daemonic=daemonic)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'setDaemon'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def setName(name: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.setName(name=name)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'setName'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_auto_assign_to_edge_rule_chain(rule_chain_id_json: str) -> str:
    """
    Set Auto Assign To Edge Rule Chain (setAutoAssignToEdgeRuleChain)  # noqa: E501

Makes the rule chain to be automatically assigned for any new edge that will be created. Does not assign this rule chain for already created edges.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_auto_assign_to_edge_rule_chain(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_auto_assign_to_edge_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_customer_home_dashboard_info(body: Optional[str] = None) -> str:
    """
    Update Customer Home Dashboard Info (setCustomerHomeDashboardInfo)  # noqa: E501

Update the home dashboard assignment for the current customer.   Available for users with 'CUSTOMER_USER' authority.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param HomeDashboardInfo body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_customer_home_dashboard_info(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_customer_home_dashboard_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_default_asset_profile(asset_profile_id_json: str) -> str:
    """
    Make Asset Profile Default (setDefaultAssetProfile)  # noqa: E501

Marks asset profile as default within a tenant scope.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str asset_profile_id: A string value representing the asset profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: AssetProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_default_asset_profile(asset_profile_id=deserialize_param(asset_profile_id_json, 'AssetProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_default_asset_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_default_device_profile(device_profile_id_json: str) -> str:
    """
    Make Device Profile Default (setDefaultDeviceProfile)  # noqa: E501

Marks device profile as default within a tenant scope.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_profile_id: A string value representing the device profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeviceProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_default_device_profile(device_profile_id=deserialize_param(device_profile_id_json, 'DeviceProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_default_device_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_default_tenant_profile(tenant_profile_id_json: str) -> str:
    """
    Make tenant profile default (setDefaultTenantProfile)  # noqa: E501

Makes specified tenant profile to be default. Referencing non-existing tenant profile Id will cause an error.   Available for users with 'SYS_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str tenant_profile_id: A string value representing the tenant profile id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: TenantProfile
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_default_tenant_profile(tenant_profile_id=deserialize_param(tenant_profile_id_json, 'TenantProfileId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_default_tenant_profile'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_edge_root_rule_chain(edge_id_json: str, rule_chain_id_json: str) -> str:
    """
    Set root rule chain for provided edge (setEdgeRootRuleChain)  # noqa: E501

Change root rule chain of the edge to the new provided rule chain.  This operation will send a notification to update root rule chain on remote edge service.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Edge
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_edge_root_rule_chain(edge_id=deserialize_param(edge_id_json, 'EdgeId'), rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_edge_root_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_edge_template_root_rule_chain(rule_chain_id_json: str) -> str:
    """
    Set Edge Template Root Rule Chain (setEdgeTemplateRootRuleChain)  # noqa: E501

Makes the rule chain to be root rule chain for any new edge that will be created. Does not update root rule chain for already created edges.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_edge_template_root_rule_chain(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_edge_template_root_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_root_rule_chain(rule_chain_id_json: str) -> str:
    """
    Set Root Rule Chain (setRootRuleChain)  # noqa: E501

Makes the rule chain to be root rule chain. Updates previous root rule chain as well.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_root_rule_chain(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_root_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_tenant_home_dashboard_info(body: Optional[str] = None) -> str:
    """
    Update Tenant Home Dashboard Info (getTenantHomeDashboardInfo)  # noqa: E501

Update the home dashboard assignment for the current tenant.   Available for users with 'TENANT_ADMIN' authority.  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param HomeDashboardInfo body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_tenant_home_dashboard_info(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_tenant_home_dashboard_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def set_user_credentials_enabled(user_id_json: str, user_credentials_enabled: Optional[str] = None) -> str:
    """
    Enable/Disable User credentials (setUserCredentialsEnabled)  # noqa: E501

Enables or Disables user credentials. Useful when you would like to block user account without deleting it. You can specify parameters to filter the results. The result is wrapped with PageData object that allows you to iterate over result set using pagination. See the 'Model' tab of the Response Class for more details.   Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str user_id: A string value representing the user id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param bool user_credentials_enabled: Enable ("true") or disable ("false") the credentials.
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.set_user_credentials_enabled(user_id=deserialize_param(user_id_json, 'UserId'), user_credentials_enabled=user_credentials_enabled)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'set_user_credentials_enabled'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def share_entity_group(entity_group_id_json: str, body: Optional[str] = None) -> str:
    """
    Share the Entity Group (shareEntityGroup)  # noqa: E501

Share the entity group with certain user group based on the provided Share Group Request. The request is quite flexible and processing of the request involves multiple security checks using platform RBAC feature.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param ShareGroupRequest body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.share_entity_group(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'share_entity_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def share_entity_group_to_child_owner_user_group(entity_group_id_json: str, user_group_id_json: str, role_id_json: str) -> str:
    """
    Share the Entity Group with User group (shareEntityGroupToChildOwnerUserGroup)  # noqa: E501

Share the entity group with specified user group using specified role.   Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for specified group.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str entity_group_id: A string value representing the Entity Group Id that you would like to share. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str user_group_id: A string value representing the Entity(User) Group Id that you would like to share with. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str role_id: A string value representing the Role Id that describes set of permissions you would like to share (read, write, etc). For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.share_entity_group_to_child_owner_user_group(entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'), user_group_id=deserialize_param(user_group_id_json, 'EntityId'), role_id=deserialize_param(role_id_json, 'RoleId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'share_entity_group_to_child_owner_user_group'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v11_post11(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v11_post11(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v11_post11'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_delete3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_delete3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_delete3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_get3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_get3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_get3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_head3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_head3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_head3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_options3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_options3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_options3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_patch3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_patch3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_patch3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sig_fox_process_request_v3_put3(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.sig_fox_process_request_v3_put3(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sig_fox_process_request_v3_put3'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sign_up(body: Optional[str] = None) -> str:
    """
    User Sign Up (signUp)  # noqa: E501

Process user sign up request. Creates the Customer and corresponding User based on self Registration parameters for the domain. See [Self Registration Controller](/swagger-ui.html#/self-registration-controller) for more details.  The result is either 'SUCCESS' or 'INACTIVE_USER_EXISTS'. If Success, the user will receive an email with instruction to activate the account. The content of the email is customizable via the mail templates.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param SignUpRequest body:
:return: str
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.sign_up(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sign_up'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def start() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.start()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'start'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def stop() -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.stop()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'stop'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def submit_two_fa_account_config(body_json: str) -> str:
    """
    Submit 2FA account config (submitTwoFaAccountConfig)  # noqa: E501

Submit 2FA account config to prepare for a future verification. Basically, this method will send a verification code for a given account config, if this has sense for a chosen 2FA provider. This code is needed to then verify and save the account config.  Example of EMAIL 2FA account config: ``` {   "providerType": "EMAIL",   "useByDefault": true,   "email": "separate-email-for-2fa@thingsboard.org" } ```  Example of SMS 2FA account config: ``` {   "providerType": "SMS",   "useByDefault": false,   "phoneNumber": "+38012312321" } ```  For TOTP this method does nothing.  Will throw an error (Bad Request) if submitted account config is not valid, or if the provider is not configured for usage.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TwoFaAccountConfig body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.submit_two_fa_account_config(body=deserialize_param(body_json, 'TwoFaAccountConfig'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'submit_two_fa_account_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def subscribe_to_attributes(device_token: str, timeout: Optional[str] = None) -> str:
    """
    Subscribe to attribute updates (subscribeToAttributes) (Deprecated)  # noqa: E501

Subscribes to client and shared scope attribute updates using http long polling. Deprecated, since long polling is resource and network consuming. Consider using MQTT or CoAP protocol for light-weight real-time updates.   The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param int timeout: Optional timeout of the long poll. Typically less then 60 seconds, since limited on the server side.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.subscribe_to_attributes(device_token=device_token, timeout=timeout)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'subscribe_to_attributes'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def subscribe_to_commands(device_token: str, timeout: Optional[str] = None) -> str:
    """
    Subscribe to RPC commands (subscribeToCommands) (Deprecated)  # noqa: E501

Subscribes to RPC commands using http long polling. Deprecated, since long polling is resource and network consuming. Consider using MQTT or CoAP protocol for light-weight real-time updates.   The API call is designed to be used by device firmware and requires device access token ('deviceToken'). It is not recommended to use this API call by third-party scripts, rule-engine or platform widgets (use 'Telemetry Controller' instead).   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str device_token: Your device access token. (required)
:param int timeout: Optional timeout of the long poll. Typically less then 60 seconds, since limited on the server side.
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.subscribe_to_commands(device_token=device_token, timeout=timeout)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'subscribe_to_commands'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def sync_edge(edge_id_json: str) -> str:
    """
    Sync edge (syncEdge)  # noqa: E501

Starts synchronization process between edge and cloud.  All entities that are assigned to particular edge are going to be send to remote edge service.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: DeferredResultResponseEntity
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.sync_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'sync_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v12_post12(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v12_post12(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v12_post12'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_delete4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_delete4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_delete4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_get4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_get4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_get4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_head4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_head4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_head4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_options4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_options4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_options4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_patch4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_patch4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_patch4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def t_mobile_iot_cdp_process_request_v4_put4(body: str, request_headers: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.t_mobile_iot_cdp_process_request_v4_put4(body=body, request_headers=request_headers, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 't_mobile_iot_cdp_process_request_v4_put4'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def tenant_has_billing_read() -> str:
    """
    tenantHasBillingRead  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.tenant_has_billing_read()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'tenant_has_billing_read'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def tenant_has_billing_write() -> str:
    """
    tenantHasBillingWrite  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.tenant_has_billing_write()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'tenant_has_billing_write'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def tenant_has_white_label_read() -> str:
    """
    tenantHasWhiteLabelRead  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.tenant_has_white_label_read()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'tenant_has_white_label_read'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def tenant_has_white_label_write() -> str:
    """
    tenantHasWhiteLabelWrite  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.tenant_has_white_label_write()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'tenant_has_white_label_write'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def tenant_white_labeling_allowed() -> str:
    """
    tenantWhiteLabelingAllowed  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.tenant_white_labeling_allowed()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'tenant_white_labeling_allowed'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def terms_of_use_accepted() -> str:
    """
    Check Terms Of User (termsOfUseAccepted)  # noqa: E501

Checks that current user accepted the privacy policy.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:return: bool
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.terms_of_use_accepted()
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'terms_of_use_accepted'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def test_down_link_converter(body_json: str = None, script_lang: Optional[str] = None) -> str:
    """
    Test converter function (testDownLinkConverter)  # noqa: E501

Returns a JSON object representing the result of the processed incoming message.   ## Request Body Example  ```json {    "metadata":{       "data":"40"    },    "msg":"{\\n    \\"temp\\": 42,\\n    \\"humidity\\": 77\\n}",    "msgType":"POST_TELEMETRY_REQUEST",    "integrationMetadata":{       "integrationName":"Integration"    },    "encoder":"// Encode downlink data from incoming Rule Engine message\\n\\n// msg - JSON message payload downlink message json\\n// msgType - type of message, for ex. 'ATTRIBUTES_UPDATED', 'POST_TELEMETRY_REQUEST', etc.\\n// metadata - list of key-value pairs with additional data about the message\\n// integrationMetadata - list of key-value pairs with additional data defined in Integration executing this converter\\n\\n/** Encoder **/\\n\\nvar data = {};\\n\\n// Process data from incoming message and metadata\\n\\ndata.tempValue = msg.temp;\\ndata.humValue = msg.humidity;\\n\\ndata.devSerialNumber = metadata['ss_serialNumber'];\\n\\n// Result object with encoded downlink payload\\nvar result = {\\n\\n    // downlink data content type: JSON, TEXT or BINARY (base64 format)\\n    contentType: \\"JSON\\",\\n\\n    // downlink data\\n    data: JSON.stringify(data),\\n\\n    // Optional metadata object presented in key/value format\\n    metadata: {\\n            topic: metadata['deviceType']+'/'+metadata['deviceName']+'/upload'\\n    }\\n\\n};\\n\\nreturn result;" } ```   * 'metadata' - message metadata pushed from the rule engine;   * 'msg' - message data pushed from the rule engine;   * 'msgType' - type of the message pushed from the rule engine;   * 'integrationMetadata' - integration metadata object;   * 'encoder' - string representation of the encoder configuration.  ## Response Body Example  ```json {    "contentType":"JSON",    "data":"{\\"tempValue\\":42,\\"humValue\\":77}",    "metadata":{       "topic":"sensor/Temp Sensor/upload"    } } ```   * 'contentType' - downlink data content type;   * 'data' - downlink data;   * 'metadata' - optional metadata object.    # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:param str script_lang: Script language: JS or TBEL
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.test_down_link_converter(body=json.loads(body_json) if body_json else None, script_lang=script_lang)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'test_down_link_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def test_script(body: str) -> str:
    """
    Test Script function  # noqa: E501

Execute the Script function and return the result. The format of request:   ```json {   "script": "Your Function as String",   "scriptType": "One of: update, generate, filter, switch, json, string",   "argNames": ["msg", "metadata", "type"],   "msg": "{\\"temperature\\": 42}",    "metadata": {     "deviceName": "Device A",     "deviceType": "Thermometer"   },   "msgType": "POST_TELEMETRY_REQUEST" } ```   Expected result JSON contains "output" and "error".  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:param str script_lang: Script language: JS or TBEL
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.test_script(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'test_script'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def test_up_link_converter(body_json: str = None, script_lang: Optional[str] = None) -> str:
    """
    Test converter function (testUpLinkConverter)  # noqa: E501

Returns a JSON object representing the result of the processed incoming message.   ## Request Body Example  ```json {    "metadata":{    },    "payload":"ewogICAgImRhdGEiOiAiZGF0YSIKfQ==",    "decoder":"// Decode an uplink message from a buffer\\n// payload - array of bytes\\n// metadata - key/value object\\n\\n/** Decoder **/\\n\\n// decode payload to string\\nvar payloadStr = decodeToString(payload);\\n\\n// decode payload to JSON\\n// var data = decodeToJson(payload);\\n\\nvar deviceName = 'Device A';\\nvar deviceType = 'thermostat';\\nvar customerName = 'customer';\\nvar groupName = 'thermostat devices';\\nvar manufacturer = 'Example corporation';\\n// use assetName and assetType instead of deviceName and deviceType\\n// to automatically create assets instead of devices.\\n// var assetName = 'Asset A';\\n// var assetType = 'building';\\n\\n// Result object with device/asset attributes/telemetry data\\nvar result = {\\n// Use deviceName and deviceType or assetName and assetType, but not both.\\n   deviceName: deviceName,\\n   deviceType: deviceType,\\n// assetName: assetName,\\n// assetType: assetType,\\n   customerName: customerName,\\n   groupName: groupName,\\n   attributes: {\\n       model: 'Model A',\\n       serialNumber: 'SN111',\\n       integrationName: metadata['integrationName']\\n       manufacturer: manufacturer\\n   },\\n   telemetry: {\\n       temperature: 42,\\n       humidity: 80,\\n       rawData: payloadStr\\n   }\\n};\\n\\n/** Helper functions **/\\n\\nfunction decodeToString(payload) {\\n   return String.fromCharCode.apply(String, payload);\\n}\\n\\nfunction decodeToJson(payload) {\\n   // covert payload to string.\\n   var str = decodeToString(payload);\\n\\n   // parse string to JSON\\n   var data = JSON.parse(str);\\n   return data;\\n}\\n\\nreturn result;" } ```   * 'metadata' - integration metadata;   * 'payload' - base64 string representation of the data;   * 'decoder' - string representation of the decoder configuration.  ## Response Body Example  ```json {    "output":"{\\"deviceName\\":\\"Device A\\",\\"deviceType\\":\\"thermostat\\",\\"customerName\\":\\"customer\\",\\"groupName\\":\\"thermostat devices\\",\\"attributes\\":{\\"model\\":\\"Model A\\",\\"serialNumber\\":\\"SN111\\"},\\"telemetry\\":{\\"temperature\\":42,\\"humidity\\":80,\\"rawData\\":\\"{\\\\n    \\\\\\"data\\\\\\": \\\\\\"data\\\\\\"\\\\n}\\"}}",    "error":"" } ```   * 'output' - string representation of the output message;   * 'error' - string representation of the error message.    # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param JsonNode body:
:param str script_lang: Script language: JS or TBEL
:return: JsonNode
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.test_up_link_converter(body=json.loads(body_json) if body_json else None, script_lang=script_lang)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'test_up_link_converter'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_delete(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_delete(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_delete'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_get(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_get(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_get'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_head(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_head(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_head'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_options(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_options(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_options'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_patch(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_patch(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_patch'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_post(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_post(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_post'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_tpe_put(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_tpe_put(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_tpe_put'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v13_post13(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v13_post13(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v13_post13'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_delete5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_delete5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_delete5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_get5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_get5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_get5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_head5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_head5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_head5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_options5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_options5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_options5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_patch5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_patch5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_patch5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def thing_park_process_request_v5_put5(body: str, request_headers: str, all_request_params: str, routing_key: str) -> str:
    """
    No description available.
    """
    try:
        client = get_client()
        result = client.thing_park_process_request_v5_put5(body=body, request_headers=request_headers, all_request_params=all_request_params, routing_key=routing_key)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'thing_park_process_request_v5_put5'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unassign_alarm(id_json: str) -> str:
    """
    Unassign Alarm (unassignAlarm)  # noqa: E501

Unassign the Alarm. Once unassigned, the 'assign_ts' field will be set to current timestamp and special rule chain event 'ALARM_UNASSIGNED' will be generated. Referencing non-existing Alarm Id will cause an error.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str alarm_id: A string value representing the alarm id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: Alarm
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unassign_alarm(id=deserialize_param(id_json, 'AlarmId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unassign_alarm'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unassign_entity_group_from_edge(edge_id_json: str, group_type: str, entity_group_id_json: str) -> str:
    """
    Unassign entity group from edge (unassignEntityGroupFromEdge)  # noqa: E501

Clears assignment of the entity group to the edge. Unassignment works in async way - first, 'unassign' notification event pushed to edge queue on platform. Second, remote edge service will receive an 'unassign' command to remove entity group (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once 'unassign' command will be delivered to edge service, it's going to remove entity group and entities inside this group locally.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str group_type: EntityGroup type (required)
:param str entity_group_id: A string value representing the Entity Group Id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: EntityGroup
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unassign_entity_group_from_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), group_type=group_type, entity_group_id=deserialize_param(entity_group_id_json, 'EntityGroupId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unassign_entity_group_from_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unassign_integration_from_edge(edge_id_json: str, integration_id_json: str) -> str:
    """
    Unassign integration from edge (unassignIntegrationFromEdge)  # noqa: E501

Clears assignment of the integration to the edge. Unassignment works in async way - first, 'unassign' notification event pushed to edge queue on platform. Second, remote edge service will receive an 'unassign' command to remove integration (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once 'unassign' command will be delivered to edge service, it's going to remove integration locally.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: edgeId (required)
:param str integration_id: integrationId (required)
:return: Integration
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unassign_integration_from_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), integration_id=deserialize_param(integration_id_json, 'IntegrationId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unassign_integration_from_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unassign_rule_chain_from_edge(edge_id_json: str, rule_chain_id_json: str) -> str:
    """
    Unassign rule chain from edge (unassignRuleChainFromEdge)  # noqa: E501

Clears assignment of the rule chain to the edge. Unassignment works in async way - first, 'unassign' notification event pushed to edge queue on platform. Second, remote edge service will receive an 'unassign' command to remove rule chain (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once 'unassign' command will be delivered to edge service, it's going to remove rule chain locally.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: edgeId (required)
:param str rule_chain_id: ruleChainId (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unassign_rule_chain_from_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unassign_rule_chain_from_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unassign_scheduler_event_from_edge(edge_id_json: str, scheduler_event_id_json: str) -> str:
    """
    Unassign scheduler event from edge (unassignSchedulerEventFromEdge)  # noqa: E501

Clears assignment of the scheduler event to the edge. Unassignment works in async way - first, 'unassign' notification event pushed to edge queue on platform. Second, remote edge service will receive an 'unassign' command to remove entity group (Edge will receive this instantly, if it's currently connected, or once it's going to be connected to platform). Third, once 'unassign' command will be delivered to edge service, it's going to remove entity group and entities inside this group locally.  Available for users with 'TENANT_ADMIN' or 'CUSTOMER_USER' authority. Security check is performed to verify that the user has 'WRITE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str edge_id: A string value representing the edge id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param str scheduler_event_id: A string value representing the scheduler id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: SchedulerEventInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unassign_scheduler_event_from_edge(edge_id=deserialize_param(edge_id_json, 'EdgeId'), scheduler_event_id=deserialize_param(scheduler_event_id_json, 'SchedulerEventId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unassign_scheduler_event_from_edge'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def uninstall_solution_template(solution_template_id: str) -> str:
    """
    Uninstall Solution Template (uninstallSolutionTemplate)  # noqa: E501

Uninstall solution template based on the provided id   Security check is performed to verify that the user has 'DELETE' permission for the entity (entities).  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str solution_template_id: A string value representing the solution template id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.uninstall_solution_template(solution_template_id=solution_template_id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'uninstall_solution_template'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def unset_auto_assign_to_edge_rule_chain(rule_chain_id_json: str) -> str:
    """
    Unset Auto Assign To Edge Rule Chain (unsetAutoAssignToEdgeRuleChain)  # noqa: E501

Removes the rule chain from the list of rule chains that are going to be automatically assigned for any new edge that will be created. Does not unassign this rule chain for already assigned edges.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str rule_chain_id: A string value representing the rule chain id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: RuleChain
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.unset_auto_assign_to_edge_rule_chain(rule_chain_id=deserialize_param(rule_chain_id_json, 'RuleChainId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'unset_auto_assign_to_edge_rule_chain'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_custom_menu_assignee_list(id_json: str, assignee_type: str, body_json: str, force: Optional[str] = None) -> str:
    """
    Update custom menu assignee list (updateCustomMenuAssigneeList)  # noqa: E501

The api designed to update the list of assignees or assignee type based on the provided Custom Menu Id. To change assignee type, put new assignee type in path parameter.  Security check is performed to verify that the user has 'WRITE' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: (required)
:param str assignee_type: (required)
:param list[str] body:
:param bool force: Use force if you want to override default menu
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_custom_menu_assignee_list(id=deserialize_param(id_json, 'CustomMenuId'), assignee_type=assignee_type, body=json.loads(body_json) if body_json else None, force=force)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_custom_menu_assignee_list'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_custom_menu_config(id_json: str, body_json: str) -> str:
    """
    Update Custom Menu configuration based on the provided Custom Menu Id (updateCustomMenuConfig)  # noqa: E501

  Security check is performed to verify that the user has 'WRITE' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param CustomMenuConfig body: (required)
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: CustomMenu
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_custom_menu_config(id=deserialize_param(id_json, 'CustomMenuId'), body=deserialize_param(body_json, 'CustomMenuConfig'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_custom_menu_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_custom_menu_name(id_json: str, body: str) -> str:
    """
    Update Custom Menu name based on the provided Custom Menu Id (updateCustomMenuName)  # noqa: E501

  Security check is performed to verify that the user has 'WRITE' permission for the custom menu with specified id.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str body: (required)
:param str custom_menu_id: A string value representing the custom menu id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_custom_menu_name(id=deserialize_param(id_json, 'CustomMenuId'), body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_custom_menu_name'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_device_credentials(body: Optional[str] = None) -> str:
    """
    Update device credentials (updateDeviceCredentials)  # noqa: E501

During device creation, platform generates random 'ACCESS_TOKEN' credentials. Use this method to update the device credentials. First use 'getDeviceCredentialsByDeviceId' to get the credentials id and value. Then use current method to update the credentials type and value. It is not possible to create multiple device credentials for the same device. The structure of device credentials id and value is simple for the 'ACCESS_TOKEN' but is much more complex for the 'MQTT_BASIC' or 'LWM2M_CREDENTIALS'. You may find the example of device with different type of credentials below:   - Credentials type: <b>"Access token"</b> with <b>device ID</b> and with <b>device ID</b> below:   ```json {   "id": {     "id":"c886a090-168d-11ee-87c9-6f157dbc816a"    },   "deviceId": {     "id":"c5fb3ac0-168d-11ee-87c9-6f157dbc816a",     "entityType":"DEVICE"    },   "credentialsType": "ACCESS_TOKEN",   "credentialsId": "6hmxew8pmmzng4e3une4" } ```  - Credentials type: <b>"X509"</b> with <b>device profile ID</b> below:   Note: <b>credentialsId</b> -  format <b>Sha3Hash</b>, <b>certificateValue</b> - format <b>PEM</b> (with "--BEGIN CERTIFICATE----" and  -"----END CERTIFICATE-").  ```json {   "id": {     "id":"309bd9c0-14f4-11ee-9fc9-d9b7463abb63"    },   "deviceId": {     "id":"3092b200-14f4-11ee-9fc9-d9b7463abb63",     "entityType":"DEVICE"    },   "credentialsType": "X509_CERTIFICATE",   "credentialsId": "6b8adb49015500e51a527acd332b51684ab9b49b4ade03a9582a44c455e2e9b6",   "credentialsValue": "-----BEGIN CERTIFICATE-----  -----END CERTIFICATE-----" } ```  - Credentials type: <b>"MQTT_BASIC"</b> with <b>device profile ID</b> below:   ```json {   "id": {     "id":"d877ffb0-14f5-11ee-9fc9-d9b7463abb63"    },   "deviceId": {     "id":"d875dcd0-14f5-11ee-9fc9-d9b7463abb63",     "entityType":"DEVICE"    },   "credentialsType": "MQTT_BASIC",   "credentialsValue": "{\\"clientId\\":\\"juy03yv4owqxcmqhqtvk\\",\\"userName\\":\\"ov19fxca0cyjn7lm7w7u\\",\\"password\\":\\"twy94he114dfi9usyk1o\\"}" } ```  - You may find the example of <b>LwM2M</b> device and <b>RPK</b> credentials below:   Note: LwM2M device - only existing device profile ID (Transport configuration -> Transport type: "LWM2M".  ```json {   "id": {     "id":"e238d4d0-1689-11ee-98c6-1713c1be5a8e"    },   "deviceId": {     "id":"e232e160-1689-11ee-98c6-1713c1be5a8e",     "entityType":"DEVICE"    },   "credentialsType": "LWM2M_CREDENTIALS",   "credentialsId": "LwRpk00000000",   "credentialsValue":        "{\\"client\\":{ \\"endpoint\\":\\"LwRpk00000000\\", \\"securityConfigClientMode\\":\\"RPK\\", \\"key\\":\\"MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEdvBZZ2vQRK9wgDhctj6B1c7bxR3Z0wYg1+YdoYFnVUKWb+rIfTTyYK9tmQJx5Vlb5fxdLnVv1RJOPiwsLIQbAA==\\"   }, \\"bootstrap\\":{ \\"bootstrapServer\\":{ \\"securityMode\\":\\"RPK\\", \\"clientPublicKeyOrId\\":\\"MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEUEBxNl/RcYJNm8mk91CyVXoIJiROYDlXcSSqK6e5bDHwOW4ZiN2lNnXalyF0Jxw8MbAytnDMERXyAja5VEMeVQ==\\", \\"clientSecretKey\\":\\"MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQgd9GAx7yZW37autew5KZykn4IgRpge/tZSjnudnZJnMahRANCAARQQHE2X9Fxgk2byaT3ULJVeggmJE5gOVdxJKorp7lsMfA5bhmI3aU2ddqXIXQnHDwxsDK2cMwRFfICNrlUQx5V\\"}, \\"lwm2mServer\\":{ \\"securityMode\\":\\"RPK\\", \\"clientPublicKeyOrId\\":\\"MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEUEBxNl/RcYJNm8mk91CyVXoIJiROYDlXcSSqK6e5bDHwOW4ZiN2lNnXalyF0Jxw8MbAytnDMERXyAja5VEMeVQ==\\", \\"clientSecretKey\\":\\"MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQgd9GAx7yZW37autew5KZykn4IgRpge/tZSjnudnZJnMahRANCAARQQHE2X9Fxgk2byaT3ULJVeggmJE5gOVdxJKorp7lsMfA5bhmI3aU2ddqXIXQnHDwxsDK2cMwRFfICNrlUQx5V\\"}} }" } ```  Update to real value:  - 'id' (this is id of Device Credentials ->  "Get Device Credentials (getDeviceCredentialsByDeviceId)",  - 'deviceId.id' (this is id of Device). Remove 'tenantId' and optionally 'customerId' from the request body example (below) to create new Device entity.  Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param DeviceCredentials body:
:return: DeviceCredentials
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_device_credentials(body=body)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_device_credentials'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_image(_type: str, key: str, file: str) -> str:
    """
    updateImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param str file:
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_image(_type=_type, key=key, file=file)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_image_info(_type: str, key: str, body_json: str) -> str:
    """
    updateImageInfo  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param TbResourceInfo body:
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_image_info(_type=_type, key=key, body=deserialize_param(body_json, 'TbResourceInfo'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_image_info'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_image_public_status(_type: str, key: str, is_public: Optional[str] = None) -> str:
    """
    updateImagePublicStatus  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str type: Type of the image: tenant or system (required)
:param str key: Image resource key, for example thermostats_dashboard_background.jpeg (required)
:param bool is_public: isPublic (required)
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_image_public_status(_type=_type, key=key, is_public=is_public)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_image_public_status'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_oauth2_clients(body_json: str, id: str) -> str:
    """
    Update oauth2 clients (updateOauth2Clients)  # noqa: E501

Update oauth2 clients of the specified mobile app.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param list[str] body: (required)
:param str id: (required)
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_oauth2_clients(body=json.loads(body_json) if body_json else None, id=id)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_oauth2_clients'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_secret_description(id_json: str, description: str) -> str:
    """
    Update Secret Description  # noqa: E501

Updates the description of the existing Secret by secretId. Only the description can be updated. Referencing a non-existing Secret Id will cause a 'Not Found' error.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str id: Unique identifier of the Secret to update (required)
:param object body:
:return: SecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_secret_description(id=deserialize_param(id_json, 'SecretId'), description=description)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_secret_description'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_secret_value(id_json: str, value: str) -> str:
    """
    Update Secret value  # noqa: E501

Updates the value of the existing Secret by secretId. Referencing a non-existing Secret Id will cause a 'Not Found' error.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param object body: (required)
:param str id: Unique identifier of the Secret to update (required)
:return: SecretInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_secret_value(id=deserialize_param(id_json, 'SecretId'), value=value)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_secret_value'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_two_fa_account_config(provider_type: str, body_json: str) -> str:
    """
    Update 2FA account config (updateTwoFaAccountConfig)  # noqa: E501

Update config for a given provider type.  Update request example: ``` {   "useByDefault": true } ``` Returns whole account's 2FA settings object.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str provider_type: providerType (required)
:param TwoFaAccountConfigUpdateRequest body:
:return: AccountTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_two_fa_account_config(provider_type=provider_type, body=deserialize_param(body_json, 'TwoFaAccountConfigUpdateRequest'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_two_fa_account_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_widgets_bundle_widget_fqns(widgets_bundle_id_json: str, body_json: str) -> str:
    """
    Update widgets bundle widgets list from widget type FQNs list (updateWidgetsBundleWidgetFqns)  # noqa: E501

Updates widgets bundle widgets list from widget type FQNs list.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: A string value representing the widget bundle id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_widgets_bundle_widget_fqns(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_widgets_bundle_widget_fqns'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def update_widgets_bundle_widget_types(widgets_bundle_id_json: str, body_json: str) -> str:
    """
    Update widgets bundle widgets types list (updateWidgetsBundleWidgetTypes)  # noqa: E501

Updates widgets bundle widgets list.  Available for users with 'SYS_ADMIN' or 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str widgets_bundle_id: A string value representing the widget bundle id. For example, '784f394c-42b6-435a-983c-b7beff2784f9' (required)
:param list[str] body:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.update_widgets_bundle_widget_types(widgets_bundle_id=deserialize_param(widgets_bundle_id_json, 'WidgetsBundleId'), body=json.loads(body_json) if body_json else None)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'update_widgets_bundle_widget_types'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def upload_custom_translation(locale_code: str, file: str) -> str:
    """
    Upload Custom Translation (uploadCustomTranslation)  # noqa: E501

Upload the Custom Translation for specified locale.   Request example:   ```json {"home":"MyHome"} ```  Security check is performed to verify that the user has 'WRITE' permission for the white labeling resource.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str locale_code: Locale code (e.g. 'en_US'). (required)
:param str file:
:return: None
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.upload_custom_translation(locale_code=locale_code, file=file)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'upload_custom_translation'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def upload_image(title: str, file: str) -> str:
    """
    uploadImage  # noqa: E501

This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str file:
:param str title:
:return: TbResourceInfo
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.upload_image(title=title, file=file)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'upload_image'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def validate_calculated_field_reprocessing(id_json: str) -> str:
    """
    Validate reprocessing capability of a calculated field (validateCalculatedFieldReprocessing)  # noqa: E501

Checks whether the specified calculated field can be reprocessed. Returns a validation result indicating if reprocessing is allowed and, if not, provides a reason.   Available for users with 'TENANT_ADMIN' authority.  # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param str calculated_field_id: (required)
:return: CfReprocessingValidationResult
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.validate_calculated_field_reprocessing(id=deserialize_param(id_json, 'CalculatedFieldId'))
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'validate_calculated_field_reprocessing'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"

@mcp.tool()
def verify_and_save_two_fa_account_config(body_json: str, verification_code: str) -> str:
    """
    Verify and save 2FA account config (verifyAndSaveTwoFaAccountConfig)  # noqa: E501

Checks the verification code for submitted config, and if it is correct, saves the provided account config.   Returns whole account's 2FA settings object. Will throw an error (Bad Request) if the provider is not configured for usage.   Available for any authorized user.   # noqa: E501
This method makes a synchronous HTTP request by default. To make an
asynchronous HTTP request, please pass async_req=True

:param async_req bool
:param TwoFaAccountConfig body:
:param str verification_code: verificationCode
:return: AccountTwoFaSettings
         If the method is called asynchronously,
         returns the request thread.
    """
    try:
        client = get_client()
        result = client.verify_and_save_two_fa_account_config(body=deserialize_param(body_json, 'TwoFaAccountConfig'), verification_code=verification_code)
        return format_response(result)
    except ApiException as e:
        # Runtime Permission Check
        if e.status == 403:
            return f"PERMISSION DENIED: Your current user role does not have permission to execute 'verify_and_save_two_fa_account_config'. (HTTP 403)"
        if e.status == 401:
            return "AUTH ERROR: Session invalid. Please check credentials."
        return f"API Error {e.status}: {e.reason}\n{e.body}"
    except Exception as e:
        return f"Error: {str(e)}"
if __name__ == '__main__':
    mcp.run()